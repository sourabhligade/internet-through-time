# This project is fully static — no API keys required for local or production.
# Copy is not needed for deploy. Kept so .gitignore's !.env.example is intentional.
#
# Optional local-only overrides (never commit real .env):
# BASE_URL=http://127.0.0.1:8080
# CI=1
