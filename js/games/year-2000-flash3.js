/**
 * Flash skip % — 2000 G3
 * Class: 3 intros
 * Key: itt00-game-flash3
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="flash3"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
