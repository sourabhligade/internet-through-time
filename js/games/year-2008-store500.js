/**
 * Store 500 — 2008 G1
 * Class: ~500 launch apps
 * Key: itt08-game-store500
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="store500"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
