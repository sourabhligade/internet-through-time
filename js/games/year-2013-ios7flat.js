/**
 * iOS 7 flat — 2013 G4
 * Class: 2 tiles
 * Key: itt13-game-ios7flat
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="ios7flat"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
