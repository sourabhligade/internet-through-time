/**
 * LinkedIn connect — 2003 G2
 * Class: empty blocked
 * Key: itt03-game-licon
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="licon"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
