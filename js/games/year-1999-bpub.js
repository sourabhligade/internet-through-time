/**
 * Blogger publish — 1999 G2
 * Class: title required
 * Key: itt99-game-bpub
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="bpub"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
