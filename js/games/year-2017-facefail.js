/**
 * Face fail — 2017 G1
 * Class: 3 attempts
 * Key: itt17-game-facefail
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="facefail"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
