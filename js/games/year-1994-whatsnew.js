/**
 * What's New ticker — 1994 G1
 * Class: NCSA What's New list
 * Key: itt94-game-whatsnew
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="whatsnew"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
