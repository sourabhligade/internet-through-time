/**
 * Wiki preview — 2001 G1
 * Class: preview then save
 * Key: itt01-game-wikiprev
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="wikiprev"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
