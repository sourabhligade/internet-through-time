/**
 * Maps drag — 2005 G3
 * Class: last view persist
 * Key: itt05-game-mapdrag
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="mapdrag"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
