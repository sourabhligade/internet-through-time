/**
 * Flap flood — 2014 G1
 * Class: Flappy clone year
 * Key: itt14-game-flapflood
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="flapflood"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
