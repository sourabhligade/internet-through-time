/**
 * Spotify invite — 2011 G2
 * Class: 2 checks
 * Key: itt11-game-spotinv
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="spotinv"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
