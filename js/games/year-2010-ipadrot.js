/**
 * iPad rotate — 2010 G2
 * Class: hold
 * Key: itt10-game-ipadrot
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="ipadrot"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
