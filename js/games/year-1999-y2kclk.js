/**
 * Y2K clock — 1999 G3
 * Class: 10s to 2000
 * Key: itt99-game-y2kclk
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="y2kclk"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
