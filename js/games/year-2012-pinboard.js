/**
 * Pin board — 2012 G1
 * Class: image + board
 * Key: itt12-game-pinboard
 * Engine: year-pack-boot.js (taps + optional phrase + optional wait).
 * Incomplete never writes. Museum original · no commercial SWF.
 */
(function () {
  "use strict";
  var host = document.querySelector('[data-year-game][data-game-id="pinboard"]');
  if (!host) return;
  host.setAttribute("data-5x-pack", "1");
})();
