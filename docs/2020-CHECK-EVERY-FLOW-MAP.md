# 2020 check every flow

| Dest | Trap | Save | Key |
|------|------|------|-----|
| `sites/zoom/meeting.html` | Join / Leave without mute+chat | mute → chat ≥2 → Send → Leave | `itt20-zoom` |
| `sites/zoom/index.html` | Join as gold | leftover only | `itt20-zm-ix` |
| Official 2–10 | listed trap | leftover-official / official-verb | `itt20-reels` … `itt20-game-among` |

Gold never writes on Join. Leftover complete never writes `itt20-zoom`.
