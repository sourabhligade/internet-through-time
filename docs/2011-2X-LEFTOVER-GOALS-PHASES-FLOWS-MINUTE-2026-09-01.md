# 2011 leftover dest-minute

Every Pack A room: land · honesty · `[failed-final]` · empty / 0 ticks / 1 hop never writes · trap never writes · verb writes `itt11-<suffix>` only · star `itt11-gplus` not written.
