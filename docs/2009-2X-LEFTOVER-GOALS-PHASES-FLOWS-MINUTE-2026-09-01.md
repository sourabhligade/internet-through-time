# 2009 leftover dest-minute

Every Pack A room: land · honesty · `[failed-final]` · empty / 0 ticks / 1 hop never writes · trap never writes · verb writes `itt09-<suffix>` only · star `itt09-like` not written.
