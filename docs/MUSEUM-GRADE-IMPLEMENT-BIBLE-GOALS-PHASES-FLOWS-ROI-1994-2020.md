# Museum-grade A — implement bible (goals · phases · flows · ROI)

**Date:** 2026-08-16  
**Status:** Execute-from-this. Start **only** when a wave or year is named (`implement W0` · `implement 2011` · `implement Craigslist 1995`).  
**Do not** run all 27 years in one pass.  
**Git only if asked.**

**What this file is:** Minute implement plan to take playable 1994–2020 to **museum-grade A** using the 10k research corpus — **without** adding 5,578 Wikipedia rooms.

**Research (read first, do not re-harvest):**

| Doc | Use |
|-----|-----|
| [`MUSEUM-GRADE-COMPLETE-NEW-FLOWS-SOURCES-1994-2020.md`](MUSEUM-GRADE-COMPLETE-NEW-FLOWS-SOURCES-1994-2020.md) | Gold A rules · first new-site list |
| [`MUSEUM-GRADE-10K-SOURCES-5X-FLOWS-1994-2020.md`](MUSEUM-GRADE-10K-SOURCES-5X-FLOWS-1994-2020.md) | 5× candidates · 10 flows/year |
| [`references/harvest-10k/wikipedia-internet-properties-1994-2020.tsv`](references/harvest-10k/wikipedia-internet-properties-1994-2020.tsv) | 6,088 named properties |
| [`MUSEUM-READY-BAR-1994-2012.md`](MUSEUM-READY-BAR-1994-2012.md) | Layers A–F (already claimed) |
| [`MUSEUM-GRADE-GAP-MAP-1994-2016.md`](MUSEUM-GRADE-GAP-MAP-1994-2016.md) | What A means (hub range stale) |
| [`DISK-TRUTH.md`](DISK-TRUTH.md) · [`NON-DONE.md`](NON-DONE.md) | Ship vs leftover |
| [`ARCHITECTURE.md`](ARCHITECTURE.md) · [`REAL-FLOW-SYSTEM.md`](REAL-FLOW-SYSTEM.md) | Config+content · incomplete never writes |

**Disk now:** 27 years hub-unlocked · `check-all-years` 27/27 · **65,832 links / 0 broken** · `flow-maps.js` **SyntaxError** (2019 leftover 2017 fragment, lines 4096–4118) · prefixes `itt94`…`itt20`.

---

## 0. How to use

### 0.1 You say → you do

| You say | You do |
|---------|--------|
| `implement W0` / `parse-fix maps` | **W0** only (unlocks every year map) |
| `implement W1` | W0 if not done · then **2019** leftover + titleMap + 5× |
| `implement 2011` | **G0–G8** + **Y2011** (Airbnb on map + Win7 shell) |
| `implement Craigslist 1995` | **G0–G8** + **R-NEW** recipe · 1995 only |
| `implement href-fix 1998` | **R-HREF** · Babel Fish only |
| `implement next-flow 1994` | **R-NEXT** · CSotD → Yahoo |
| `implement all years` | **Do not.** Use wave table §2 |

### 0.2 Every phase has

| Field | Meaning |
|-------|---------|
| **Goal** | What done looks like |
| **ROI** | Visitor feel / score lift / why now |
| **Disk start** | What is true before you touch files |
| **Files** | Exact paths |
| **Minute steps** | Numbered. Do in order. |
| **Storage / REAL** | Prefix · incomplete never writes |
| **Flow** | Visitor path (diagram or numbered) |
| **Acceptance** | Pass / fail |
| **Tests** | Commands |
| **Anti-patterns** | Forbidden |

### 0.3 Status marks

| Mark | Meaning |
|------|---------|
| **[x]** | Already true on disk |
| **[ ]** | Open |
| **[~]** | Optional / L4 forever |

### 0.4 Two bars (do not mix)

| Bar | Done when | Blocks A? |
|-----|-----------|-----------|
| **Museum-ready A–F** | Thesis · P0 REAL · honest pixels or failed-final · CAPTURE · e2e · `ittYY` · hub | Already **[x]** 1994–2020 except maps runtime |
| **Museum-grade A** | One-thing **is** a gold machine · leftover = **L4 pixels only** · forest does not drown · map href = writer | **This file** |

**L4 never blocks A:** perfect WA logo, evolt OEM toolbar, modem WAV, live APIs.

**Failed-final is A-legal.** HTML interstitial → log `[failed-final]` · keep RECON text · **never invent pixels**.

---

## 1. Hard rules (every phase)

1. **Config + content only.** No forked `amazon-2011.js`. New mass product → `js/immersion/<product>.js` **or** `year-YYYY-extras.js` boot.  
2. **Prefix `ittYY-*` only.** 1994 = `itt94`. Never bare `itt-`. Never write `itt17-*` from a 2019 page.  
3. **Incomplete never writes.** Empty field / missing checks / Join-only / Accept All → error flash, no `localStorage`.  
4. Reload must restore gold state (list / session / queue / continue row).  
5. **Never invent brand pixels.** WA `id_`/`im_` · WDM · Version Museum · newsroom · or failed-final.  
6. Guided `<ol>` on home stays **exactly 6**. Densify is a chip, not a 7th one-thing.  
7. Lean years (2011–2020) **hard cap ~60 HTML** (2015 already 96 — do not grow).  
8. Forest years (2006–2010) **do not add rooms** unless the user names one product.  
9. 2019/2020: **do not invent** a Live Stats June websites digit (table ends 2018).  
10. One year (or one wave) fully gated before the next.  
11. After ship: e2e green · urlMap path exists · `check-all-years` · one line in DISK-TRUTH if visitor-facing.

---

## 2. ROI scoreboard — what to do first

**ROI = visitor feel (40) + Gold-A score lift (30) + hours inverse (20) + unlocks others (10).**

| Wave | ID | Hours | ROI | Why first |
|------|----|------:|----:|-----------|
| Parse-fix maps | **W0** | 0.5 | **100** | Unlocks **27** maps. One delete. |
| 2019 leftover + titles + 5× | **W1** | 3 | **92** | The bomb is this year’s fragment |
| 2011 Airbnb map + Win7 shell | **W2** | 4 | **88** | Lean year, gold exists, map/shell lie |
| 2012 SoundCloud map + shell | **W3** | 3 | **86** | Same shell as W2 · copy the recipe |
| 2015 strip 2014 titles | **W4** | 2 | **84** | Honesty, no new rooms |
| 2016 Chrome clone + Marketplace | **W5** | 3 | **84** | Grade claims A; disk is 2014 Chrome |
| 2010 pick one gold | **W6** | 2 | **80** | Imgur vs IG split |
| Href-fix pack (96/98/01/03/06/08/13/14/17/18/20) | **W7** | 6 | **78** | Map text becomes true |
| Next-flow chips 1994–2015 | **W8** | 5 | **75** | Handoff is the UX hole |
| 1995 Craigslist | **W9** | 6 | **74** | Biggest missing mass site |
| 2002 Stumble map leaf | **W10** | 2 | **70** | B+ → A without forest |
| 2008 SO + GitHub chip | **W11** | 5 | **68** | SO launched Sep 2008 |
| 2017 reference kit + keys | **W12** | 3 | **65** | Paper A → real A− |
| 2020 grade file + keys | **W13** | 2 | **62** | Docs honesty |
| Optional new rooms (IMDb, NYT, Justin.tv, BuzzFeed…) | **W14** | 8–20 | **40–55** | Only after W0–W8 |

**Already Gold A — do not reopen:** 1995 (except optional Craigslist) · 1996 (except My-portal href) · 1997 · 2005.

```
ROI
100 ┤ W0
 90 ┤ W1  W2
 80 ┤ W3 W4 W5 W6
 70 ┤ W7 W8 W9 W10
 60 ┤ W11 W12 W13
 40 ┤                    W14 new rooms
      parse  honesty  href  handoff  new-site
```

---

## 3. Full-system flow diagrams

### 3.1 Boot (already on disk — do not fork)

```
Hub index.html
  └─ years/YYYY/index.html          shell (OS + browser chrome)
       util → browser-core → config/YYYY.js → browser-YYYY.js
       ITT.bootBrowserYear → iframe #content
  └─ pages/home.html  (or a site room)
       immersion-YYYY.js sets ITT._immersionYear
       immersion/boot.js
         registry FEATURES_BY_YEAR[YYYY]
         config/immersion-YYYY.js
         immersion/create.js → feature.init
  └─ visitor action
       real-flow.js  |  year-5x-pack.js  |  year-YYYY-extras.js  |  product module
       incomplete → no write
       complete   → localStorage ittYY-* { multiStep, real, ts }
                    markTourUsed · revealNextFlow([data-next-flow])
```

### 3.2 Gold visitor loop (what A feels like)

```
Start (home chip data-ott-one-thing="YYYY")
  → Room A (door / trap: Join, Accept All, trial — NEVER writes)
  → Room B (required verbs: mute+chat / Manage+rights / profile+2 titles)
  → Save (gates pass) → ittYY-<gold>
  → [data-next-flow] unhides → Room C (F1 leftover or trail #2)
  → Year game (optional)
  → Exit Year menu → hub
```

Examples already on disk: 1995 SSL · 2014 WA install→chat · 2018 GDPR Manage · 2019 Disney+ Continue · 2020 Zoom mute→chat→leave.

### 3.3 Map / trail / 5× (after W0)

```
pages/map.html
  <script src="js/config/flow-maps.js">     ← MUST parse
  <script src="js/config/flow-maps-3x.js">
  immersion-YYYY.js → flow-map.js
       ITT.flowMaps["YYYY"].branches[].sites[].href
       ITT.flowTrails["YYYY"][n].whenKey  must == writer key

5× leftover
  [data-5x-loop][data-5x-suffix] + [data-5x-req]×N + [data-5x-save]
  year-5x-pack.js → ittYY-<suffix>
  incomplete checks → no write
```

### 3.4 New-room data flow

```
years/YYYY/sites/<slug>/{index,about,<step>}.html
       data-itt-year="YYYY"  data-* hooks only
       <script src="../../../../js/immersion-YYYY.js">
js/immersion/year-YYYY-extras.js   bootXxx(doc)
       OR js/immersion/<product>.js + registry.js year list
js/config/YYYY.js                  urlMap + titleMap + bookmark
js/config/immersion-YYYY.js        nav / footer / tour
js/config/flow-maps.js             branch + star
js/config/flow-trails.js           only if it is trail #1 or replaces a stop
years/YYYY/pages/home.html         chip (do NOT add 7th guided li)
e2e/YYYY-<slug>-real.spec.js       incomplete + complete + reload + no neighbor leak
docs/references/YYYY/CAPTURE-LOG.md
```

### 3.5 REAL gate (copy this contract)

```
click Save
  if checks < min OR field.length < min → feedback error · return
  if click-steps < minClicks → error · return
  localStorage[ittYY-suffix] = { multiStep:true, real:true, year, ts, … }
  markTourUsed
  revealNextFlow  → show [data-next-flow]
reload
  if key exists → paint restored list/session · show next chip
```

**Trap pattern (must use when the period product had a dark door):**

| Year | Trap (no write) | Real save |
|------|-----------------|-----------|
| 1995 | empty SSL fields | name+card+city |
| 2014 | deal page only | chat send after install |
| 2018 | **Accept All** | Manage + rights |
| 2019 | **7-day trial** | Who’s watching + 2 Continues |
| 2020 | **Join** | mute persist + chat + leave → recap |

---

## 4. Shared recipes (copy for every year)

### R-HREF — map/5× href = writer page

**Goal:** Advertised `do` / `whenKey` / 5× suffix land on the HTML that actually writes.

**Minute steps:**
1. Open `js/config/flow-maps.js` year block. Copy every `href` + `do` + `ittYY-*`.  
2. Open `js/config/flow-trails.js` year array. Copy `whenKey`.  
3. `rg "ittYY-|data-storage-key|data-5x-suffix|YX.key\(" years/YYYY js/immersion` — find the **writer**.  
4. If writer ≠ map href: change **href** (prefer) or move the writer (only if the index is the wrong costume).  
5. Align trail `whenKey` to the extras/real-flow suffix (Photobucket: `itt03-photobucket-album`).  
6. Align 5× `data-5x-suffix` to the same suffix **or** leave 5× as a *different* leftover key and change map `do` text so it does not claim the gold key.  
7. `node --check js/config/flow-maps.js`.  
8. e2e: open map (after W0) · click leaf · complete machine · assert key.

**Anti-patterns:** Write the gold key from two pages. Keep “Babel Fish” `do` on AltaVista home.

### R-NEXT — handoff chip

**Goal:** After REAL write, visitor sees one period “next” link to **this year’s** gold or trail #2.

**HTML (hidden until write):**

```html
<p hidden data-next-flow data-next-when-key="itt94-csotd" style="margin-top:12px">
  Next: <a href="../yahoo/index.html"><b>Yahoo @ Stanford</b></a> — browse, don’t search.
</p>
```

**Minute steps:**
1. Confirm the product already calls `ITT.revealNextFlow(doc)` or `YX.showNext(doc)` on success (`real-flow.js`, extras, one-thing-machines, 5× pack already do).  
2. Add the `<p hidden data-next-flow>` on the **writer page**.  
3. `data-next-when-key` = full storage key (`ittYY-suffix`). Multiple keys: comma-separated.  
4. Href is **same-year** relative. No `/years/2015/` from 2016 except labeled continuity.  
5. Do not add a 7th `<li>` to the guided `<ol>`.  
6. e2e: complete write → chip visible → click → destination 200.

**Already dense:** 2016 (~28) · 2017 (~33) · 2018 (~29). **Thin:** 1994–2015.

### R-5X — leftover loops

**Goal:** F1–F5 either work as 2-check REAL **or** the map stops advertising them.

**HTML:**

```html
<div data-5x-loop data-5x-suffix="iuma" data-5x-year="1994">
  <label><input type="checkbox" data-5x-req="a"> Helper-app era — no CD store</label>
  <label><input type="checkbox" data-5x-req="b"> I waited / I opened a band</label>
  <button type="button" data-5x-save>Save 5×</button>
  <span data-5x-status></span>
</div>
```

**Minute steps:**
1. Confirm `year-5x-pack.js` is in `registry.js` for that year (it is for all 27).  
2. Years **2010, 2011, 2012, 2019** have **zero** `data-5x-*` — either paste the block on 5 keep-set rooms **or** delete the “5× F1–F5” branch in `flow-maps.js`.  
3. Suffix must not collide with the gold key unless it is *the* leftover overlay (document in map `do`).  
4. e2e: `e2e/YYYY-5x-live.spec.js` — incomplete no-write · complete writes `ittYY-suffix`.

### R-NEW — new year-true room

**Goal:** One mass product, ≥2 pages, REAL, discoverable, gated.

**Files (create):**
```
years/YYYY/sites/<slug>/index.html
years/YYYY/sites/<slug>/<step>.html
years/YYYY/sites/<slug>/about.html
assets/period/YYYY/<slug>/README-AUTHENTICITY.txt
```

**Files (edit):**
```
js/config/YYYY.js                         urlMap + titleMap + defaultBookmarks
js/config/immersion-YYYY.js               nav/footer/tour optional
js/config/flow-maps.js                    new sites[] under a P1 branch — not a 2nd star
js/immersion/year-YYYY-extras.js          bootXxx  (create file + registry if missing)
js/immersion/registry.js                  only if new shared module
years/YYYY/pages/home.html                chip in 3×/trail strip — NOT guided 6
docs/references/YYYY/CAPTURE-LOG.md
e2e/YYYY-<slug>-real.spec.js
```

**Page template:**

```html
<!DOCTYPE html>
<html lang="en" data-itt-year="YYYY">
<head>
<meta charset="utf-8">
<title>PRODUCT — YYYY period title</title>
<link rel="stylesheet" href="../../../../css/period-YYYY.css">
</head>
<body bgcolor="#ffffff" text="#000000" link="#0000cc" vlink="#551a8b">
<div id="itt-nav-slot" class="itt-nav-slot" aria-hidden="true"></div>
<p><a href="index.html">Home</a> · <a href="about.html">About</a></p>
<!-- hooks -->
<p data-prod-status data-itt-action-status></p>
<p hidden data-next-flow data-next-when-key="ittYY-suffix">Next: …</p>
<script src="../../../../js/immersion-YYYY.js"></script>
</body>
</html>
```

**Extras boot (append inside existing IIFE `bootAll`):**

```js
function bootSlug(doc) {
  doc = doc || document;
  if (!doc.querySelector("[data-slug-save]")) return;
  var YX = ITT.YearExtras.forYear("YYYY");
  var st = doc.querySelector("[data-prod-status]");
  var prev = YX.loadJSON(YX.key("suffix"), null);
  if (prev) { /* paint */ YX.showNext(doc); }
  doc.querySelector("[data-slug-save]").addEventListener("click", function () {
    var v = YX.val(doc, "[name='q']");
    if (v.length < 2) { YX.feedback("Type something first.", st, { error: true }); return; }
    YX.saveJSON(YX.key("suffix"), { multiStep: true, real: true, year: "YYYY", ts: Date.now(), q: v });
    YX.markUsed();
    YX.feedback("Saved · " + YX.key("suffix"), st);
    YX.showNext(doc);
  });
}
```

**urlMap row:** `"sites/<slug>/index.html": "http://museum.local/years/YYYY/sites/<slug>/index.html"`  
Period display URLs (ebay.com etc.) only when the year actually used that host.

**Acceptance:** incomplete no-write · complete write · reload persist · no `ittXX` neighbor leak · `python3 scripts/process/check_urlmap.py` · `python3 scripts/audit-internal-links.py`.

**Anti-patterns:** Guided 7th item · invent logo · clone 2004 Facebook into 2016 · `document.write` product JS.

### R-SHELL — 2011–2013 Win7 honesty

**Goal:** Body class, Start banner, toolbar GIFs, and shutdown confirm match **Win7 + IE9**, not XP + 2007 chrome.

**Disk start:** `body.os-win7.browser-ie9` but:
- `<img src="../../assets/period/2007/chrome/btn-*.gif">`
- `<span class="start-banner-text">Windows<b>XP</b></span>`
- `confirm("Shut down Windows XP immersion…")`

**Minute steps:**
1. Copy toolbar GIFs only if a **dated** IE9/Win7 crop exists (evolt / GUIdebook / WDM). Else keep 2007 GIFs and add an honesty line: “Toolbar bitmaps are IE7 continuity — shell class is IE9.” **Do not invent IE9 pixels.**  
2. Change Start banner text to **Windows 7** (2011–12) or keep Win7 residual (2013).  
3. Change shutdown confirm string.  
4. Open Location copy: “Internet Explorer 9”, not generic IE.  
5. `e2e/YYYY-shell-honesty.spec.js` (2013 already has one — extend pattern to 2011/2012).

### R-VOICE — year titles

**Goal:** `<title>` and `titleMap` match the year on the folder.

**2015 (confirmed on disk):** WhatsApp / Oculus / Secret compose titled 2014; titleMap Touch ID 5s, Snap Stories 2013, iOS 8 / Pay.  
**2016:** `sites/chrome/index.html` title “Google Chrome — 2014 browser war”, class `ch-2014`, `data-itt-primary-year="2008"`.  
**2019:** titleMap “iPhone 5s — 2019”, “Windows 10 free upgrade — 2019”.

**Minute steps:** `rg -n "2014|2013|5s|free upgrade|ch-2014" years/YYYY js/config/YYYY.js` · fix titles · if the room is *intentional* residual, add a one-line “continuity archive · YEAR” chip and `data-itt-primary-year` of the **birth** year.

### R-CAPTURE — pixels

```
1. Find CDX: web.archive.org/web/*/http://example.com  filter year-correct
2. Open id_ capture · extract logo/chrome URLs
3. Download …/web/{ts}id_/{orig} · `file` must be GIF/JPEG/PNG
4. Install assets/period/YYYY/<brand>/logo-wa.gif (keep logo-recon.gif)
5. Log CAPTURE-LOG [wa] or [failed] / [failed-final]
6. Wire img src · do not claim WA if failed-final
```

Create `docs/references/2017/` — **missing**.

---

## 5. Shared phases G0–G8 (run once per named year)

Do these around that year’s Y-phases. Do not skip G7.

### G0 — Freeze `[ ]`

**Goal:** Thesis, bans, gold key, HTML cap locked.  
**ROI:** Prevents forest / anachronism.

**Minute steps:**
1. Read year `*-READ-FIRST.md` or `*-MUSEUM-GRADE.md` + this file’s **Y-YYYY** section.  
2. Write on paper: gold key · trap · writer page · HTML cap · bans.  
3. Confirm one-thing chip on `years/YYYY/pages/home.html` `data-ott-one-thing="YYYY"`.  
4. Do not change the gold product unless the user says so (2010 Imgur vs IG is the only allowed pick).

**Acceptance:** One paragraph freeze in the PR/commit message. No 2nd star.

### G1 — Inventory `[ ]`

**Goal:** Touch list. No duplicate rooms.

**Minute steps:**
1. `ls years/YYYY/sites`  
2. `rg "ittYY-|data-ott-one-thing|data-5x-|data-next-flow" years/YYYY`  
3. Open TSV filter `^YYYY` in `references/harvest-10k/…tsv` — pick **at most** the candidates in Y-YYYY.  
4. List files you will edit (copy §4 recipe).

### G2 — Wire plan `[ ]`

**Goal:** urlMap · map branch · chip · extras boot named.

**Minute steps:** Draw the 3.2 loop for this year. Name every href.

### G3 — Rooms `[ ]`

**Goal:** Pages load in the year shell (`years/YYYY/index.html` → Open Location or in-iframe nav).

### G4 — Product JS `[ ]`

**Goal:** Incomplete blocks · complete writes typed blob.

### G5 — Persist + next `[ ]`

**Goal:** Reload restores · next chip shows.

### G6 — Discover `[ ]`

**Goal:** Home chip · map leaf (after W0) · bookmark · titleMap.

### G7 — Gates `[ ]`

```
node --check js/config/flow-maps.js
python3 scripts/process/check_urlmap.py
python3 scripts/audit-internal-links.py
python3 scripts/check-all-years.py
npx playwright test e2e/YYYY-*.spec.js e2e/one-thing-per-year.spec.js -g "YYYY" --workers=1
```

If year alias exists: `npm run test:e2e:YYYY` (note 1994–96/98–99 aliases are **5×-only** — run the named specs too).

### G8 — Honesty `[ ]`

**Goal:** CAPTURE row · no invented pixels · DISK-TRUTH one-liner if ship-facing · HTML counts in grade/READ-FIRST match `find years/YYYY -name '*.html' | wc -l`.

---

## 6. Wave W0 — parse-fix maps `[ ]`  ROI 100

### Goal
`js/config/flow-maps.js` parses. Every `pages/map.html` renders `.itt-fmap` with branches and site links. **2019** map has no 2017 Fortnite / 280 / WannaCry / Face ID leaves.

### Why
The file is one IIFE. A SyntaxError at line 4174 means **`ITT.flowMaps` is never assigned for any year**. 27 map pages show “Flow map data missing.”

### Disk start
```
4094        ]
4095      },
4096
4097      {   ← leftover 2017 objects
...
4117          ]
4118},
4119
4120      { "label": "Enter", ...
```
`node --check js/config/flow-maps.js` → `SyntaxError: Unexpected token ']'`.  
`git show HEAD:js/config/flow-maps.js` **parses**.

### Files
- Edit: `js/config/flow-maps.js` **only** (delete lines 4096–4118 inclusive).  
- Do not restyle 2019 5× F1–F5 (those stay).

### Minute steps
1. Open `js/config/flow-maps.js` at `ITT.flowMaps["2019"]`.  
2. Confirm the real 5× branch ends at Star Disney+ (`sites/disneyplus/home.html`) then `] },`.  
3. Select from the blank line after that `},` through the stray `] },` after Star Face ID. **Delete.**  
4. Confirm next object is `"label": "Enter"` / 2019 lobby.  
5. `node --check js/config/flow-maps.js` — must print nothing, exit 0.  
6. `python3 -c` or node eval: load the IIFE in vm and `Object.keys(ITT.flowMaps)` includes `1994`…`2020`.  
7. Spot-check 2019 unique hrefs: **no** `sites/wannacry/index.html`, `sites/iphone/x.html`, `sites/fortnite/index.html`.  
8. Serve `python3 -m http.server 8080 --bind 127.0.0.1`. Open `/years/1994/pages/map.html` and `/years/2019/pages/map.html`. Expect `.itt-fmap-branch` visible, `.itt-fmap-missing` count 0.  
9. `npx playwright test e2e/flow-maps.spec.js --workers=1`.

### Storage / REAL
None.

### Flow
```
map.html → flow-maps.js parses → flow-map.js boot → tree + 10 trails
```

### Acceptance
- `node --check` exit 0  
- 2019 has Disney+ / TikTok / Arcade / TV+ / Stadia / iPhone 11 only in 5×  
- 1994 map shows CSotD / Yahoo  
- flow-maps e2e green  

### Anti-patterns
Rewrite the whole 2019 object. “Fix” by commenting with `//`. Leave the 2017 hrefs as 2019 continuity.

---

## 7. Year phases (after W0)

Each **Y-YYYY** assumes G0–G8. ROI is additional to W0.

---

### Y-1994 `[ ]`  ROI 70 (after W0)

**Gold (locked):** CSotD `itt94-csotd` · `sites/csotd/index.html`  
**Cap:** do not grow past ~190 HTML.

| Phase | Goal | Minute steps | Files |
|-------|------|--------------|-------|
| **Y94-1** | FishCam timer is the gate | `media-1994.js`: write `itt94-fishcam` only after still timer; 5× overlay may stay as *different* suffix or be removed from “waited the timer” `do` | `js/immersion/media-1994.js` · `years/1994/sites/fishcam/index.html` · flow-maps `do` |
| **Y94-2** | WH imagemap writes | click region → `itt94-wh-map` (or change map `do` to 5×-only) | `years/1994/sites/whitehouse/index.html` · one-thing or extras |
| **Y94-3** | Yahoo 5× cannot bypass 3-hub | 5× suffix ≠ `yahoo-wander` **or** disable 5× on index | `sites/yahoo/index.html` · `one-thing-machines.js` `bootYahooWander` |
| **Y94-4** | R-NEXT | After CSotD write → Yahoo | `sites/csotd/index.html` |
| **Y94-5 [~]** | R-NEW IMDb or FogCam | Only if named | R-NEW |

**Tests:** `e2e/1994-csotd-real.spec.js` · `1994-yahoo-wander.spec.js` · `1994-5x-live.spec.js` · `e2e/flow-maps.spec.js` 1994.

**Anti-patterns:** `period-1994.css` full fork without need · evolt NN1 required.

---

### Y-1995 `[ ]`  ROI 74 if Craigslist · else 55 (href only)

**Gold (locked):** SSL `itt95-ssl-checkout`.

| Phase | Goal |
|-------|------|
| **Y95-1** | AuctionWeb 5× on **item** bid form, not list index (R-HREF) |
| **Y95-2** | R-NEXT SSL → AuctionWeb item |
| **Y95-3** | **R-NEW Craigslist** — city → category → compose (no real ads). Sources: Craigslist 1995 history · WA 1996–97 if 1995 id_ fails. Key `itt95-craigslist`. Map P1 branch “Local classifieds”. Chip on home 3× strip. **Do not** steal SSL star. |
| **Y95-4 [~]** | Salon.com article → letters |

**Files (Craigslist):** `years/1995/sites/craigslist/{index,city,post,about}.html` · `js/config/1995.js` urlMap · `js/config/immersion-1995.js` · `js/config/flow-maps.js` · extras or small `immersion` boot in `one-thing-machines` **only if** you add `bootCraigslist` — prefer 1995 extras file **or** page-local `registerLocal` in a new `js/immersion/craigslist.js` + registry `1995` list.  
**e2e:** `e2e/1995-craigslist-real.spec.js`.

---

### Y-1996 `[ ]`  ROI 72

**Gold (locked):** portal wars `itt96-portal-wars`.

| Phase | Goal |
|-------|------|
| **Y96-1** | F1 My portal href → `sites/yahoo/my.html` (and/or Excite my). R-HREF |
| **Y96-2** | R-NEXT wars → Hotmail |
| **Y96-3 [~]** | R-NEW nytimes.com section → story |

---

### Y-1997 `[ ]`  ROI 50

**Gold (locked):** PointCast `itt97-pointcast`.  
**Y97-1** eBay 5× on `item-laptop.html`. **Y97-2** R-NEXT PointCast → ICQ. **Y97-3 [~]** Netflix-mail queue (founded 1997 · **no streaming**).

---

### Y-1998 `[ ]`  ROI 76

**Gold (locked):** Lucky `itt98-lucky`.

| Phase | Goal |
|-------|------|
| **Y98-1** | Babel Fish: map href `sites/altavista/babelfish.html` · 5× `do` must not claim typed pair on index |
| **Y98-2** | R-NEXT Lucky → Amazon Music |
| **Y98-3 [~]** | Open Diary or Deja News (R-NEW) |

---

### Y-1999 `[ ]`  ROI 60

**Gold (locked):** AIM `itt99-aim`.  
**Y99-1** Guided `<ol>` from 7 → **6** (X6). **Y99-2** Napster F1 `data-trail-keys="itt99-napster"`. **Y99-3 [~]** LiveJournal year-true room (now only 2002).

---

### Y-2000 `[ ]`  ROI 68

**Gold (locked):** MapQuest `itt00-mapquest` / `-trip`.

| Phase | Goal | Files |
|-------|------|-------|
| **Y00-1** | Home loads `js/immersion-2000.js` once, **not** `config/immersion-2000.js` twice | `years/2000/pages/home.html` |
| **Y00-2** | Strip Y2K / Hampster / Zombo / YGM from 2000 gold 3× (leave as labeled archive or remove) | `pages/home.html` |
| **Y00-3** | R-NEXT MapQuest → Amazon smile | `sites/mapquest/directions.html` |
| **Y00-4 [~]** | TripAdvisor R-NEW | |

---

### Y-2001 `[ ]`  ROI 77

**Gold (locked):** MSN `itt01-msn`.

| Phase | Goal |
|-------|------|
| **Y01-1** | Map F2 href → `sites/apple/ipod.html` (and iTunes). Stop 5× `ipod` on Think Different `apple/index.html` |
| **Y01-2** | Remove Windows Live link from MSN (2013 product) |
| **Y01-3** | Home boot: `immersion-2001.js` not config-only |
| **Y01-4 [~]** | BitTorrent literacy R-NEW (no payload) |

---

### Y-2002 `[ ]`  ROI 70

**Gold (locked):** Stumble `itt02-stumble`.  
**Y02-1** Add named map leaf “StumbleUpon” (today Friendster is the social leaf).  
**Y02-2** R-NEXT Stumble → Friendster profile.  
**Do not** add forest rooms.

---

### Y-2003 `[ ]`  ROI 73

**Gold (locked):** Photobucket → MySpace.

| Phase | Goal |
|-------|------|
| **Y03-1** | Trail `whenKey` = `itt03-photobucket-album` (or write both keys — prefer one) |
| **Y03-2** | MySpace Top 8 **picker** (8 slots) writes `itt03-ms-top8` — not 5× “saved 8” |
| **Y03-3** | R-NEXT album → MySpace |

---

### Y-2004 `[ ]`  ROI 58

**Gold (locked):** thefacebook networks.  
**Y04-1** After join, friends/poke graph persist (already friends e2e — expose from chip).  
**Y04-2** Gmail F2 href → `invite.html` / `compose.html`.  
**Do not** add rooms (84 already).

---

### Y-2005 `[ ]`  ROI 40

**Already A.** Only: YouTube 5× on upload/watch (R-HREF). No new rooms. No `year-2005-*.js` required (HoverChop reuse is L4).

---

### Y-2006 `[ ]`  ROI 64

**Gold (locked):** Twitter `itt06-tweets`. **Do not grow 302 HTML.**

| Phase | Goal |
|-------|------|
| **Y06-1** | Feed F2 href → `sites/facebook/feed.html` |
| **Y06-2** | R-NEXT tweet → Feed |
| **Y06-3 [~]** | CAPTURE P0 Twitter/Feed/YT/Digg — failed-final OK |

---

### Y-2007 `[ ]`  ROI 62

**Gold (locked):** iPhone `itt07-iphone`.  
**Y07-1** Street View 5× → `maps/streetview.html`.  
**Y07-2** R-NEXT iPhone → Street View.  
**Y07-3 [~]** Justin.tv R-NEW (broadcast/watch literacy).  
**Do not** grow 318 HTML otherwise.

---

### Y-2008 `[ ]`  ROI 68

**Gold (locked):** GitHub issue write. Mass object = App Store.

| Phase | Goal |
|-------|------|
| **Y08-1** | Home chip href → `sites/github/issue.html` (index stays lobby) |
| **Y08-2** | Guided #1 stays App Store (or swap copy so mass vs builder is honest) |
| **Y08-3** | urlMap `sites/playable/index.html` (file exists, unmapped) |
| **Y08-4** | **R-NEW Stack Overflow** (Sep 2008) ask → answer → accept `itt08-so-accepted` — **or** dual-date chip to 2009 room with “launched Sep 2008” |
| **Y08-5** | R-NEXT issue → App Store |

---

### Y-2009 `[ ]`  ROI 55

**Gold (locked):** Like `itt09-fb-likes` on `feed.html`.  
**Y09-1** Add `data-itt-year="2009"` on `feed.html`.  
**Y09-2** FarmVille 5× must not replace plant/harvest writer.  
**Y09-3** R-NEXT Like → Farm plant.  
**Do not** grow 339 HTML.

---

### Y-2010 `[ ]`  ROI 80

**Gold pick (user must choose one):**

| Option | Chip | Map F1 | Trail #1 |
|--------|------|--------|----------|
| **A Imgur (current chip)** | `sites/imgur/index.html` | Imgur | Imgur |
| **B Instagram (mass)** | `sites/instagram/index.html` | IG filter→share | IG |

**Y10-1** Make chip, map F1, trail #1, and `e2e/one-thing-per-year.spec.js` **the same**.  
**Y10-2** R-5X: add 5× on 5 rooms **or** delete 5× branch.  
**Y10-3** About: remove one-click thesis ack if a 2-check panel exists.  
**Do not** grow 379 HTML.

---

### Y-2011 `[ ]`  ROI 88

**Gold (locked):** Airbnb `itt11-airbnb`.

| Phase | Goal | Files |
|-------|------|-------|
| **Y11-1** | Map leaf Airbnb (city → listing → request) | `flow-maps.js` 2011 |
| **Y11-2** | Trail Siri href = `sites/iphone/siri.html` | `flow-trails.js` |
| **Y11-3** | R-SHELL Win7 / IE9 | `years/2011/index.html` |
| **Y11-4** | R-5X or drop F1–F5 copy | home + map |
| **Y11-5** | Path CSS `itt15-*` → `itt11-*` | `sites/path/` |
| **Y11-6** | READ-FIRST / DISK-TRUTH HTML **52** | docs |
| **Y11-7 [~]** | `year-2011-extras.js` only if 5×/Siri need it | registry |

**Tests:** `e2e/2011-airbnb-listing.spec.js` · add map leaf to `2011-flows` or `flow-maps.spec.js`.  
**Cap:** 60 HTML.

---

### Y-2012 `[ ]`  ROI 86

**Gold (locked):** SoundCloud `itt12-soundcloud`.

| Phase | Goal |
|-------|------|
| **Y12-1** | Map leaf SoundCloud `track.html` timed comment |
| **Y12-2** | Trail nextLabels: IPO href vs “FB buys IG”; Win8 vs iPad mini |
| **Y12-3** | R-SHELL (copy 2011) |
| **Y12-4** | R-5X or drop |
| **Y12-5 [~]** | BuzzFeed R-NEW list/share theater |
| **Y12-6** | READ-FIRST “forest” line → pruned 49 HTML |

**Cap:** 60 HTML.

---

### Y-2013 `[ ]`  ROI 72

**Gold (locked):** Vine `itt13-vine-posts`.

| Phase | Goal |
|-------|------|
| **Y13-1** | Snap 24h: map F2 href `snapchat/story.html` · key `itt13-snap-story` |
| **Y13-2** | iOS7: map href `iphone/ios7.html` / Touch ID `touchid.html` |
| **Y13-3** | Snowden writer key = map key |
| **Y13-4** | R-SHELL |
| **Y13-5** | READ-FIRST “tree not yet” → live 61 HTML |

---

### Y-2014 `[ ]`  ROI 66

**Gold (locked):** WhatsApp `itt14-wa-install` / `itt14-wa-msgs`.

| Phase | Goal |
|-------|------|
| **Y14-1** | Twitch: change map `do` to go-live **or** add chat+reload writer `itt14-twitch` |
| **Y14-2** | Heartbleed 5× vs `itt14-heartbleed-rotate` — don’t claim rotate on index |
| **Y14-3** | Home `portal-2013` class → 2014 |
| **Y14-4** | Grade/DISK-TRUTH HTML **62** not 43 |
| **Y14-5 [~]** | Venmo R-NEW literacy |

---

### Y-2015 `[ ]`  ROI 84

**Gold (locked):** Watch `itt15-watch`.

| Phase | Goal |
|-------|------|
| **Y15-1** | R-VOICE: WhatsApp/Oculus/Secret titles; titleMap 5s / Stories 2013 / iOS8 / Pay |
| **Y15-2** | Periscope / Music / Photos: one key each (map vs extras) |
| **Y15-3** | Discord: load `bootDiscord15` via extras/registry **or** stop claiming the machine |
| **Y15-4** | Open Location “Internet Explorer” → Edge/Chrome honesty if body is `browser-chrome` |

**Do not add rooms (96 already).**

---

### Y-2016 `[ ]`  ROI 84

**Gold (locked):** Stories `itt16-ig-stories`.

| Phase | Goal |
|-------|------|
| **Y16-1** | Rewrite `sites/chrome/index.html` as 2016 habit (not `ch-2014` / 2014 war / `primary-year="2008"`) |
| **Y16-2** | F5 href → `facebook/marketplace.html` · key `itt16-marketplace` |
| **Y16-3** | Stories `data-itt-primary-year` 2016 or labeled 2010 residual |
| **Y16-4** | Grade: remove Allo / LinkedIn / Switch as live P1 |
| **Y16-5** | HTML count 54 |

**Do not restore forest backup.**

---

### Y-2017 `[ ]`  ROI 65

**Gold (locked):** Face ID `itt17-faceid`.

| Phase | Goal |
|-------|------|
| **Y17-1** | Create `docs/references/2017/CAPTURE-LOG.md` + `assets/period/2017/README-PIXELS.txt` |
| **Y17-2** | F3 href `twitter/280.html` · key `itt17-twitter280` |
| **Y17-3** | Netflix 5× key = `itt17-nf-mylist` |
| **Y17-4** | Next-link Face ID → `iphone/x.html` not 8 residual |
| **Y17-5** | Grade card: A → **A−** until kit exists (honesty) |

---

### Y-2018 `[ ]`  ROI 72

**Gold (locked):** GDPR `itt18-gdpr` · Accept All never writes.

| Phase | Goal |
|-------|------|
| **Y18-1** | F1 href `tiktok/fyp.html` |
| **Y18-2** | F4 href `chrome/not-secure.html` |
| **Y18-3** | Grade footer “2019+ not on disk” → false |
| **Y18-4** | READ-FIRST “do not scaffold” → live 48 HTML |

---

### Y-2019 `[ ]`  ROI 92 (after W0)

**Gold (locked):** Disney+ `itt19-disneyplus`.

| Phase | Goal |
|-------|------|
| **Y19-1** | W0 (if not done) |
| **Y19-2** | R-5X on TikTok / Arcade / TV+ / Stadia / iPhone 11 **or** drop 5× heading |
| **Y19-3** | titleMap: iPhone 11 not 5s · Win10 not “free upgrade” |
| **Y19-4** | Trail TikTok `whenKey` = `itt19-tiktok` |
| **Y19-5** | HTML 52 · READ-FIRST 49 |

**Tests:** `npm run test:e2e:2019` plus `2019-flow-link-verify` · `2019-game` (not in the npm alias — add or run named).

---

### Y-2020 `[ ]`  ROI 62

**Gold (locked):** Zoom `itt20-zoom`.

| Phase | Goal |
|-------|------|
| **Y20-1** | Write `docs/2020-MUSEUM-GRADE.md` (copy 2018 card shape) |
| **Y20-2** | READ-FIRST title: remove “do not scaffold yet”; game **is** on disk |
| **Y20-3** | CCPA one key `itt20-ccpa-dns` (home 5× + extras + shop `[data-dns]` or drop shop claim) |
| **Y20-4** | Quibi 5× key = extras key |
| **Y20-5** | R-NEXT recap → Reels |

**Bans:** no ChatGPT, no Jan 6 gold, no ATT-as-default, no case-count dashboard as one-thing.

---

## 8. e2e recipes (every gold / new room)

```js
// e2e/YYYY-slug-real.spec.js
const { test, expect } = require("@playwright/test");
const { enterYear, contentFrame, goInFrame } = require("./helpers");

test("incomplete never writes", async ({ page }) => {
  await enterYear(page, "YYYY");
  await goInFrame(page, "sites/slug/index.html");
  const f = contentFrame(page);
  await f.locator("[data-slug-save]").click();
  const n = await page.evaluate(() => localStorage.getItem("ittYY-suffix"));
  expect(n).toBe(null);
});

test("complete writes and reload persists", async ({ page }) => {
  // fill / check / click
  // expect localStorage JSON.real === true
  // reload iframe · expect painted state
});

test("no neighbor leak", async ({ page }) => {
  // after write, enterYear neighbor · key ittYY-suffix absent
});
```

Use `helpers.js` `enterYear` / `goInFrame`. Do **not** call `killOverlays` in shell-honesty specs.

**Isolation:** prefix `itt14` must not appear when testing 2015.

---

## 9. Definition of done (museum-grade A, one year)

A year is **A** when:

1. W0 green (maps parse).  
2. One-thing chip = writer path = trail `whenKey` = e2e key.  
3. Incomplete never writes · reload persist.  
4. Map F-loops that claim a key land on the writer **or** the `do` text is honesty-only.  
5. At least one `data-next-flow` from gold to trail #2.  
6. Shell class / Start / Open Location match the year (2011–13 especially).  
7. Titles are year-true or labeled continuity.  
8. CAPTURE exists (create 2017). Pixels WA or failed-final.  
9. HTML count in DISK-TRUTH / READ-FIRST / grade = `find years/YYYY -name '*.html' | wc -l`.  
10. `check-all-years` · urlMap · link audit · year e2e green.  
11. Residual list is **L4 only**.  
12. Lean years stay lean. Forest years did not grow.

---

## 10. Anti-patterns (will fail review)

- Adding rooms from the 5,578-name JSON “to hit 10k”.  
- Second one-thing star.  
- Guided 7th `<li>`.  
- Invented brand logos / ripped SWF / live Zoom.  
- `itt17-*` writes from 2019 pages.  
- Restoring `/tmp/itt-*-forest-backup-*`.  
- Claiming Live Stats June 2019/2020 website totals.  
- `if (year === "2018")` sprawl in shared modules.  
- Shipping W0 “fixed” without `node --check`.  
- `npm run test:e2e:1994` as the only 1994 proof (that alias is 5×-only).

---

## 11. Wave checklist (print this)

```
[ ] W0  parse-fix flow-maps.js
[ ] W1  Y-2019 leftover + titles + 5×
[ ] W2  Y-2011 Airbnb map + Win7 shell
[ ] W3  Y-2012 SoundCloud map + shell
[ ] W4  Y-2015 voice
[ ] W5  Y-2016 Chrome + Marketplace
[ ] W6  Y-2010 one gold
[ ] W7  R-HREF pack (96 98 01 03 06 08 13 14 17 18 20)
[ ] W8  R-NEXT 1994–2015 gold rooms
[ ] W9  Y-1995 Craigslist          [only if named]
[ ] W10 Y-2002 Stumble leaf
[ ] W11 Y-2008 SO + GitHub chip
[ ] W12 Y-2017 kit + keys
[ ] W13 Y-2020 grade + keys
[ ] W14 optional R-NEW list        [only if named]
```

---

*Educational reconstruction only. Implement one wave. Incomplete never writes. Never invent pixels.*
