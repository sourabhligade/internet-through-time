# 2020 leftover dest-minute

Every Pack A room: land · yellow honesty · `[failed-final]` · empty / 0 ticks / 1 hop / skip wait never writes · trap never writes · verb writes `itt20-<suffix>` only · reload persist · star `itt20-zoom` not written.

Join `sites/zoom/index.html` is leftover `itt20-zm-ix`. Waiting Room `sites/zoom/wait.html` needs `data-lo-wait`.
