# Period CSS year deltas

**Rule:** Year content styles compose via `@import` of the previous year.  
**Do not** full-fork 1997 into every later file.  
**Do not** truncate signature rules without checking HTML class usage + authenticity densify tests.

## Chain (2001+)

```
period-1995.css
  ← period-1996.css
    ← period-1997.css
      ← period-1998.css   (still large continuity pack)
        ← period-1999.css
          ← period-2000.css
            ← period-2001.css   # wiki + iPod only
              ← period-2002.css   # Wired + KaZaA helpers
                ← period-2003.css   # MySpace · iTunes · WP · LinkedIn
                  ← period-2004.css   # Gmail · Flickr · FB · Firefox
                    ← period-2005.css   # YouTube · Maps · Reddit · Digg
```

## Backups of pre-rewrite slim files

`css/_delta_backup_2026-07-24/` (project-local only)

## Verify

```bash
python3 scripts/test-authenticity.py  # 2003 densify checks .ms-body .it-chrome .li-body
```
