# 2011 check every flow

| Dest | Trap | Save | Key |
|------|------|------|-----|
| `sites/googleplus/index.html` | G+ won / empty circle / 0–1 people | circle ≥2 + Ada + Al + Hangout | `itt11-gplus` |
| Official 2–10 | listed trap | official-verb leftover | `itt11-spotify` … `itt11-game-letterswap` |

Leftover complete never writes `itt11-gplus`.
