# 2001 — every flow · minute · incomplete · complete · trap

**Status:** RESEARCH LOCK. Do not implement until this year is named as a wave.
**Feel:** anyone can edit · iPod white · XP
**Star:** Wikipedia UseMod · `itt01-wiki` · `sites/wikipedia/edit.html`
**ILS June:** 29,254,370 · 500,609,240
**Leftover-official dests in this file:** 194
**Official 10:** frozen in `js/config/flow-trails.js`. **Guided 6:** frozen in `ui/year/start-data.js`.
**Law:** leftover never writes `itt01-wiki`. Trap / empty / 0 ticks / wrong pick / skip-wait never write. Neighbor-year keys stay empty.
**5k:** research envelope (ILS + Hosting.com + Pew + this dest list). Not 5,000 new rooms.

Open dests as `/years/2001/<href>`. Serve `python3 -m http.server 8080 --bind 127.0.0.1`.

---

## G0 · Guided 6 (frozen · not a 7th item)

Start at `years/2001/pages/home.html` (or the year shell Starting Point).
The painted `<ol>` is **exactly 6**. A 7th `<li>` is a fail.
1. About 2001 — memory · jukebox · monopoly
2. Wikipedia — preview is not Save
3. Wayback leftover — Oct 24 public
4. iTunes library leftover — no Store
5. iPod leftover — 1,000 songs · $399
6. Year flow map

Walk: About → ★ gold → official leftovers named in the list → map. Visiting a guided dest is not a write. Only the dest machine writes.

---

## G1 · ★ GOLD · `itt01-wiki`

**Room:** `years/2001/sites/wikipedia/edit.html`
**Hook:** `data-wiki-save`
**Nostalgia:** Wikipedia UseMod — this is the year chip. One star.

**Incomplete never writes**
- Preview only · empty body · Google-as-gold trap never write.
- Leftover panel on this room (if any) writes a leftover key, **not** `itt01-wiki`.

**Complete writes**
- Edit + Save (not Preview). Writes itt01-wiki / itt01-wiki-pages per dest hook.
- Payload is REAL (`real: true`) · year `2001` · key `itt01-wiki`.
- Reload still shows saved. Neighbor `itt-wiki` — do not write `itt00-*` or `itt02-*` gold.
- **Next after gold:** Wayback → `years/2001/sites/archive/index.html`

**Do not:** second star · leftover save that stamps this key · dest-field plaque as the gold machine.

---

## G2 · Official 10 (frozen)

Each official dest is its own flow. n=1 is gold (above). n=2–10 write **their** `whenKey`, never the star.

### OFF-01 · Wikipedia · `itt01-wiki`

**Room:** `years/2001/sites/wikipedia/edit.html`
**This is gold.** Same walk as G1. Writes `itt01-wiki`.
- **Next:** Wayback → `years/2001/sites/archive/index.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `amazon`

### OFF-02 · Wayback · `itt01-wayback`

**Room:** `years/2001/sites/archive/index.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-wayback` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** iTunes → `years/2001/sites/itunes/index.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `amazon`

### OFF-03 · iTunes library · `itt01-itunes`

**Room:** `years/2001/sites/itunes/index.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-itunes` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** iPod → `years/2001/sites/apple/ipod.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `amazon`

### OFF-04 · iPod · `itt01-ipod`

**Room:** `years/2001/sites/apple/ipod.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-ipod` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** Napster → `years/2001/sites/napster/index.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `amazon`

### OFF-05 · Napster leftover · `itt01-napster`

**Room:** `years/2001/sites/napster/index.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-napster` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** Movable Type → `years/2001/sites/movabletype/index.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `amazon`

### OFF-06 · Movable Type · `itt01-mt`

**Room:** `years/2001/sites/movabletype/index.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-mt` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** Google → `years/2001/sites/google/index.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `amazon`

### OFF-07 · Google leftover · `itt01-google`

**Room:** `years/2001/sites/google/index.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-google` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** Yahoo → `years/2001/sites/yahoo/index.html`
- **Legal leftover hops from this room:** `yahoo` · `amazon` · `ebay`

### OFF-08 · Yahoo leftover · `itt01-yahoo`

**Room:** `years/2001/sites/yahoo/index.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-yahoo` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** Amazon → `years/2001/sites/amazon/index.html`
- **Legal leftover hops from this room:** `google` · `amazon` · `ebay`

### OFF-09 · Amazon smile leftover · `itt01-amz`

**Room:** `years/2001/sites/amazon/index.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-amz` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** Clickscape → `years/2001/sites/playable/game.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `ebay`

### OFF-10 · Clickscape · `itt01-game-clickscape`

**Room:** `years/2001/sites/playable/game.html`
**Incomplete never writes**
- Official trap / leftover trap on this room never writes the official `whenKey`.
- Empty official-verb / 0 official-req / wrong official-pick never writes.
- Leftover save on this room writes a **leftover** key (see leftover section), not this `whenKey`.

**Complete writes**
- Honesty + official verb / product hook on this dest writes `itt01-game-clickscape` with `real:true`, year `2001`.
- Does **not** write `itt01-wiki`.
- **Next:** Wikipedia → `years/2001/sites/wikipedia/edit.html`
- **Legal leftover hops from this room:** `yahoo` · `google` · `amazon`

## G2b · Leftover-trail n=11–20

These `whenKey`s are **leftover** stamps. Leftover-official save on that dest **must** write them (n>10 is not official-10).

- n=11 CNN leftover · `itt01-cnn` · `sites/cnn/index.html` · next eBay leftover
- n=12 eBay leftover · `itt01-ebay-d3` · `sites/ebay/index.html` · next Microsoft leftover
- n=13 Microsoft leftover · `itt01-microsoft` · `sites/microsoft/index.html` · next PayPal leftover
- n=14 PayPal leftover · `itt01-paypal-d3` · `sites/paypal/index.html` · next Slashdot leftover
- n=15 Slashdot leftover · `itt01-slashdot-d3` · `sites/slashdot/index.html` · next Blogger leftover
- n=16 Blogger leftover · `itt01-blogger` · `sites/blogger/index.html` · next Mozilla leftover
- n=17 Mozilla leftover · `itt01-moz` · `sites/mozilla/index.html` · next Ask Jeeves leftover
- n=18 Ask Jeeves leftover · `itt01-askjeeves-d3` · `sites/askjeeves/index.html` · next dmoz leftover
- n=19 dmoz leftover · `itt01-dmoz-d3` · `sites/dmoz/index.html` · next Encarta leftover
- n=20 Encarta leftover · `itt01-encarta` · `sites/encarta/index.html` · next ★ year gold leftover

---

## L · Leftover-official — every dest · 194 flows

Protocol is the same machine (`js/immersion/leftover-official.js`). The minutes below are dest-true: key, needPick, field, next, hops.

### L-0001 · `itt01-amz-lx` · sites/amazon/index.html

**Open:** `/years/2001/sites/amazon/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amz-lx"])`
**Need:** needPick=`book` · field ≥2 (placeholder “leftover book”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amz-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amz-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `book`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-amz-lx` and `itt02-amz-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="book"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover book”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amz-lx"]`.
- `localStorage['itt01-amz-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amz-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** CNN Nov 2001 leftover → `/years/2001/sites/cnn/index.html`

### L-0002 · `itt01-ipod-lx` · sites/apple/ipod.html

**Open:** `/years/2001/sites/apple/ipod.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ipod-lx"])`
**Need:** needPick=`399`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ipod-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ipod-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `399`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ipod-lx` and `itt02-ipod-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="399"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ipod-lx"]`.
- `localStorage['itt01-ipod-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ipod-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Napster endgame leftover → `/years/2001/sites/napster/index.html`

### L-0003 · `itt01-ipod-lx` · sites/apple/ipod/faq.html

**Open:** `/years/2001/sites/apple/ipod/faq.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ipod-lx"])`
**Need:** needPick=`wheel` · field ≥2 (placeholder “scroll leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ipod-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ipod-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `wheel`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-ipod-lx` and `itt02-ipod-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="wheel"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “scroll leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ipod-lx"]`.
- `localStorage['itt01-ipod-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ipod-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Napster endgame leftover → `/years/2001/sites/napster/index.html`

### L-0004 · `itt01-wa-url` · sites/archive/about.html

**Open:** `/years/2001/sites/archive/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wa-url"])`
**Need:** needPick=`crawl` · field ≥2 (placeholder “24 Oct leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wa-url"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wa-url']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `crawl`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-wa-url` and `itt02-wa-url` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="crawl"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “24 Oct leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wa-url"]`.
- `localStorage['itt01-wa-url']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wa-url`.
- Does **not** write `itt01-wiki`.
- **Next:** iTunes library leftover → `/years/2001/sites/itunes/index.html`

### L-0005 · `itt01-wayback-lx` · sites/archive/index.html

**Open:** `/years/2001/sites/archive/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wayback-lx"])`
**Need:** needPick=`url` · field ≥2 (placeholder “http://www.yahoo.com/”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wayback-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wayback-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `url`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-wayback-lx` and `itt02-wayback-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="url"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “http://www.yahoo.com/”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wayback-lx"]`.
- `localStorage['itt01-wayback-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wayback-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Wayback leftover about → `/years/2001/sites/archive/about.html`

### L-0006 · `itt01-blogdex` · sites/blogdex/index.html

**Open:** `/years/2001/sites/blogdex/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogdex"])`
**Need:** needPick=`hot` · field ≥2 (placeholder “hottest url leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogdex"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogdex']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `hot`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-blogdex` and `itt02-blogdex` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="hot"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “hottest url leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogdex"]`.
- `localStorage['itt01-blogdex']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogdex`.
- Does **not** write `itt01-wiki`.
- **Next:** Blogger Pyra leftover → `/years/2001/sites/blogger/index.html`

### L-0007 · `itt01-blogger` · sites/blogger/index.html

**Open:** `/years/2001/sites/blogger/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger"])`
**Need:** needPick=`post` · field ≥2 (placeholder “leftover post”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `post`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-blogger` and `itt02-blogger` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="post"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover post”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger"]`.
- `localStorage['itt01-blogger']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger`.
- Does **not** write `itt01-wiki`.
- **Next:** Warblog leftover → `/years/2001/sites/tpm/index.html`

### L-0008 · `itt01-cnn` · sites/cnn/index.html

**Open:** `/years/2001/sites/cnn/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="cnn"])`
**Need:** needPick=`2001`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="cnn"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-cnn']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `2001`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-cnn` and `itt02-cnn` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="2001"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="cnn"]`.
- `localStorage['itt01-cnn']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-cnn`.
- Does **not** write `itt01-wiki`.
- **Next:** Mozilla 0.9 leftover → `/years/2001/sites/mozilla/index.html`

### L-0009 · `itt01-encarta` · sites/encarta/index.html

**Open:** `/years/2001/sites/encarta/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="encarta"])`
**Need:** needPick=`art` · field ≥2 (placeholder “encarta leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="encarta"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-encarta']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `art`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-encarta` and `itt02-encarta` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="art"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “encarta leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="encarta"]`.
- `localStorage['itt01-encarta']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-encarta`.
- Does **not** write `itt01-wiki`.
- **Next:** Weblogs.com ping leftover → `/years/2001/sites/weblogs/index.html`

### L-0010 · `itt01-google-lx` · sites/google/index.html

**Open:** `/years/2001/sites/google/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-lx"])`
**Need:** needPick=`q` · field ≥2 (placeholder “leftover query”)
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `q`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-google-lx` and `itt02-google-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="q"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover query”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-lx"]`.
- `localStorage['itt01-google-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Yahoo 2001 leftover → `/years/2001/sites/yahoo/index.html`

### L-0011 · `itt01-itunes-lx` · sites/itunes/about.html

**Open:** `/years/2001/sites/itunes/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="itunes-lx"])`
**Need:** needPick=`lib` · field ≥2 (placeholder “rip mix leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="itunes-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-itunes-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `lib`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-itunes-lx` and `itt02-itunes-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="lib"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “rip mix leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="itunes-lx"]`.
- `localStorage['itt01-itunes-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-itunes-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** iTunes rip/mix leftover → `/years/2001/sites/itunes/about.html`

### L-0012 · `itt01-itunes-lx` · sites/itunes/index.html

**Open:** `/years/2001/sites/itunes/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="itunes-lx"])`
**Need:** needPick=`lib` · field ≥2 (placeholder “commute mix”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="itunes-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-itunes-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `lib`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-itunes-lx` and `itt02-itunes-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="lib"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “commute mix”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="itunes-lx"]`.
- `localStorage['itt01-itunes-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-itunes-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** iTunes rip/mix leftover → `/years/2001/sites/itunes/about.html`

### L-0013 · `itt01-mt-lx` · sites/movabletype/about.html

**Open:** `/years/2001/sites/movabletype/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="mt-lx"])`
**Need:** needPick=`perl` · field ≥2 (placeholder “perl leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="mt-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-mt-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `perl`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-mt-lx` and `itt02-mt-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="perl"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “perl leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="mt-lx"]`.
- `localStorage['itt01-mt-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-mt-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Movable Type leftover about → `/years/2001/sites/movabletype/about.html`

### L-0014 · `itt01-mt-lx` · sites/movabletype/index.html

**Open:** `/years/2001/sites/movabletype/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="mt-lx"])`
**Need:** needPick=`post` · field ≥2 (placeholder “leftover post”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="mt-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-mt-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `post`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-mt-lx` and `itt02-mt-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="post"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover post”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="mt-lx"]`.
- `localStorage['itt01-mt-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-mt-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Movable Type leftover about → `/years/2001/sites/movabletype/about.html`

### L-0015 · `itt01-moz` · sites/mozilla/index.html

**Open:** `/years/2001/sites/mozilla/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="moz"])`
**Need:** needPick=`moz` · field ≥2 (placeholder “mozilla leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="moz"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-moz']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `moz`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-moz` and `itt02-moz` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="moz"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “mozilla leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="moz"]`.
- `localStorage['itt01-moz']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-moz`.
- Does **not** write `itt01-wiki`.
- **Next:** Encarta leftover → `/years/2001/sites/encarta/index.html`

### L-0016 · `itt01-nap-q` · sites/napster/about.html

**Open:** `/years/2001/sites/napster/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="nap-q"])`
**Need:** needPick=`q` · field ≥2 (placeholder “leftover.mp3”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="nap-q"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-nap-q']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `q`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-nap-q` and `itt02-nap-q` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="q"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover.mp3”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="nap-q"]`.
- `localStorage['itt01-nap-q']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-nap-q`.
- Does **not** write `itt01-wiki`.
- **Next:** Movable Type leftover → `/years/2001/sites/movabletype/index.html`

### L-0017 · `itt01-napster-lx` · sites/napster/index.html

**Open:** `/years/2001/sites/napster/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="napster-lx"])`
**Need:** needPick=`q` · field ≥2 (placeholder “leftover.mp3”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="napster-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-napster-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `q`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-napster-lx` and `itt02-napster-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="q"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover.mp3”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="napster-lx"]`.
- `localStorage['itt01-napster-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-napster-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Napster leftover search → `/years/2001/sites/napster/about.html`

### L-0018 · `itt01-tpm` · sites/tpm/index.html

**Open:** `/years/2001/sites/tpm/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="tpm"])`
**Need:** needPick=`post` · field ≥2 (placeholder “warblog leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="tpm"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-tpm']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `post`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-tpm` and `itt02-tpm` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="post"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “warblog leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="tpm"]`.
- `localStorage['itt01-tpm']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-tpm`.
- Does **not** write `itt01-wiki`.
- **Next:** Google 2001 leftover → `/years/2001/sites/google/index.html`

### L-0019 · `itt01-ping` · sites/weblogs/index.html

**Open:** `/years/2001/sites/weblogs/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ping"])`
**Need:** needPick=`ping` · field ≥2 (placeholder “leftover ping”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ping"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ping']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `ping`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-ping` and `itt02-ping` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="ping"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover ping”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ping"]`.
- `localStorage['itt01-ping']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ping`.
- Does **not** write `itt01-wiki`.
- **Next:** Clickscape → `/years/2001/sites/playable/game.html`

### L-0020 · `itt01-wiki-hist` · sites/wikipedia/history.html

**Open:** `/years/2001/sites/wikipedia/history.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wiki-hist"])`
**Need:** needPick=`hist` · field ≥2 (placeholder “leftover edit”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wiki-hist"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wiki-hist']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `hist`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-wiki-hist` and `itt02-wiki-hist` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="hist"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “leftover edit”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wiki-hist"]`.
- `localStorage['itt01-wiki-hist']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wiki-hist`.
- Does **not** write `itt01-wiki`.
- **Next:** Wiki leftover languages → `/years/2001/sites/wikipedia/languages.html`

### L-0021 · `itt01-wiki-lang` · sites/wikipedia/languages.html

**Open:** `/years/2001/sites/wikipedia/languages.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wiki-lang"])`
**Need:** needPick=`lang` · field ≥2 (placeholder “Deutsch leftover”)
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wiki-lang"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wiki-lang']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `lang`) + Save → “Wrong leftover.” Key absent.
6. Ticks + pick (if any) + empty field + Save → “Empty never writes.”
7. `itt01-wiki` stays empty. `itt00-wiki-lang` and `itt02-wiki-lang` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="lang"]`. Wrong pick never writes.
- Type ≥2 chars in `[data-lo-field]` (e.g. “Deutsch leftover”). Empty never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wiki-lang"]`.
- `localStorage['itt01-wiki-lang']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wiki-lang`.
- Does **not** write `itt01-wiki`.
- **Next:** Wayback leftover URL → `/years/2001/sites/archive/index.html`

### L-0022 · `itt01-yahoo-lx` · sites/yahoo/index.html

**Open:** `/years/2001/sites/yahoo/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="yahoo-lx"])`
**Need:** needPick=`news`
**Nostalgia hop targets from here:** `google` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="yahoo-lx"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-yahoo-lx']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `news`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-yahoo-lx` and `itt02-yahoo-lx` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="news"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="yahoo-lx"]`.
- `localStorage['itt01-yahoo-lx']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-yahoo-lx`.
- Does **not** write `itt01-wiki`.
- **Next:** Amazon smile leftover → `/years/2001/sites/amazon/index.html`

### L-0023 · `itt01-amazon` · sites/amazon/about.html

**Open:** `/years/2001/sites/amazon/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amazon"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amazon"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amazon']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-amazon` and `itt02-amazon` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amazon"]`.
- `localStorage['itt01-amazon']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amazon`.
- Does **not** write `itt01-wiki`.
- **Next:** Cart leftover → `/years/2001/sites/amazon/cart.html`

### L-0024 · `itt01-amazon-d2` · sites/amazon/about.html

**Open:** `/years/2001/sites/amazon/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amazon-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amazon-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amazon-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-amazon-d2` and `itt02-amazon-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amazon-d2"]`.
- `localStorage['itt01-amazon-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amazon-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Amazon.com — smile leftover 2001 → `/years/2001/sites/amazon/index.html`

### L-0025 · `itt01-amazon-cart` · sites/amazon/cart.html

**Open:** `/years/2001/sites/amazon/cart.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amazon-cart"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amazon-cart"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amazon-cart']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-amazon-cart` and `itt02-amazon-cart` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amazon-cart"]`.
- `localStorage['itt01-amazon-cart']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amazon-cart`.
- Does **not** write `itt01-wiki`.
- **Next:** Search leftover → `/years/2001/sites/amazon/search.html`

### L-0026 · `itt01-amazon-cart-d2` · sites/amazon/cart.html

**Open:** `/years/2001/sites/amazon/cart.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amazon-cart-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amazon-cart-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amazon-cart-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-amazon-cart-d2` and `itt02-amazon-cart-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amazon-cart-d2"]`.
- `localStorage['itt01-amazon-cart-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amazon-cart-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Faq → `/years/2001/sites/apple/ipod/faq.html`

### L-0027 · `itt01-amz-d2` · sites/amazon/index.html

**Open:** `/years/2001/sites/amazon/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amz-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amz-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amz-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-amz-d2` and `itt02-amz-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amz-d2"]`.
- `localStorage['itt01-amz-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amz-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** How to → `/years/2001/sites/apple/ipod/howto.html`

### L-0028 · `itt01-amazon-search` · sites/amazon/search.html

**Open:** `/years/2001/sites/amazon/search.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amazon-search"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amazon-search"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amazon-search']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-amazon-search` and `itt02-amazon-search` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amazon-search"]`.
- `localStorage['itt01-amazon-search']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amazon-search`.
- Does **not** write `itt01-wiki`.
- **Next:** Specs → `/years/2001/sites/apple/ipod/specs.html`

### L-0029 · `itt01-amazon-search-d2` · sites/amazon/search.html

**Open:** `/years/2001/sites/amazon/search.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="amazon-search-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="amazon-search-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-amazon-search-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-amazon-search-d2` and `itt02-amazon-search-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="amazon-search-d2"]`.
- `localStorage['itt01-amazon-search-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-amazon-search-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Ipod → `/years/2001/sites/apple/ipod.html`

### L-0030 · `itt01-ipod-lx-d2` · sites/apple/ipod/faq.html

**Open:** `/years/2001/sites/apple/ipod/faq.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ipod-lx-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ipod-lx-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ipod-lx-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ipod-lx-d2` and `itt02-ipod-lx-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ipod-lx-d2"]`.
- `localStorage['itt01-ipod-lx-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ipod-lx-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** iMac leftover → `/years/2001/sites/appleimac/index.html`

### L-0031 · `itt01-apple-ipod-howto` · sites/apple/ipod/howto.html

**Open:** `/years/2001/sites/apple/ipod/howto.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="apple-ipod-howto"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="apple-ipod-howto"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-apple-ipod-howto']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-apple-ipod-howto` and `itt02-apple-ipod-howto` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="apple-ipod-howto"]`.
- `localStorage['itt01-apple-ipod-howto']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-apple-ipod-howto`.
- Does **not** write `itt01-wiki`.
- **Next:** About this leftover → `/years/2001/sites/archive/about.html`

### L-0032 · `itt01-apple-ipod-howto-d2` · sites/apple/ipod/howto.html

**Open:** `/years/2001/sites/apple/ipod/howto.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="apple-ipod-howto-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="apple-ipod-howto-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-apple-ipod-howto-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-apple-ipod-howto-d2` and `itt02-apple-ipod-howto-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="apple-ipod-howto-d2"]`.
- `localStorage['itt01-apple-ipod-howto-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-apple-ipod-howto-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Internet Archive Wayback Machine → `/years/2001/sites/archive/index.html`

### L-0033 · `itt01-apple-ipod-specs` · sites/apple/ipod/specs.html

**Open:** `/years/2001/sites/apple/ipod/specs.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="apple-ipod-specs"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="apple-ipod-specs"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-apple-ipod-specs']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-apple-ipod-specs` and `itt02-apple-ipod-specs` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="apple-ipod-specs"]`.
- `localStorage['itt01-apple-ipod-specs']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-apple-ipod-specs`.
- Does **not** write `itt01-wiki`.
- **Next:** What this leftover will not do → `/years/2001/sites/archive/more.html`

### L-0034 · `itt01-apple-ipod-specs-d2` · sites/apple/ipod/specs.html

**Open:** `/years/2001/sites/apple/ipod/specs.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="apple-ipod-specs-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="apple-ipod-specs-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-apple-ipod-specs-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-apple-ipod-specs-d2` and `itt02-apple-ipod-specs-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="apple-ipod-specs-d2"]`.
- `localStorage['itt01-apple-ipod-specs-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-apple-ipod-specs-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Jeeves leftover → `/years/2001/sites/askjeeves/about.html`

### L-0035 · `itt01-ipod-d2` · sites/apple/ipod.html

**Open:** `/years/2001/sites/apple/ipod.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ipod-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ipod-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ipod-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ipod-d2` and `itt02-ipod-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ipod-d2"]`.
- `localStorage['itt01-ipod-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ipod-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Ask Jeeves leftover → `/years/2001/sites/askjeeves/index.html`

### L-0036 · `itt01-appleimac` · sites/appleimac/index.html

**Open:** `/years/2001/sites/appleimac/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="appleimac"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="appleimac"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-appleimac']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-appleimac` and `itt02-appleimac` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="appleimac"]`.
- `localStorage['itt01-appleimac']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-appleimac`.
- Does **not** write `itt01-wiki`.
- **Next:** About Blogdex → `/years/2001/sites/blogdex/about.html`

### L-0037 · `itt01-appleimac-d2` · sites/appleimac/index.html

**Open:** `/years/2001/sites/appleimac/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="appleimac-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="appleimac-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-appleimac-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-appleimac-d2` and `itt02-appleimac-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="appleimac-d2"]`.
- `localStorage['itt01-appleimac-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-appleimac-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Blogdex → `/years/2001/sites/blogdex/index.html`

### L-0038 · `itt01-wa-url-d2` · sites/archive/about.html

**Open:** `/years/2001/sites/archive/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wa-url-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wa-url-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wa-url-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wa-url-d2` and `itt02-wa-url-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wa-url-d2"]`.
- `localStorage['itt01-wa-url-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wa-url-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Pyra → `/years/2001/sites/blogger/about.html`

### L-0039 · `itt01-wayback-d2` · sites/archive/index.html

**Open:** `/years/2001/sites/archive/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wayback-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wayback-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wayback-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wayback-d2` and `itt02-wayback-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wayback-d2"]`.
- `localStorage['itt01-wayback-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wayback-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** blogger! — post → `/years/2001/sites/blogger/edit.html`

### L-0040 · `itt01-archive` · sites/archive/more.html

**Open:** `/years/2001/sites/archive/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="archive"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="archive"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-archive']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-archive` and `itt02-archive` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="archive"]`.
- `localStorage['itt01-archive']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-archive`.
- Does **not** write `itt01-wiki`.
- **Next:** Blogger → `/years/2001/sites/blogger/index.html`

### L-0041 · `itt01-archive-d2` · sites/archive/more.html

**Open:** `/years/2001/sites/archive/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="archive-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="archive-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-archive-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-archive-d2` and `itt02-archive-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="archive-d2"]`.
- `localStorage['itt01-archive-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-archive-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** More → `/years/2001/sites/blogger/more.html`

### L-0042 · `itt01-askjeeves` · sites/askjeeves/about.html

**Open:** `/years/2001/sites/askjeeves/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="askjeeves"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="askjeeves"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-askjeeves']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-askjeeves` and `itt02-askjeeves` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="askjeeves"]`.
- `localStorage['itt01-askjeeves']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-askjeeves`.
- Does **not** write `itt01-wiki`.
- **Next:** My Weblog — blogger! → `/years/2001/sites/blogger/view.html`

### L-0043 · `itt01-askjeeves-d2` · sites/askjeeves/about.html

**Open:** `/years/2001/sites/askjeeves/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="askjeeves-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="askjeeves-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-askjeeves-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-askjeeves-d2` and `itt02-askjeeves-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="askjeeves-d2"]`.
- `localStorage['itt01-askjeeves-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-askjeeves-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About this leftover → `/years/2001/sites/cnn/about.html`

### L-0044 · `itt01-askjeeves-d3` · sites/askjeeves/index.html

**Open:** `/years/2001/sites/askjeeves/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="askjeeves-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="askjeeves-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-askjeeves-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-askjeeves-d3` and `itt02-askjeeves-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="askjeeves-d3"]`.
- `localStorage['itt01-askjeeves-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-askjeeves-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** CNN.com leftover → `/years/2001/sites/cnn/index.html`

### L-0045 · `itt01-askjeeves-d4` · sites/askjeeves/index.html

**Open:** `/years/2001/sites/askjeeves/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="askjeeves-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="askjeeves-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-askjeeves-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-askjeeves-d4` and `itt02-askjeeves-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="askjeeves-d4"]`.
- `localStorage['itt01-askjeeves-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-askjeeves-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** World leftover → `/years/2001/sites/cnn/world.html`

### L-0046 · `itt01-blogdex-d2` · sites/blogdex/about.html

**Open:** `/years/2001/sites/blogdex/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogdex-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogdex-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogdex-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogdex-d2` and `itt02-blogdex-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogdex-d2"]`.
- `localStorage['itt01-blogdex-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogdex-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About DMOZ leftover → `/years/2001/sites/dmoz/about.html`

### L-0047 · `itt01-blogdex-d3` · sites/blogdex/about.html

**Open:** `/years/2001/sites/blogdex/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogdex-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogdex-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogdex-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogdex-d3` and `itt02-blogdex-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogdex-d3"]`.
- `localStorage['itt01-blogdex-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogdex-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** DMOZ leftover → `/years/2001/sites/dmoz/index.html`

### L-0048 · `itt01-blogdex-d4` · sites/blogdex/index.html

**Open:** `/years/2001/sites/blogdex/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogdex-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogdex-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogdex-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogdex-d4` and `itt02-blogdex-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogdex-d4"]`.
- `localStorage['itt01-blogdex-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogdex-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** About eBay leftover → `/years/2001/sites/ebay/about.html`

### L-0049 · `itt01-blogger-d2` · sites/blogger/about.html

**Open:** `/years/2001/sites/blogger/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-d2` and `itt02-blogger-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-d2"]`.
- `localStorage['itt01-blogger-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** eBay leftover → `/years/2001/sites/ebay/index.html`

### L-0050 · `itt01-blogger-d3` · sites/blogger/about.html

**Open:** `/years/2001/sites/blogger/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-d3` and `itt02-blogger-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-d3"]`.
- `localStorage['itt01-blogger-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** About Encarta leftover → `/years/2001/sites/encarta/about.html`

### L-0051 · `itt01-blogger-edit` · sites/blogger/edit.html

**Open:** `/years/2001/sites/blogger/edit.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-edit"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-edit"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-edit']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-edit` and `itt02-blogger-edit` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-edit"]`.
- `localStorage['itt01-blogger-edit']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-edit`.
- Does **not** write `itt01-wiki`.
- **Next:** Encarta leftover → `/years/2001/sites/encarta/index.html`

### L-0052 · `itt01-blogger-edit-d2` · sites/blogger/edit.html

**Open:** `/years/2001/sites/blogger/edit.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-edit-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-edit-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-edit-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-edit-d2` and `itt02-blogger-edit-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-edit-d2"]`.
- `localStorage['itt01-blogger-edit-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-edit-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Excite leftover → `/years/2001/sites/excite/about.html`

### L-0053 · `itt01-blogger-d4` · sites/blogger/index.html

**Open:** `/years/2001/sites/blogger/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-d4` and `itt02-blogger-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-d4"]`.
- `localStorage['itt01-blogger-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Excite leftover → `/years/2001/sites/excite/index.html`

### L-0054 · `itt01-blogger-d5` · sites/blogger/more.html

**Open:** `/years/2001/sites/blogger/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-d5"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-d5"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-d5']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-d5` and `itt02-blogger-d5` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-d5"]`.
- `localStorage['itt01-blogger-d5']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-d5`.
- Does **not** write `itt01-wiki`.
- **Next:** About Google leftover → `/years/2001/sites/google/about.html`

### L-0055 · `itt01-blogger-d6` · sites/blogger/more.html

**Open:** `/years/2001/sites/blogger/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-d6"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-d6"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-d6']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-d6` and `itt02-blogger-d6` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-d6"]`.
- `localStorage['itt01-blogger-d6']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-d6`.
- Does **not** write `itt01-wiki`.
- **Next:** Google → `/years/2001/sites/google/index.html`

### L-0056 · `itt01-blogger-view` · sites/blogger/view.html

**Open:** `/years/2001/sites/blogger/view.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-view"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-view"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-view']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-view` and `itt02-blogger-view` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-view"]`.
- `localStorage['itt01-blogger-view']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-view`.
- Does **not** write `itt01-wiki`.
- **Next:** More → `/years/2001/sites/google/more.html`

### L-0057 · `itt01-blogger-view-d2` · sites/blogger/view.html

**Open:** `/years/2001/sites/blogger/view.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="blogger-view-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="blogger-view-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-blogger-view-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-blogger-view-d2` and `itt02-blogger-view-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="blogger-view-d2"]`.
- `localStorage['itt01-blogger-view-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-blogger-view-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Google Search: Results → `/years/2001/sites/google/search.html`

### L-0058 · `itt01-cnn-d2` · sites/cnn/about.html

**Open:** `/years/2001/sites/cnn/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="cnn-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="cnn-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-cnn-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-cnn-d2` and `itt02-cnn-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="cnn-d2"]`.
- `localStorage['itt01-cnn-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-cnn-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Grokster leftover → `/years/2001/sites/grok/index.html`

### L-0059 · `itt01-cnn-d3` · sites/cnn/about.html

**Open:** `/years/2001/sites/cnn/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="cnn-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="cnn-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-cnn-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-cnn-d3` and `itt02-cnn-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="cnn-d3"]`.
- `localStorage['itt01-cnn-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-cnn-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Rip and mix → `/years/2001/sites/itunes/about.html`

### L-0060 · `itt01-cnn-d4` · sites/cnn/index.html

**Open:** `/years/2001/sites/cnn/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="cnn-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="cnn-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-cnn-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-cnn-d4` and `itt02-cnn-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="cnn-d4"]`.
- `localStorage['itt01-cnn-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-cnn-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Burn → `/years/2001/sites/itunes/burn.html`

### L-0061 · `itt01-cnn-world` · sites/cnn/world.html

**Open:** `/years/2001/sites/cnn/world.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="cnn-world"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="cnn-world"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-cnn-world']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-cnn-world` and `itt02-cnn-world` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="cnn-world"]`.
- `localStorage['itt01-cnn-world']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-cnn-world`.
- Does **not** write `itt01-wiki`.
- **Next:** Itunes → `/years/2001/sites/itunes/index.html`

### L-0062 · `itt01-cnn-world-d2` · sites/cnn/world.html

**Open:** `/years/2001/sites/cnn/world.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="cnn-world-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="cnn-world-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-cnn-world-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-cnn-world-d2` and `itt02-cnn-world-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="cnn-world-d2"]`.
- `localStorage['itt01-cnn-world-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-cnn-world-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** IE6 leftover → `/years/2001/sites/microsoft/ie6.html`

### L-0063 · `itt01-dmoz` · sites/dmoz/about.html

**Open:** `/years/2001/sites/dmoz/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="dmoz"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="dmoz"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-dmoz']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-dmoz` and `itt02-dmoz` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="dmoz"]`.
- `localStorage['itt01-dmoz']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-dmoz`.
- Does **not** write `itt01-wiki`.
- **Next:** Internet Explorer 6 leftover → `/years/2001/sites/microsoft/index.html`

### L-0064 · `itt01-dmoz-d2` · sites/dmoz/about.html

**Open:** `/years/2001/sites/dmoz/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="dmoz-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="dmoz-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-dmoz-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-dmoz-d2` and `itt02-dmoz-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="dmoz-d2"]`.
- `localStorage['itt01-dmoz-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-dmoz-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Install leftover → `/years/2001/sites/movabletype/about.html`

### L-0065 · `itt01-dmoz-d3` · sites/dmoz/index.html

**Open:** `/years/2001/sites/dmoz/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="dmoz-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="dmoz-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-dmoz-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-dmoz-d3` and `itt02-dmoz-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="dmoz-d3"]`.
- `localStorage['itt01-dmoz-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-dmoz-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Movable Type → `/years/2001/sites/movabletype/index.html`

### L-0066 · `itt01-dmoz-d4` · sites/dmoz/index.html

**Open:** `/years/2001/sites/dmoz/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="dmoz-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="dmoz-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-dmoz-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-dmoz-d4` and `itt02-dmoz-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="dmoz-d4"]`.
- `localStorage['itt01-dmoz-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-dmoz-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Templates → `/years/2001/sites/movabletype/templates.html`

### L-0067 · `itt01-ebay` · sites/ebay/about.html

**Open:** `/years/2001/sites/ebay/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ebay"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ebay"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ebay']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ebay` and `itt02-ebay` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ebay"]`.
- `localStorage['itt01-ebay']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ebay`.
- Does **not** write `itt01-wiki`.
- **Next:** MoveOn leftover → `/years/2001/sites/moveon/index.html`

### L-0068 · `itt01-ebay-d2` · sites/ebay/about.html

**Open:** `/years/2001/sites/ebay/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ebay-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ebay-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ebay-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ebay-d2` and `itt02-ebay-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ebay-d2"]`.
- `localStorage['itt01-ebay-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ebay-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Mozilla leftover → `/years/2001/sites/mozilla/about.html`

### L-0069 · `itt01-ebay-d3` · sites/ebay/index.html

**Open:** `/years/2001/sites/ebay/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ebay-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ebay-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ebay-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ebay-d3` and `itt02-ebay-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ebay-d3"]`.
- `localStorage['itt01-ebay-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ebay-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Mozilla leftover → `/years/2001/sites/mozilla/index.html`

### L-0070 · `itt01-ebay-d4` · sites/ebay/index.html

**Open:** `/years/2001/sites/ebay/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ebay-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ebay-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ebay-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ebay-d4` and `itt02-ebay-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ebay-d4"]`.
- `localStorage['itt01-ebay-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ebay-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** About MSN leftover → `/years/2001/sites/msn/about.html`

### L-0071 · `itt01-encarta-d2` · sites/encarta/about.html

**Open:** `/years/2001/sites/encarta/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="encarta-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="encarta-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-encarta-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-encarta-d2` and `itt02-encarta-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="encarta-d2"]`.
- `localStorage['itt01-encarta-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-encarta-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** MSN leftover → `/years/2001/sites/msn/index.html`

### L-0072 · `itt01-encarta-d3` · sites/encarta/about.html

**Open:** `/years/2001/sites/encarta/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="encarta-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="encarta-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-encarta-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-encarta-d3` and `itt02-encarta-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="encarta-d3"]`.
- `localStorage['itt01-encarta-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-encarta-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** After July → `/years/2001/sites/napster/about.html`

### L-0073 · `itt01-encarta-d4` · sites/encarta/index.html

**Open:** `/years/2001/sites/encarta/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="encarta-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="encarta-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-encarta-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-encarta-d4` and `itt02-encarta-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="encarta-d4"]`.
- `localStorage['itt01-encarta-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-encarta-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Napster leftover — the war is over → `/years/2001/sites/napster/index.html`

### L-0074 · `itt01-excite` · sites/excite/about.html

**Open:** `/years/2001/sites/excite/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="excite"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="excite"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-excite']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-excite` and `itt02-excite` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="excite"]`.
- `localStorage['itt01-excite']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-excite`.
- Does **not** write `itt01-wiki`.
- **Next:** No files → `/years/2001/sites/napster/more.html`

### L-0075 · `itt01-excite-d2` · sites/excite/about.html

**Open:** `/years/2001/sites/excite/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="excite-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="excite-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-excite-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-excite-d2` and `itt02-excite-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="excite-d2"]`.
- `localStorage['itt01-excite-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-excite-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About PayPal leftover → `/years/2001/sites/paypal/about.html`

### L-0076 · `itt01-excite-d3` · sites/excite/index.html

**Open:** `/years/2001/sites/excite/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="excite-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="excite-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-excite-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-excite-d3` and `itt02-excite-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="excite-d3"]`.
- `localStorage['itt01-excite-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-excite-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** PayPal leftover → `/years/2001/sites/paypal/index.html`

### L-0077 · `itt01-excite-d4` · sites/excite/index.html

**Open:** `/years/2001/sites/excite/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="excite-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="excite-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-excite-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-excite-d4` and `itt02-excite-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="excite-d4"]`.
- `localStorage['itt01-excite-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-excite-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Extra cabinet → `/years/2001/sites/playable/extra-a.html`

### L-0078 · `itt01-google-d2` · sites/google/about.html

**Open:** `/years/2001/sites/google/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-google-d2` and `itt02-google-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-d2"]`.
- `localStorage['itt01-google-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Extra B → `/years/2001/sites/playable/extra-b.html`

### L-0079 · `itt01-google-d3` · sites/google/about.html

**Open:** `/years/2001/sites/google/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-google-d3` and `itt02-google-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-d3"]`.
- `localStorage['itt01-google-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Miniclip Pitch → `/years/2001/sites/playable/extra-c.html`

### L-0080 · `itt01-google-d4` · sites/google/index.html

**Open:** `/years/2001/sites/google/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-google-d4` and `itt02-google-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-d4"]`.
- `localStorage['itt01-google-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Applet Slice → `/years/2001/sites/playable/extra-d.html`

### L-0081 · `itt01-google-d5` · sites/google/more.html

**Open:** `/years/2001/sites/google/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-d5"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-d5"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-d5']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-google-d5` and `itt02-google-d5` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-d5"]`.
- `localStorage['itt01-google-d5']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-d5`.
- Does **not** write `itt01-wiki`.
- **Next:** Code Red Patch → `/years/2001/sites/playable/extra-e.html`

### L-0082 · `itt01-google-d6` · sites/google/more.html

**Open:** `/years/2001/sites/google/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-d6"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-d6"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-d6']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-google-d6` and `itt02-google-d6` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-d6"]`.
- `localStorage['itt01-google-d6']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-d6`.
- Does **not** write `itt01-wiki`.
- **Next:** iPod Spin → `/years/2001/sites/playable/extra-f.html`

### L-0083 · `itt01-google-search` · sites/google/search.html

**Open:** `/years/2001/sites/google/search.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-search"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-search"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-search']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-google-search` and `itt02-google-search` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-search"]`.
- `localStorage['itt01-google-search']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-search`.
- Does **not** write `itt01-wiki`.
- **Next:** Wiki Hold → `/years/2001/sites/playable/extra-g.html`

### L-0084 · `itt01-google-search-d2` · sites/google/search.html

**Open:** `/years/2001/sites/google/search.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="google-search-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="google-search-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-google-search-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-google-search-d2` and `itt02-google-search-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="google-search-d2"]`.
- `localStorage['itt01-google-search-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-google-search-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Edit Note → `/years/2001/sites/playable/extra-h.html`

### L-0085 · `itt01-grok` · sites/grok/index.html

**Open:** `/years/2001/sites/grok/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="grok"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="grok"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-grok']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-grok` and `itt02-grok` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="grok"]`.
- `localStorage['itt01-grok']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-grok`.
- Does **not** write `itt01-wiki`.
- **Next:** Code Hold → `/years/2001/sites/playable/extra-i.html`

### L-0086 · `itt01-grok-d2` · sites/grok/index.html

**Open:** `/years/2001/sites/grok/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="grok-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="grok-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-grok-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-grok-d2` and `itt02-grok-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="grok-d2"]`.
- `localStorage['itt01-grok-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-grok-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Famous leftover → `/years/2001/sites/playable/famous.html`

### L-0087 · `itt01-itunes-lx-d2` · sites/itunes/about.html

**Open:** `/years/2001/sites/itunes/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="itunes-lx-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="itunes-lx-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-itunes-lx-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-itunes-lx-d2` and `itt02-itunes-lx-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="itunes-lx-d2"]`.
- `localStorage['itt01-itunes-lx-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-itunes-lx-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** MapQuest print → `/years/2001/sites/playable/game-2.html`

### L-0088 · `itt01-itunes-burn` · sites/itunes/burn.html

**Open:** `/years/2001/sites/itunes/burn.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="itunes-burn"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="itunes-burn"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-itunes-burn']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-itunes-burn` and `itt02-itunes-burn` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="itunes-burn"]`.
- `localStorage['itt01-itunes-burn']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-itunes-burn`.
- Does **not** write `itt01-wiki`.
- **Next:** Pets sock → `/years/2001/sites/playable/game-3.html`

### L-0089 · `itt01-itunes-burn-d2` · sites/itunes/burn.html

**Open:** `/years/2001/sites/itunes/burn.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="itunes-burn-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="itunes-burn-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-itunes-burn-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-itunes-burn-d2` and `itt02-itunes-burn-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="itunes-burn-d2"]`.
- `localStorage['itt01-itunes-burn-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-itunes-burn-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Flash skip % → `/years/2001/sites/playable/game-4.html`

### L-0090 · `itt01-itunes-d2` · sites/itunes/index.html

**Open:** `/years/2001/sites/itunes/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="itunes-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="itunes-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-itunes-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-itunes-d2` and `itt02-itunes-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="itunes-d2"]`.
- `localStorage['itt01-itunes-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-itunes-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** eBay Dutch → `/years/2001/sites/playable/game-5.html`

### L-0091 · `itt01-microsoft-ie6` · sites/microsoft/ie6.html

**Open:** `/years/2001/sites/microsoft/ie6.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="microsoft-ie6"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="microsoft-ie6"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-microsoft-ie6']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-microsoft-ie6` and `itt02-microsoft-ie6` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="microsoft-ie6"]`.
- `localStorage['itt01-microsoft-ie6']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-microsoft-ie6`.
- Does **not** write `itt01-wiki`.
- **Next:** Clickscape → `/years/2001/sites/playable/game.html`

### L-0092 · `itt01-microsoft-ie6-d2` · sites/microsoft/ie6.html

**Open:** `/years/2001/sites/microsoft/ie6.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="microsoft-ie6-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="microsoft-ie6-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-microsoft-ie6-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-microsoft-ie6-d2` and `itt02-microsoft-ie6-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="microsoft-ie6-d2"]`.
- `localStorage['itt01-microsoft-ie6-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-microsoft-ie6-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** 2001 cabinet → `/years/2001/sites/playable/index.html`

### L-0093 · `itt01-microsoft` · sites/microsoft/index.html

**Open:** `/years/2001/sites/microsoft/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="microsoft"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="microsoft"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-microsoft']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-microsoft` and `itt02-microsoft` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="microsoft"]`.
- `localStorage['itt01-microsoft']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-microsoft`.
- Does **not** write `itt01-wiki`.
- **Next:** More A → `/years/2001/sites/playable/more-a.html`

### L-0094 · `itt01-microsoft-d2` · sites/microsoft/index.html

**Open:** `/years/2001/sites/microsoft/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="microsoft-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="microsoft-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-microsoft-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-microsoft-d2` and `itt02-microsoft-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="microsoft-d2"]`.
- `localStorage['itt01-microsoft-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-microsoft-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** More B → `/years/2001/sites/playable/more-b.html`

### L-0095 · `itt01-mt-lx-d2` · sites/movabletype/about.html

**Open:** `/years/2001/sites/movabletype/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="mt-lx-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="mt-lx-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-mt-lx-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-mt-lx-d2` and `itt02-mt-lx-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="mt-lx-d2"]`.
- `localStorage['itt01-mt-lx-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-mt-lx-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** D2 Rift → `/years/2001/sites/playable/more-c.html`

### L-0096 · `itt01-mt-d2` · sites/movabletype/index.html

**Open:** `/years/2001/sites/movabletype/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="mt-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="mt-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-mt-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-mt-d2` and `itt02-mt-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="mt-d2"]`.
- `localStorage['itt01-mt-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-mt-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** CS Dust → `/years/2001/sites/playable/more-d.html`

### L-0097 · `itt01-movabletype-templates` · sites/movabletype/templates.html

**Open:** `/years/2001/sites/movabletype/templates.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="movabletype-templates"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="movabletype-templates"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-movabletype-templates']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-movabletype-templates` and `itt02-movabletype-templates` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="movabletype-templates"]`.
- `localStorage['itt01-movabletype-templates']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-movabletype-templates`.
- Does **not** write `itt01-wiki`.
- **Next:** About Slashdot leftover → `/years/2001/sites/slashdot/about.html`

### L-0098 · `itt01-movabletype-templates-d2` · sites/movabletype/templates.html

**Open:** `/years/2001/sites/movabletype/templates.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="movabletype-templates-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="movabletype-templates-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-movabletype-templates-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-movabletype-templates-d2` and `itt02-movabletype-templates-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="movabletype-templates-d2"]`.
- `localStorage['itt01-movabletype-templates-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-movabletype-templates-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Slashdot leftover → `/years/2001/sites/slashdot/index.html`

### L-0099 · `itt01-moveon` · sites/moveon/index.html

**Open:** `/years/2001/sites/moveon/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="moveon"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="moveon"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-moveon']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-moveon` and `itt02-moveon` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="moveon"]`.
- `localStorage['itt01-moveon']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-moveon`.
- Does **not** write `itt01-wiki`.
- **Next:** About this leftover → `/years/2001/sites/tpm/about.html`

### L-0100 · `itt01-moveon-d2` · sites/moveon/index.html

**Open:** `/years/2001/sites/moveon/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="moveon-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="moveon-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-moveon-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-moveon-d2` and `itt02-moveon-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="moveon-d2"]`.
- `localStorage['itt01-moveon-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-moveon-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Talking Points leftover → `/years/2001/sites/tpm/index.html`

### L-0101 · `itt01-mozilla` · sites/mozilla/about.html

**Open:** `/years/2001/sites/mozilla/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="mozilla"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="mozilla"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-mozilla']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-mozilla` and `itt02-mozilla` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="mozilla"]`.
- `localStorage['itt01-mozilla']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-mozilla`.
- Does **not** write `itt01-wiki`.
- **Next:** About pings → `/years/2001/sites/weblogs/about.html`

### L-0102 · `itt01-mozilla-d2` · sites/mozilla/about.html

**Open:** `/years/2001/sites/mozilla/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="mozilla-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="mozilla-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-mozilla-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-mozilla-d2` and `itt02-mozilla-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="mozilla-d2"]`.
- `localStorage['itt01-mozilla-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-mozilla-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Weblogs.com leftover → `/years/2001/sites/weblogs/index.html`

### L-0103 · `itt01-moz-d2` · sites/mozilla/index.html

**Open:** `/years/2001/sites/mozilla/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="moz-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="moz-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-moz-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-moz-d2` and `itt02-moz-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="moz-d2"]`.
- `localStorage['itt01-moz-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-moz-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Nupedia → `/years/2001/sites/wikipedia/article-nupedia.html`

### L-0104 · `itt01-msn` · sites/msn/about.html

**Open:** `/years/2001/sites/msn/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="msn"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="msn"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-msn']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-msn` and `itt02-msn` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="msn"]`.
- `localStorage['itt01-msn']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-msn`.
- Does **not** write `itt01-wiki`.
- **Next:** Wiki → `/years/2001/sites/wikipedia/article-wiki.html`

### L-0105 · `itt01-msn-d2` · sites/msn/about.html

**Open:** `/years/2001/sites/msn/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="msn-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="msn-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-msn-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-msn-d2` and `itt02-msn-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="msn-d2"]`.
- `localStorage['itt01-msn-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-msn-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Community → `/years/2001/sites/wikipedia/community.html`

### L-0106 · `itt01-msn-d3` · sites/msn/index.html

**Open:** `/years/2001/sites/msn/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="msn-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="msn-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-msn-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-msn-d3` and `itt02-msn-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="msn-d3"]`.
- `localStorage['itt01-msn-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-msn-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Help → `/years/2001/sites/wikipedia/help.html`

### L-0107 · `itt01-msn-d4` · sites/msn/index.html

**Open:** `/years/2001/sites/msn/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="msn-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="msn-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-msn-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-msn-d4` and `itt02-msn-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="msn-d4"]`.
- `localStorage['itt01-msn-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-msn-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Recent changes → `/years/2001/sites/wikipedia/history.html`

### L-0108 · `itt01-nap-q-d2` · sites/napster/about.html

**Open:** `/years/2001/sites/napster/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="nap-q-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="nap-q-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-nap-q-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-nap-q-d2` and `itt02-nap-q-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="nap-q-d2"]`.
- `localStorage['itt01-nap-q-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-nap-q-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Wikipedia → `/years/2001/sites/wikipedia/index.html`

### L-0109 · `itt01-napster-d2` · sites/napster/index.html

**Open:** `/years/2001/sites/napster/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="napster-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="napster-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-napster-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-napster-d2` and `itt02-napster-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="napster-d2"]`.
- `localStorage['itt01-napster-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-napster-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Languages → `/years/2001/sites/wikipedia/languages.html`

### L-0110 · `itt01-napster-d3` · sites/napster/more.html

**Open:** `/years/2001/sites/napster/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="napster-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="napster-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-napster-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-napster-d3` and `itt02-napster-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="napster-d3"]`.
- `localStorage['itt01-napster-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-napster-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** About XP leftover → `/years/2001/sites/xp/about.html`

### L-0111 · `itt01-napster-d4` · sites/napster/more.html

**Open:** `/years/2001/sites/napster/more.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="napster-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="napster-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-napster-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-napster-d4` and `itt02-napster-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="napster-d4"]`.
- `localStorage['itt01-napster-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-napster-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Windows XP leftover → `/years/2001/sites/xp/index.html`

### L-0112 · `itt01-paypal` · sites/paypal/about.html

**Open:** `/years/2001/sites/paypal/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="paypal"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="paypal"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-paypal']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-paypal` and `itt02-paypal` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="paypal"]`.
- `localStorage['itt01-paypal']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-paypal`.
- Does **not** write `itt01-wiki`.
- **Next:** About Yahoo leftover → `/years/2001/sites/yahoo/about.html`

### L-0113 · `itt01-paypal-d2` · sites/paypal/about.html

**Open:** `/years/2001/sites/paypal/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="paypal-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="paypal-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-paypal-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-paypal-d2` and `itt02-paypal-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="paypal-d2"]`.
- `localStorage['itt01-paypal-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-paypal-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Yahoo! → `/years/2001/sites/yahoo/index.html`

### L-0114 · `itt01-paypal-d3` · sites/paypal/index.html

**Open:** `/years/2001/sites/paypal/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="paypal-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="paypal-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-paypal-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-paypal-d3` and `itt02-paypal-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="paypal-d3"]`.
- `localStorage['itt01-paypal-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-paypal-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Yahoo News leftover → `/years/2001/sites/yahoo/news.html`

### L-0115 · `itt01-paypal-d4` · sites/paypal/index.html

**Open:** `/years/2001/sites/paypal/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="paypal-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="paypal-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-paypal-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-paypal-d4` and `itt02-paypal-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="paypal-d4"]`.
- `localStorage['itt01-paypal-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-paypal-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** About this leftover → `/years/2001/sites/amazon/about.html`

### L-0116 · `itt01-playable-extra-a` · sites/playable/extra-a.html

**Open:** `/years/2001/sites/playable/extra-a.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-a"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-a"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-a']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-a` and `itt02-playable-extra-a` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-a"]`.
- `localStorage['itt01-playable-extra-a']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-a`.
- Does **not** write `itt01-wiki`.
- **Next:** Cart leftover → `/years/2001/sites/amazon/cart.html`

### L-0117 · `itt01-playable-extra-a-d2` · sites/playable/extra-a.html

**Open:** `/years/2001/sites/playable/extra-a.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-a-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-a-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-a-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-a-d2` and `itt02-playable-extra-a-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-a-d2"]`.
- `localStorage['itt01-playable-extra-a-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-a-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Amazon.com — smile leftover 2001 → `/years/2001/sites/amazon/index.html`

### L-0118 · `itt01-playable-extra-b` · sites/playable/extra-b.html

**Open:** `/years/2001/sites/playable/extra-b.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-b"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-b"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-b']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-b` and `itt02-playable-extra-b` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-b"]`.
- `localStorage['itt01-playable-extra-b']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-b`.
- Does **not** write `itt01-wiki`.
- **Next:** Search leftover → `/years/2001/sites/amazon/search.html`

### L-0119 · `itt01-playable-extra-b-d2` · sites/playable/extra-b.html

**Open:** `/years/2001/sites/playable/extra-b.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-b-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-b-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-b-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-b-d2` and `itt02-playable-extra-b-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-b-d2"]`.
- `localStorage['itt01-playable-extra-b-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-b-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Faq → `/years/2001/sites/apple/ipod/faq.html`

### L-0120 · `itt01-playable-extra-c` · sites/playable/extra-c.html

**Open:** `/years/2001/sites/playable/extra-c.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-c"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-c"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-c']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-c` and `itt02-playable-extra-c` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-c"]`.
- `localStorage['itt01-playable-extra-c']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-c`.
- Does **not** write `itt01-wiki`.
- **Next:** How to → `/years/2001/sites/apple/ipod/howto.html`

### L-0121 · `itt01-playable-extra-c-d2` · sites/playable/extra-c.html

**Open:** `/years/2001/sites/playable/extra-c.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-c-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-c-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-c-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-c-d2` and `itt02-playable-extra-c-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-c-d2"]`.
- `localStorage['itt01-playable-extra-c-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-c-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Specs → `/years/2001/sites/apple/ipod/specs.html`

### L-0122 · `itt01-playable-extra-d` · sites/playable/extra-d.html

**Open:** `/years/2001/sites/playable/extra-d.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-d"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-d"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-d']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-d` and `itt02-playable-extra-d` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-d"]`.
- `localStorage['itt01-playable-extra-d']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-d`.
- Does **not** write `itt01-wiki`.
- **Next:** Ipod → `/years/2001/sites/apple/ipod.html`

### L-0123 · `itt01-playable-extra-d-d2` · sites/playable/extra-d.html

**Open:** `/years/2001/sites/playable/extra-d.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-d-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-d-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-d-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-d-d2` and `itt02-playable-extra-d-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-d-d2"]`.
- `localStorage['itt01-playable-extra-d-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-d-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** iMac leftover → `/years/2001/sites/appleimac/index.html`

### L-0124 · `itt01-playable-extra-e` · sites/playable/extra-e.html

**Open:** `/years/2001/sites/playable/extra-e.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-e"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-e"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-e']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-e` and `itt02-playable-extra-e` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-e"]`.
- `localStorage['itt01-playable-extra-e']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-e`.
- Does **not** write `itt01-wiki`.
- **Next:** About this leftover → `/years/2001/sites/archive/about.html`

### L-0125 · `itt01-playable-extra-e-d2` · sites/playable/extra-e.html

**Open:** `/years/2001/sites/playable/extra-e.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-e-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-e-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-e-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-e-d2` and `itt02-playable-extra-e-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-e-d2"]`.
- `localStorage['itt01-playable-extra-e-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-e-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Internet Archive Wayback Machine → `/years/2001/sites/archive/index.html`

### L-0126 · `itt01-playable-extra-f` · sites/playable/extra-f.html

**Open:** `/years/2001/sites/playable/extra-f.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-f"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-f"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-f']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-f` and `itt02-playable-extra-f` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-f"]`.
- `localStorage['itt01-playable-extra-f']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-f`.
- Does **not** write `itt01-wiki`.
- **Next:** What this leftover will not do → `/years/2001/sites/archive/more.html`

### L-0127 · `itt01-playable-extra-f-d2` · sites/playable/extra-f.html

**Open:** `/years/2001/sites/playable/extra-f.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-f-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-f-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-f-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-f-d2` and `itt02-playable-extra-f-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-f-d2"]`.
- `localStorage['itt01-playable-extra-f-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-f-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Jeeves leftover → `/years/2001/sites/askjeeves/about.html`

### L-0128 · `itt01-playable-extra-g` · sites/playable/extra-g.html

**Open:** `/years/2001/sites/playable/extra-g.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-g"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-g"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-g']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-g` and `itt02-playable-extra-g` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-g"]`.
- `localStorage['itt01-playable-extra-g']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-g`.
- Does **not** write `itt01-wiki`.
- **Next:** Ask Jeeves leftover → `/years/2001/sites/askjeeves/index.html`

### L-0129 · `itt01-playable-extra-g-d2` · sites/playable/extra-g.html

**Open:** `/years/2001/sites/playable/extra-g.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-g-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-g-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-g-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-g-d2` and `itt02-playable-extra-g-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-g-d2"]`.
- `localStorage['itt01-playable-extra-g-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-g-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Blogdex → `/years/2001/sites/blogdex/about.html`

### L-0130 · `itt01-playable-extra-h` · sites/playable/extra-h.html

**Open:** `/years/2001/sites/playable/extra-h.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-h"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-h"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-h']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-h` and `itt02-playable-extra-h` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-h"]`.
- `localStorage['itt01-playable-extra-h']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-h`.
- Does **not** write `itt01-wiki`.
- **Next:** Blogdex → `/years/2001/sites/blogdex/index.html`

### L-0131 · `itt01-playable-extra-h-d2` · sites/playable/extra-h.html

**Open:** `/years/2001/sites/playable/extra-h.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-h-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-h-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-h-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-h-d2` and `itt02-playable-extra-h-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-h-d2"]`.
- `localStorage['itt01-playable-extra-h-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-h-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Pyra → `/years/2001/sites/blogger/about.html`

### L-0132 · `itt01-playable-extra-i` · sites/playable/extra-i.html

**Open:** `/years/2001/sites/playable/extra-i.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-i"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-i"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-i']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-i` and `itt02-playable-extra-i` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-i"]`.
- `localStorage['itt01-playable-extra-i']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-i`.
- Does **not** write `itt01-wiki`.
- **Next:** blogger! — post → `/years/2001/sites/blogger/edit.html`

### L-0133 · `itt01-playable-extra-i-d2` · sites/playable/extra-i.html

**Open:** `/years/2001/sites/playable/extra-i.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-extra-i-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-extra-i-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-extra-i-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-extra-i-d2` and `itt02-playable-extra-i-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-extra-i-d2"]`.
- `localStorage['itt01-playable-extra-i-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-extra-i-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Blogger → `/years/2001/sites/blogger/index.html`

### L-0134 · `itt01-playable-famous` · sites/playable/famous.html

**Open:** `/years/2001/sites/playable/famous.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-famous"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-famous"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-famous']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-famous` and `itt02-playable-famous` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-famous"]`.
- `localStorage['itt01-playable-famous']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-famous`.
- Does **not** write `itt01-wiki`.
- **Next:** More → `/years/2001/sites/blogger/more.html`

### L-0135 · `itt01-playable-famous-d2` · sites/playable/famous.html

**Open:** `/years/2001/sites/playable/famous.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-famous-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-famous-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-famous-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-famous-d2` and `itt02-playable-famous-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-famous-d2"]`.
- `localStorage['itt01-playable-famous-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-famous-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** My Weblog — blogger! → `/years/2001/sites/blogger/view.html`

### L-0136 · `itt01-playable-game-2` · sites/playable/game-2.html

**Open:** `/years/2001/sites/playable/game-2.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-2` and `itt02-playable-game-2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-2"]`.
- `localStorage['itt01-playable-game-2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-2`.
- Does **not** write `itt01-wiki`.
- **Next:** About this leftover → `/years/2001/sites/cnn/about.html`

### L-0137 · `itt01-playable-game-2-d2` · sites/playable/game-2.html

**Open:** `/years/2001/sites/playable/game-2.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-2-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-2-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-2-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-2-d2` and `itt02-playable-game-2-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-2-d2"]`.
- `localStorage['itt01-playable-game-2-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-2-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** CNN.com leftover → `/years/2001/sites/cnn/index.html`

### L-0138 · `itt01-playable-game-3` · sites/playable/game-3.html

**Open:** `/years/2001/sites/playable/game-3.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-3` and `itt02-playable-game-3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-3"]`.
- `localStorage['itt01-playable-game-3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-3`.
- Does **not** write `itt01-wiki`.
- **Next:** World leftover → `/years/2001/sites/cnn/world.html`

### L-0139 · `itt01-playable-game-3-d2` · sites/playable/game-3.html

**Open:** `/years/2001/sites/playable/game-3.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-3-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-3-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-3-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-3-d2` and `itt02-playable-game-3-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-3-d2"]`.
- `localStorage['itt01-playable-game-3-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-3-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About DMOZ leftover → `/years/2001/sites/dmoz/about.html`

### L-0140 · `itt01-playable-game-4` · sites/playable/game-4.html

**Open:** `/years/2001/sites/playable/game-4.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-4` and `itt02-playable-game-4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-4"]`.
- `localStorage['itt01-playable-game-4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-4`.
- Does **not** write `itt01-wiki`.
- **Next:** DMOZ leftover → `/years/2001/sites/dmoz/index.html`

### L-0141 · `itt01-playable-game-4-d2` · sites/playable/game-4.html

**Open:** `/years/2001/sites/playable/game-4.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-4-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-4-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-4-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-4-d2` and `itt02-playable-game-4-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-4-d2"]`.
- `localStorage['itt01-playable-game-4-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-4-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About eBay leftover → `/years/2001/sites/ebay/about.html`

### L-0142 · `itt01-playable-game-5` · sites/playable/game-5.html

**Open:** `/years/2001/sites/playable/game-5.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-5"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-5"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-5']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-5` and `itt02-playable-game-5` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-5"]`.
- `localStorage['itt01-playable-game-5']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-5`.
- Does **not** write `itt01-wiki`.
- **Next:** eBay leftover → `/years/2001/sites/ebay/index.html`

### L-0143 · `itt01-playable-game-5-d2` · sites/playable/game-5.html

**Open:** `/years/2001/sites/playable/game-5.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-5-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-5-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-5-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-5-d2` and `itt02-playable-game-5-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-5-d2"]`.
- `localStorage['itt01-playable-game-5-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-5-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Encarta leftover → `/years/2001/sites/encarta/about.html`

### L-0144 · `itt01-playable-game` · sites/playable/game.html

**Open:** `/years/2001/sites/playable/game.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game` and `itt02-playable-game` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game"]`.
- `localStorage['itt01-playable-game']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game`.
- Does **not** write `itt01-wiki`.
- **Next:** Encarta leftover → `/years/2001/sites/encarta/index.html`

### L-0145 · `itt01-playable-game-d2` · sites/playable/game.html

**Open:** `/years/2001/sites/playable/game.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-game-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-game-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-game-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-game-d2` and `itt02-playable-game-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-game-d2"]`.
- `localStorage['itt01-playable-game-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-game-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Excite leftover → `/years/2001/sites/excite/about.html`

### L-0146 · `itt01-playable` · sites/playable/index.html

**Open:** `/years/2001/sites/playable/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable` and `itt02-playable` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable"]`.
- `localStorage['itt01-playable']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable`.
- Does **not** write `itt01-wiki`.
- **Next:** Excite leftover → `/years/2001/sites/excite/index.html`

### L-0147 · `itt01-playable-d2` · sites/playable/index.html

**Open:** `/years/2001/sites/playable/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-d2` and `itt02-playable-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-d2"]`.
- `localStorage['itt01-playable-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** About Google leftover → `/years/2001/sites/google/about.html`

### L-0148 · `itt01-playable-more-a` · sites/playable/more-a.html

**Open:** `/years/2001/sites/playable/more-a.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-a"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-a"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-a']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-a` and `itt02-playable-more-a` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-a"]`.
- `localStorage['itt01-playable-more-a']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-a`.
- Does **not** write `itt01-wiki`.
- **Next:** Google → `/years/2001/sites/google/index.html`

### L-0149 · `itt01-playable-more-a-d2` · sites/playable/more-a.html

**Open:** `/years/2001/sites/playable/more-a.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-a-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-a-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-a-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-a-d2` and `itt02-playable-more-a-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-a-d2"]`.
- `localStorage['itt01-playable-more-a-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-a-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** More → `/years/2001/sites/google/more.html`

### L-0150 · `itt01-playable-more-b` · sites/playable/more-b.html

**Open:** `/years/2001/sites/playable/more-b.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-b"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-b"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-b']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-b` and `itt02-playable-more-b` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-b"]`.
- `localStorage['itt01-playable-more-b']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-b`.
- Does **not** write `itt01-wiki`.
- **Next:** Google Search: Results → `/years/2001/sites/google/search.html`

### L-0151 · `itt01-playable-more-b-d2` · sites/playable/more-b.html

**Open:** `/years/2001/sites/playable/more-b.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-b-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-b-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-b-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-b-d2` and `itt02-playable-more-b-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-b-d2"]`.
- `localStorage['itt01-playable-more-b-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-b-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Grokster leftover → `/years/2001/sites/grok/index.html`

### L-0152 · `itt01-playable-more-c` · sites/playable/more-c.html

**Open:** `/years/2001/sites/playable/more-c.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-c"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-c"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-c']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-c` and `itt02-playable-more-c` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-c"]`.
- `localStorage['itt01-playable-more-c']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-c`.
- Does **not** write `itt01-wiki`.
- **Next:** Rip and mix → `/years/2001/sites/itunes/about.html`

### L-0153 · `itt01-playable-more-c-d2` · sites/playable/more-c.html

**Open:** `/years/2001/sites/playable/more-c.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-c-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-c-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-c-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-c-d2` and `itt02-playable-more-c-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-c-d2"]`.
- `localStorage['itt01-playable-more-c-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-c-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Burn → `/years/2001/sites/itunes/burn.html`

### L-0154 · `itt01-playable-more-d` · sites/playable/more-d.html

**Open:** `/years/2001/sites/playable/more-d.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-d"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-d"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-d']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-d` and `itt02-playable-more-d` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-d"]`.
- `localStorage['itt01-playable-more-d']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-d`.
- Does **not** write `itt01-wiki`.
- **Next:** Itunes → `/years/2001/sites/itunes/index.html`

### L-0155 · `itt01-playable-more-d-d2` · sites/playable/more-d.html

**Open:** `/years/2001/sites/playable/more-d.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="playable-more-d-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="playable-more-d-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-playable-more-d-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-playable-more-d-d2` and `itt02-playable-more-d-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="playable-more-d-d2"]`.
- `localStorage['itt01-playable-more-d-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-playable-more-d-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** IE6 leftover → `/years/2001/sites/microsoft/ie6.html`

### L-0156 · `itt01-slashdot` · sites/slashdot/about.html

**Open:** `/years/2001/sites/slashdot/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="slashdot"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="slashdot"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-slashdot']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-slashdot` and `itt02-slashdot` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="slashdot"]`.
- `localStorage['itt01-slashdot']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-slashdot`.
- Does **not** write `itt01-wiki`.
- **Next:** Internet Explorer 6 leftover → `/years/2001/sites/microsoft/index.html`

### L-0157 · `itt01-slashdot-d2` · sites/slashdot/about.html

**Open:** `/years/2001/sites/slashdot/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="slashdot-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="slashdot-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-slashdot-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-slashdot-d2` and `itt02-slashdot-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="slashdot-d2"]`.
- `localStorage['itt01-slashdot-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-slashdot-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Install leftover → `/years/2001/sites/movabletype/about.html`

### L-0158 · `itt01-slashdot-d3` · sites/slashdot/index.html

**Open:** `/years/2001/sites/slashdot/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="slashdot-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="slashdot-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-slashdot-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-slashdot-d3` and `itt02-slashdot-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="slashdot-d3"]`.
- `localStorage['itt01-slashdot-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-slashdot-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Movable Type → `/years/2001/sites/movabletype/index.html`

### L-0159 · `itt01-slashdot-d4` · sites/slashdot/index.html

**Open:** `/years/2001/sites/slashdot/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="slashdot-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="slashdot-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-slashdot-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-slashdot-d4` and `itt02-slashdot-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="slashdot-d4"]`.
- `localStorage['itt01-slashdot-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-slashdot-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Templates → `/years/2001/sites/movabletype/templates.html`

### L-0160 · `itt01-tpm-d2` · sites/tpm/about.html

**Open:** `/years/2001/sites/tpm/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="tpm-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="tpm-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-tpm-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-tpm-d2` and `itt02-tpm-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="tpm-d2"]`.
- `localStorage['itt01-tpm-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-tpm-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** MoveOn leftover → `/years/2001/sites/moveon/index.html`

### L-0161 · `itt01-tpm-d3` · sites/tpm/about.html

**Open:** `/years/2001/sites/tpm/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="tpm-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="tpm-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-tpm-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-tpm-d3` and `itt02-tpm-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="tpm-d3"]`.
- `localStorage['itt01-tpm-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-tpm-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** About Mozilla leftover → `/years/2001/sites/mozilla/about.html`

### L-0162 · `itt01-tpm-d4` · sites/tpm/index.html

**Open:** `/years/2001/sites/tpm/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="tpm-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="tpm-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-tpm-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-tpm-d4` and `itt02-tpm-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="tpm-d4"]`.
- `localStorage['itt01-tpm-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-tpm-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Mozilla leftover → `/years/2001/sites/mozilla/index.html`

### L-0163 · `itt01-weblogs` · sites/weblogs/about.html

**Open:** `/years/2001/sites/weblogs/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="weblogs"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="weblogs"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-weblogs']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-weblogs` and `itt02-weblogs` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="weblogs"]`.
- `localStorage['itt01-weblogs']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-weblogs`.
- Does **not** write `itt01-wiki`.
- **Next:** About MSN leftover → `/years/2001/sites/msn/about.html`

### L-0164 · `itt01-weblogs-d2` · sites/weblogs/about.html

**Open:** `/years/2001/sites/weblogs/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="weblogs-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="weblogs-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-weblogs-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-weblogs-d2` and `itt02-weblogs-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="weblogs-d2"]`.
- `localStorage['itt01-weblogs-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-weblogs-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** MSN leftover → `/years/2001/sites/msn/index.html`

### L-0165 · `itt01-ping-d2` · sites/weblogs/index.html

**Open:** `/years/2001/sites/weblogs/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="ping-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="ping-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-ping-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-ping-d2` and `itt02-ping-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="ping-d2"]`.
- `localStorage['itt01-ping-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-ping-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** After July → `/years/2001/sites/napster/about.html`

### L-0166 · `itt01-wikipedia-article-nupedi` · sites/wikipedia/article-nupedia.html

**Open:** `/years/2001/sites/wikipedia/article-nupedia.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-article-nupedi"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-article-nupedi"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-article-nupedi']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-article-nupedi` and `itt02-wikipedia-article-nupedi` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-article-nupedi"]`.
- `localStorage['itt01-wikipedia-article-nupedi']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-article-nupedi`.
- Does **not** write `itt01-wiki`.
- **Next:** Napster leftover — the war is over → `/years/2001/sites/napster/index.html`

### L-0167 · `itt01-wikipedia-article-nupedi-d2` · sites/wikipedia/article-nupedia.html

**Open:** `/years/2001/sites/wikipedia/article-nupedia.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-article-nupedi-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-article-nupedi-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-article-nupedi-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-article-nupedi-d2` and `itt02-wikipedia-article-nupedi-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-article-nupedi-d2"]`.
- `localStorage['itt01-wikipedia-article-nupedi-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-article-nupedi-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** No files → `/years/2001/sites/napster/more.html`

### L-0168 · `itt01-wikipedia-article-wiki` · sites/wikipedia/article-wiki.html

**Open:** `/years/2001/sites/wikipedia/article-wiki.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-article-wiki"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-article-wiki"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-article-wiki']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-article-wiki` and `itt02-wikipedia-article-wiki` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-article-wiki"]`.
- `localStorage['itt01-wikipedia-article-wiki']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-article-wiki`.
- Does **not** write `itt01-wiki`.
- **Next:** About PayPal leftover → `/years/2001/sites/paypal/about.html`

### L-0169 · `itt01-wikipedia-article-wiki-d2` · sites/wikipedia/article-wiki.html

**Open:** `/years/2001/sites/wikipedia/article-wiki.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-article-wiki-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-article-wiki-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-article-wiki-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-article-wiki-d2` and `itt02-wikipedia-article-wiki-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-article-wiki-d2"]`.
- `localStorage['itt01-wikipedia-article-wiki-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-article-wiki-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** PayPal leftover → `/years/2001/sites/paypal/index.html`

### L-0170 · `itt01-wikipedia-community` · sites/wikipedia/community.html

**Open:** `/years/2001/sites/wikipedia/community.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-community"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-community"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-community']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-community` and `itt02-wikipedia-community` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-community"]`.
- `localStorage['itt01-wikipedia-community']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-community`.
- Does **not** write `itt01-wiki`.
- **Next:** Extra cabinet → `/years/2001/sites/playable/extra-a.html`

### L-0171 · `itt01-wikipedia-community-d2` · sites/wikipedia/community.html

**Open:** `/years/2001/sites/wikipedia/community.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-community-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-community-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-community-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-community-d2` and `itt02-wikipedia-community-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-community-d2"]`.
- `localStorage['itt01-wikipedia-community-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-community-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Extra B → `/years/2001/sites/playable/extra-b.html`

### L-0172 · `itt01-wikipedia-edit` · sites/wikipedia/edit.html

**Open:** `/years/2001/sites/wikipedia/edit.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-edit"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-edit"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-edit']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-edit` and `itt02-wikipedia-edit` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-edit"]`.
- `localStorage['itt01-wikipedia-edit']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-edit`.
- Does **not** write `itt01-wiki`.
- **Next:** Miniclip Pitch → `/years/2001/sites/playable/extra-c.html`

### L-0173 · `itt01-wikipedia-edit-d2` · sites/wikipedia/edit.html

**Open:** `/years/2001/sites/wikipedia/edit.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-edit-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-edit-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-edit-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-edit-d2` and `itt02-wikipedia-edit-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-edit-d2"]`.
- `localStorage['itt01-wikipedia-edit-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-edit-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Applet Slice → `/years/2001/sites/playable/extra-d.html`

### L-0174 · `itt01-wikipedia-help` · sites/wikipedia/help.html

**Open:** `/years/2001/sites/wikipedia/help.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-help"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-help"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-help']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-help` and `itt02-wikipedia-help` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-help"]`.
- `localStorage['itt01-wikipedia-help']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-help`.
- Does **not** write `itt01-wiki`.
- **Next:** Code Red Patch → `/years/2001/sites/playable/extra-e.html`

### L-0175 · `itt01-wikipedia-help-d2` · sites/wikipedia/help.html

**Open:** `/years/2001/sites/wikipedia/help.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-help-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-help-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-help-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-help-d2` and `itt02-wikipedia-help-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-help-d2"]`.
- `localStorage['itt01-wikipedia-help-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-help-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** iPod Spin → `/years/2001/sites/playable/extra-f.html`

### L-0176 · `itt01-wiki-hist-d2` · sites/wikipedia/history.html

**Open:** `/years/2001/sites/wikipedia/history.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wiki-hist-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wiki-hist-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wiki-hist-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wiki-hist-d2` and `itt02-wiki-hist-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wiki-hist-d2"]`.
- `localStorage['itt01-wiki-hist-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wiki-hist-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Wiki Hold → `/years/2001/sites/playable/extra-g.html`

### L-0177 · `itt01-wikipedia` · sites/wikipedia/index.html

**Open:** `/years/2001/sites/wikipedia/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia` and `itt02-wikipedia` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia"]`.
- `localStorage['itt01-wikipedia']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia`.
- Does **not** write `itt01-wiki`.
- **Next:** Edit Note → `/years/2001/sites/playable/extra-h.html`

### L-0178 · `itt01-wikipedia-d2` · sites/wikipedia/index.html

**Open:** `/years/2001/sites/wikipedia/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wikipedia-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wikipedia-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wikipedia-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wikipedia-d2` and `itt02-wikipedia-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wikipedia-d2"]`.
- `localStorage['itt01-wikipedia-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wikipedia-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Code Hold → `/years/2001/sites/playable/extra-i.html`

### L-0179 · `itt01-wiki-lang-d2` · sites/wikipedia/languages.html

**Open:** `/years/2001/sites/wikipedia/languages.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="wiki-lang-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="wiki-lang-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-wiki-lang-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-wiki-lang-d2` and `itt02-wiki-lang-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="wiki-lang-d2"]`.
- `localStorage['itt01-wiki-lang-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-wiki-lang-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Famous leftover → `/years/2001/sites/playable/famous.html`

### L-0180 · `itt01-xp` · sites/xp/about.html

**Open:** `/years/2001/sites/xp/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="xp"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="xp"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-xp']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-xp` and `itt02-xp` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="xp"]`.
- `localStorage['itt01-xp']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-xp`.
- Does **not** write `itt01-wiki`.
- **Next:** MapQuest print → `/years/2001/sites/playable/game-2.html`

### L-0181 · `itt01-xp-d2` · sites/xp/about.html

**Open:** `/years/2001/sites/xp/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="xp-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="xp-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-xp-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-xp-d2` and `itt02-xp-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="xp-d2"]`.
- `localStorage['itt01-xp-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-xp-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Pets sock → `/years/2001/sites/playable/game-3.html`

### L-0182 · `itt01-xp-d3` · sites/xp/index.html

**Open:** `/years/2001/sites/xp/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="xp-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="xp-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-xp-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-xp-d3` and `itt02-xp-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="xp-d3"]`.
- `localStorage['itt01-xp-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-xp-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** Flash skip % → `/years/2001/sites/playable/game-4.html`

### L-0183 · `itt01-xp-d4` · sites/xp/index.html

**Open:** `/years/2001/sites/xp/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="xp-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="xp-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-xp-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-xp-d4` and `itt02-xp-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="xp-d4"]`.
- `localStorage['itt01-xp-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-xp-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** eBay Dutch → `/years/2001/sites/playable/game-5.html`

### L-0184 · `itt01-yahoo-d2` · sites/yahoo/about.html

**Open:** `/years/2001/sites/yahoo/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="yahoo-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `google` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="yahoo-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-yahoo-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-yahoo-d2` and `itt02-yahoo-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="yahoo-d2"]`.
- `localStorage['itt01-yahoo-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-yahoo-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** Clickscape → `/years/2001/sites/playable/game.html`

### L-0185 · `itt01-yahoo-d3` · sites/yahoo/about.html

**Open:** `/years/2001/sites/yahoo/about.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="yahoo-d3"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `google` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="yahoo-d3"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-yahoo-d3']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-yahoo-d3` and `itt02-yahoo-d3` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="yahoo-d3"]`.
- `localStorage['itt01-yahoo-d3']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-yahoo-d3`.
- Does **not** write `itt01-wiki`.
- **Next:** 2001 cabinet → `/years/2001/sites/playable/index.html`

### L-0186 · `itt01-yahoo-d4` · sites/yahoo/index.html

**Open:** `/years/2001/sites/yahoo/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="yahoo-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `google` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="yahoo-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-yahoo-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-yahoo-d4` and `itt02-yahoo-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="yahoo-d4"]`.
- `localStorage['itt01-yahoo-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-yahoo-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** More A → `/years/2001/sites/playable/more-a.html`

### L-0187 · `itt01-yahoo-news` · sites/yahoo/news.html

**Open:** `/years/2001/sites/yahoo/news.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="yahoo-news"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `google` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="yahoo-news"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-yahoo-news']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-yahoo-news` and `itt02-yahoo-news` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="yahoo-news"]`.
- `localStorage['itt01-yahoo-news']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-yahoo-news`.
- Does **not** write `itt01-wiki`.
- **Next:** More B → `/years/2001/sites/playable/more-b.html`

### L-0188 · `itt01-yahoo-news-d2` · sites/yahoo/news.html

**Open:** `/years/2001/sites/yahoo/news.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="yahoo-news-d2"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `google` · `amazon` · `ebay`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="yahoo-news-d2"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-yahoo-news-d2']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-yahoo-news-d2` and `itt02-yahoo-news-d2` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="yahoo-news-d2"]`.
- `localStorage['itt01-yahoo-news-d2']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-yahoo-news-d2`.
- Does **not** write `itt01-wiki`.
- **Next:** D2 Rift → `/years/2001/sites/playable/more-c.html`

### L-0189 · `itt01-appleimac-d4` · sites/appleimac/index.html

**Open:** `/years/2001/sites/appleimac/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="appleimac-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="appleimac-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-appleimac-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-appleimac-d4` and `itt02-appleimac-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="appleimac-d4"]`.
- `localStorage['itt01-appleimac-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-appleimac-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Amazon.com — smile leftover 2001 → `/years/2001/sites/amazon/index.html`

### L-0190 · `itt01-appleimac-d5` · sites/appleimac/index.html

**Open:** `/years/2001/sites/appleimac/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="appleimac-d5"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="appleimac-d5"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-appleimac-d5']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-appleimac-d5` and `itt02-appleimac-d5` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="appleimac-d5"]`.
- `localStorage['itt01-appleimac-d5']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-appleimac-d5`.
- Does **not** write `itt01-wiki`.
- **Next:** Ipod → `/years/2001/sites/apple/ipod.html`

### L-0191 · `itt01-grok-d4` · sites/grok/index.html

**Open:** `/years/2001/sites/grok/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="grok-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="grok-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-grok-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-grok-d4` and `itt02-grok-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="grok-d4"]`.
- `localStorage['itt01-grok-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-grok-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** iMac leftover → `/years/2001/sites/appleimac/index.html`

### L-0192 · `itt01-grok-d5` · sites/grok/index.html

**Open:** `/years/2001/sites/grok/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="grok-d5"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="grok-d5"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-grok-d5']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-grok-d5` and `itt02-grok-d5` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="grok-d5"]`.
- `localStorage['itt01-grok-d5']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-grok-d5`.
- Does **not** write `itt01-wiki`.
- **Next:** Internet Archive Wayback Machine → `/years/2001/sites/archive/index.html`

### L-0193 · `itt01-moveon-d4` · sites/moveon/index.html

**Open:** `/years/2001/sites/moveon/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="moveon-d4"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="moveon-d4"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-moveon-d4']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-moveon-d4` and `itt02-moveon-d4` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="moveon-d4"]`.
- `localStorage['itt01-moveon-d4']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-moveon-d4`.
- Does **not** write `itt01-wiki`.
- **Next:** Ask Jeeves leftover → `/years/2001/sites/askjeeves/index.html`

### L-0194 · `itt01-moveon-d5` · sites/moveon/index.html

**Open:** `/years/2001/sites/moveon/index.html`
**Panel:** `[data-lo-panel]:has([data-lo-save][data-lo-key="moveon-d5"])`
**Need:** needPick=`keep`
**Nostalgia hop targets from here:** `yahoo` · `google` · `amazon`

**Incomplete never writes**
1. Wait until `[data-lo-save][data-lo-key="moveon-d5"]` has `data-lo-bound="1"`.
2. Clear `localStorage['itt01-moveon-d5']`.
3. Click `[data-lo-trap]` → “Trap. That click never writes.” Key absent.
4. Click Save with 0 ticks → “Tick honesty first.” Key absent.
5. Ticks + wrong pick (not `keep`) + Save → “Wrong leftover.” Key absent.
7. `itt01-wiki` stays empty. `itt00-moveon-d5` and `itt02-moveon-d5` stay empty.

**Complete writes**
- Tick every `[data-lo-req]` (usually 2 honesty ticks).
- Click `[data-lo-pick="keep"]`. Wrong pick never writes.
- If `[data-lo-wait]` exists: click it, wait ~800ms, then save. Skip-wait never writes.
- Click `[data-lo-save][data-lo-key="moveon-d5"]`.
- `localStorage['itt01-moveon-d5']` = JSON `{real:true, leftover:true, year:"2001", multiStep:true}`.
- Status: `Saved · itt01-moveon-d5`.
- Does **not** write `itt01-wiki`.
- **Next:** Blogdex → `/years/2001/sites/blogdex/index.html`

