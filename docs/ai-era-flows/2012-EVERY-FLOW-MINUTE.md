# 2012 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2012` or `densify 2012`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | Instagram Android filter → share |
| Chip | ★ One-thing · Instagram Android REAL |
| href | `sites/instagram/android.html` |
| Key | `itt12-ig-android` |
| Shell | Win7 + Chrome habit rising |
| Thesis | The filter leaves iOS. IPO, Maps flop, SOPA blackout. |
| Scale | ILS June ~697 million |
| Bans | Stories, IG video 15s as default, Vine |
| Disk now | **58 HTML** · **32 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt12-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2012/sites/instagram/android.html
      =  pages/home.html data-ott-one-thing="2012"
      =  flowTrails["2012"][0].href
key   =  itt12-ig-android
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt11-*` / `itt13-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2012/sites/instagram/android.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt12-ig-android")` | — |
| Incomplete 1 | Click [data-ig12-share] with no [data-ig12-filter] | **no** |
| Incomplete 2 | Caption only | **no** |
| Incomplete 3 | iOS-only copy as if 2010 gold | **no** |
| Trap | Stories · Vine 6s as default | **no** |
| Complete | Pick [data-ig12-filter] · optional [data-ig12-caption] · [data-ig12-share] | **`itt12-ig-android`** |
| Payload | real:true · year:"2012" · android:true | must include `real` + `year:"2012"` |
| Next | [data-next-flow] → Pinterest | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2012 | `pages/about.html` | HTTP 200 · dual scale · bans |
| 2 | IG Android | `sites/instagram/android.html` | HTTP 200 · filter then share |
| 3 | Pinterest | `sites/pinterest/index.html` | HTTP 200 · open beta leftover |
| 4 | Facebook IPO | `sites/facebook/ipo.html` | HTTP 200 · $38 |
| 5 | Maps flop | `sites/iphone/maps.html` | HTTP 200 · iOS 6 Maps |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2012 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | Instagram Android | `sites/instagram/android.html` | `itt12-ig-android` | use Gold minute — not dest-field | Gold complete | `sites/pinterest/index.html` Pinterest |
| 2 | Pinterest | `sites/pinterest/index.html` | `itt12-pin` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/facebook/ipo.html` IPO |
| 3 | Facebook IPO | `sites/facebook/ipo.html` | `itt12-fb-ipo` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/facebook/index.html` 1B |
| 4 | Facebook 1B | `sites/facebook/index.html` | `itt12-facebook` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/iphone/maps.html` Maps flop |
| 5 | Maps flop | `sites/iphone/maps.html` | `itt12-maps` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/wikipedia/sopa.html` SOPA |
| 6 | SOPA | `sites/wikipedia/sopa.html` | `itt12-sopa` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/medium/index.html` Medium |
| 7 | Medium | `sites/medium/index.html` | `itt12-pop-medium` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/path/index.html` Path |
| 8 | Path | `sites/path/index.html` | `itt12-pop-path` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/flipboard/index.html` Flipboard |
| 9 | Flipboard | `sites/flipboard/index.html` | `itt12-pop-flipboard` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/playable/game.html` Guess Doodle |
| 10 | Guess Doodle | `sites/playable/game.html` | `itt12-game-guessdoodle` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/instagram/android.html` Instagram Android |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/medium/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt12-pop-medium` |
| `sites/path/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt12-pop-path` |
| `sites/flipboard/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt12-pop-flipboard` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/facebook/index.html` | leftover-official | Click [data-lo-trap] — never writes | Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save] | [data-lo-trap] · costume pick | `itt12-pop-facebook` |
| `sites/iphone/maps.html` | leftover-official | Click [data-lo-trap] — never writes | Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save] | [data-lo-trap] · costume pick | `itt12-pop-maps` |
| `sites/wikipedia/sopa.html` | leftover-official | Click [data-lo-trap] — never writes | Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save] | [data-lo-trap] · costume pick | `itt12-pop-sopa` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/reddit/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt12-pop3-reddit` |
| `sites/tinder/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt12-tinder` |
| `sites/windows8/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt12-win8` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/drive/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop4-drive` |
| `sites/kindlefire/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop4-kindlefire` |
| `sites/soundcloud/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop4-soundcloud` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/pinterest/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop5-pinterest` |
| `sites/facebook/ipo-about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop5-facebook` |
| `sites/instagram/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop5-instagram` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/youtube/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop6-youtube` |
| `sites/twitter/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop6-twitter` |
| `sites/tumblr/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt12-pop6-tumblr` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

| F | key | path | kind | field | Incomplete | Complete | Next |
|---|-----|------|------|-------|------------|----------|------|
| F1 | `itt12-pin` | `/years/2012/sites/pinterest/index.html` | new | — | empty field / 0 ticks / trap | pick + honesty + go | `../facebook/ipo.html` |
| F2 | `itt12-fb-ipo` | `/years/2012/sites/facebook/ipo.html` | reuse | — | empty field / 0 ticks / trap | pick + honesty + go | `../facebook/index.html` |
| F3 | `itt12-facebook` | `/years/2012/sites/facebook/index.html` | reuse | — | empty field / 0 ticks / trap | pick + honesty + go | `../iphone/maps.html` |
| F4 | `itt12-maps` | `/years/2012/sites/iphone/maps.html` | reuse | — | empty field / 0 ticks / trap | pick + honesty + go | `../wikipedia/sopa.html` |
| F5 | `itt12-sopa` | `/years/2012/sites/wikipedia/sopa.html` | reuse | — | empty field / 0 ticks / trap | pick + honesty + go | `../instagram/android.html` |

Plaque-only dests that the matrix expects must grow `data-itt-popular-save` **or** a leftover-pop machine. Never run unscoped `build-popular-flows.py`.

---

## 7. Leftover-official machines (e2e matrix)

e2e: trap → 0 ticks → ticks only → wrong pick → empty field → complete. Neighbor year keys stay empty.

| href | key | needPick | field | placeholder | Incomplete | Complete |
|------|-----|----------|-------|-------------|------------|----------|
| `sites/facebook/ipo.html` | `itt12-fb-ipo` | `38` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `38` + field + `[data-lo-save]` |
| `sites/facebook/index.html` | `itt12-facebook` | `mau` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `mau` + field + `[data-lo-save]` |
| `sites/iphone/maps.html` | `itt12-maps` | `flop` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `flop` + field + `[data-lo-save]` |
| `sites/wikipedia/sopa.html` | `itt12-sopa` | `blackout` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `blackout` + field + `[data-lo-save]` |

---

## 8. EVERY dest on disk (minute)

58 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 13, 'dest-field-4x': 25, 'leftover-official': 4, 'leftover-pop': 7, 'year-verb': 1, 'playable': 8}

### D.01 `index.html` — Internet Explorer 9.0 — 2012

| | |
|--|--|
| URL | `/years/2012/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 21058 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About the Web in 2012

| | |
|--|--|
| URL | `/years/2012/pages/about.html` |
| Machine | **dest-field-4x** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 4412 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-action-status`, `data-itt-real-save`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2012

| | |
|--|--|
| URL | `/years/2012/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt12-buzz-more`, `itt12-flip-more`, `itt12-lyft-more`, `itt12-medium-more`, `itt12-path-more`, `itt12-tinder-more`, `itt12-trello-more`, `itt12-waze-more` |
| Primary write | `itt12-buzz-more` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 8819 |
| Hooks | `data-itt-5x-atlas`, `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-tour`, `data-itt-year`, `data-itt-year-extras` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2012 — UX flow map

| | |
|--|--|
| URL | `/years/2012/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3040 |
| Hooks | `data-itt-3x-also`, `data-itt-flow-map`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What's New — 2012

| | |
|--|--|
| URL | `/years/2012/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 1507 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/amazon/index.html` — Amazon — 2012 continuity

| | |
|--|--|
| URL | `/years/2012/sites/amazon/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1854 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.07 `sites/buzzfeed/index.html` — BuzzFeed — 2012 leftover

| | |
|--|--|
| URL | `/years/2012/sites/buzzfeed/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-buzzfeed` |
| Primary write | `itt12-buzzfeed` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2830 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-bf-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.08 `sites/buzzfeed/more.html` — BuzzFeed leftover list — 2012

| | |
|--|--|
| URL | `/years/2012/sites/buzzfeed/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-buzz-more` |
| Primary write | `itt12-buzz-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2653 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.09 `sites/chrome/index.html` — Chrome > IE — May 2012

| | |
|--|--|
| URL | `/years/2012/sites/chrome/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-chrome` |
| Primary write | `itt12-chrome` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2540 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.10 `sites/facebook/index.html` — Facebook — 1 billion MAU · 4 Oct 2012

| | |
|--|--|
| URL | `/years/2012/sites/facebook/index.html` |
| Machine | **leftover-official** |
| Keys in file | `itt12-facebook`, `itt12-pop-facebook` |
| Primary write | `itt12-facebook` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 5679 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-fb1b-req`, `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap`, `data-min-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.11 `sites/facebook/ipo.html` — Facebook IPO — $38 · 18 May 2012

| | |
|--|--|
| URL | `/years/2012/sites/facebook/ipo.html` |
| Machine | **leftover-official** |
| Keys in file | `itt12-fb-ipo` |
| Primary write | `itt12-fb-ipo` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3860 |
| Hooks | `data-ipo-req`, `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap`, `data-min-req`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.12 `sites/flipboard/index.html` — Flipboard — 2012 flip

| | |
|--|--|
| URL | `/years/2012/sites/flipboard/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt12-pop-flipboard` |
| Primary write | `itt12-pop-flipboard` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2977 |
| Hooks | `data-flip-save`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.13 `sites/flipboard/more.html` — Flipboard leftover — 2012

| | |
|--|--|
| URL | `/years/2012/sites/flipboard/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-flip-more` |
| Primary write | `itt12-flip-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2625 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.14 `sites/gmail/index.html` — Gmail — 2012 continuity

| | |
|--|--|
| URL | `/years/2012/sites/gmail/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1843 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.15 `sites/google/index.html` — Google — 2012 continuity

| | |
|--|--|
| URL | `/years/2012/sites/google/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1882 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.16 `sites/googledrive/index.html` — Google Drive leftover — 24 Apr 2012

| | |
|--|--|
| URL | `/years/2012/sites/googledrive/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt12-drive` |
| Primary write | `itt12-drive` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2564 |
| Hooks | `data-drive-pick`, `data-drive-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.17 `sites/googleplus/index.html` — Google+ residual — 2012

| | |
|--|--|
| URL | `/years/2012/sites/googleplus/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-gplus` |
| Primary write | `itt12-gplus` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2476 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.18 `sites/instagram/acquired.html` — Facebook buys Instagram — 9 Apr 2012

| | |
|--|--|
| URL | `/years/2012/sites/instagram/acquired.html` |
| Machine | **thin-door** |
| Keys in file | `itt12-ig-fb` |
| Primary write | `itt12-ig-fb` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2570 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.19 `sites/instagram/android.html` — Instagram — Android 3 Apr 2012

| | |
|--|--|
| URL | `/years/2012/sites/instagram/android.html` |
| Machine | **year-verb** |
| Keys in file | `itt12-ig-android` |
| Primary write | `itt12-ig-android` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3473 |
| Hooks | `data-ig12-caption`, `data-ig12-filter`, `data-ig12-share`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-ig12-share] with no [data-ig12-filter]
2. Caption only
3. iOS-only copy as if 2010 gold

**Trap (never writes):** Stories · Vine 6s as default

**Complete:** Pick [data-ig12-filter] · optional [data-ig12-caption] · [data-ig12-share]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.20 `sites/instagram/index.html` — Instagram — 2012 continuity

| | |
|--|--|
| URL | `/years/2012/sites/instagram/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 2065 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.21 `sites/iphone/index.html` — iPhone 5 — Lightning · 2012

| | |
|--|--|
| URL | `/years/2012/sites/iphone/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-iphone5` |
| Primary write | `itt12-iphone5` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2586 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.22 `sites/iphone/maps.html` — iOS 6 Maps flop — 2012

| | |
|--|--|
| URL | `/years/2012/sites/iphone/maps.html` |
| Machine | **leftover-official** |
| Keys in file | `itt12-maps`, `itt12-pop-maps` |
| Primary write | `itt12-maps` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5027 |
| Hooks | `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap`, `data-maps-req`, `data-min-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.23 `sites/lyft/index.html` — Lyft seed — 2012

| | |
|--|--|
| URL | `/years/2012/sites/lyft/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-lyft` |
| Primary write | `itt12-lyft` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2776 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.24 `sites/lyft/more.html` — Lyft leftover — 2012

| | |
|--|--|
| URL | `/years/2012/sites/lyft/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-lyft-more` |
| Primary write | `itt12-lyft-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2633 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.25 `sites/medium/index.html` — Medium — 2012 draft

| | |
|--|--|
| URL | `/years/2012/sites/medium/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt12-pop-medium` |
| Primary write | `itt12-pop-medium` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2649 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-medium-req`, `data-medium-save`, `data-pop-field`, `data-pop-go`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.26 `sites/medium/more.html` — Medium leftover post — 2012

| | |
|--|--|
| URL | `/years/2012/sites/medium/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-medium-more` |
| Primary write | `itt12-medium-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2633 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.27 `sites/netflix/index.html` — Netflix — 2012 continuity

| | |
|--|--|
| URL | `/years/2012/sites/netflix/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1849 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.28 `sites/path/index.html` — Path — 2012 moment

| | |
|--|--|
| URL | `/years/2012/sites/path/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt12-pop-path` |
| Primary write | `itt12-pop-path` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2603 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-path-req`, `data-path-save`, `data-pop-field`, `data-pop-go`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.29 `sites/path/more.html` — Path leftover — 2012

| | |
|--|--|
| URL | `/years/2012/sites/path/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-path-more` |
| Primary write | `itt12-path-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2629 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.30 `sites/pinterest/index.html` — Pinterest — 2012 public pin

| | |
|--|--|
| URL | `/years/2012/sites/pinterest/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-pin` |
| Primary write | `itt12-pin` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3786 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-pin-save`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.31 `sites/play/index.html` — Google Play / Nexus leftover — 2012

| | |
|--|--|
| URL | `/years/2012/sites/play/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-play` |
| Primary write | `itt12-play` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2481 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.32 `sites/playable/extra-a.html` — Android share — 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | `itt12-game-andshare` |
| Primary write | `itt12-game-andshare` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3972 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.33 `sites/playable/extra-b.html` — IPO pin — 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | `itt12-game-ipopin` |
| Primary write | `itt12-game-ipopin` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3840 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.34 `sites/playable/extra-c.html` — Frog Slice — 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt12-game-frogslice` |
| Primary write | `itt12-game-frogslice` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3241 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.35 `sites/playable/extra-d.html` — Republia Press — 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt12-game-repubpress` |
| Primary write | `itt12-game-repubpress` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3326 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.36 `sites/playable/extra-e.html` — Stroke Guess — 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt12-game-strokeguess` |
| Primary write | `itt12-game-strokeguess` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3341 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-prompt`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.37 `sites/playable/famous.html` — Famous games · 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt12-game-snake`, `itt12-game-tetris` |
| Primary write | `itt12-game-snake` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 4137 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.38 `sites/playable/game.html` — Guess Doodle — 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt12-game-guessdoodle` |
| Primary write | `itt12-game-guessdoodle` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3531 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-gd-prompt`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.39 `sites/playable/index.html` — Playables — 2012

| | |
|--|--|
| URL | `/years/2012/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 2797 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-itt-year-extras`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.40 `sites/reddit/ama.html` — Obama AMA — 29 Aug 2012

| | |
|--|--|
| URL | `/years/2012/sites/reddit/ama.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-ama` |
| Primary write | `itt12-ama` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2539 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-ama-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.41 `sites/reddit/index.html` — reddit — 2012 leftover

| | |
|--|--|
| URL | `/years/2012/sites/reddit/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt12-pop3-reddit` |
| Primary write | `itt12-pop3-reddit` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3403 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-rd12-trap` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.42 `sites/snapchat/index.html` — Snapchat — snaps, not Stories · 2012

| | |
|--|--|
| URL | `/years/2012/sites/snapchat/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-snap` |
| Primary write | `itt12-snap` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2596 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year`, `data-snap12-send` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.43 `sites/soundcloud/index.html` — SoundCloud — 2012 timed comment

| | |
|--|--|
| URL | `/years/2012/sites/soundcloud/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-soundcloud` |
| Primary write | `itt12-soundcloud` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2602 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-sc-save` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.44 `sites/tinder/index.html` — Tinder — 12 Sep 2012

| | |
|--|--|
| URL | `/years/2012/sites/tinder/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt12-pop3-tinder`, `itt12-tinder` |
| Primary write | `itt12-pop3-tinder` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3969 |
| Hooks | `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-tinder-save` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.45 `sites/tinder/more.html` — Tinder leftover swipe — 2012

| | |
|--|--|
| URL | `/years/2012/sites/tinder/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-tinder-more` |
| Primary write | `itt12-tinder-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2635 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.46 `sites/trello/index.html` — Trello — 2012 leftover

| | |
|--|--|
| URL | `/years/2012/sites/trello/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-trello` |
| Primary write | `itt12-trello` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2839 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-trello-go` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.47 `sites/trello/more.html` — Trello leftover card — 2012

| | |
|--|--|
| URL | `/years/2012/sites/trello/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-trello-more` |
| Primary write | `itt12-trello-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2637 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.48 `sites/tumblr/index.html` — Tumblr — 2012 leftover

| | |
|--|--|
| URL | `/years/2012/sites/tumblr/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-tumblr` |
| Primary write | `itt12-tumblr` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2504 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-tumblr-go`, `data-tumblr-post` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.49 `sites/twitter/index.html` — Twitter — still 140 · 2012

| | |
|--|--|
| URL | `/years/2012/sites/twitter/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-tweets` |
| Primary write | `itt12-tweets` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2596 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.50 `sites/uber/index.html` — UberX leftover — 2012

| | |
|--|--|
| URL | `/years/2012/sites/uber/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-uberx` |
| Primary write | `itt12-uberx` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2519 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-uber-go` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.51 `sites/vinewait/index.html` — Vine wait leftover — 2012

| | |
|--|--|
| URL | `/years/2012/sites/vinewait/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt12-pop-vinewait` |
| Primary write | `itt12-pop-vinewait` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2684 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.52 `sites/waze/index.html` — Waze — 2012 leftover

| | |
|--|--|
| URL | `/years/2012/sites/waze/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-waze` |
| Primary write | `itt12-waze` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2826 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-waze-go` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.53 `sites/waze/more.html` — Waze leftover route — 2012

| | |
|--|--|
| URL | `/years/2012/sites/waze/more.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-waze-more` |
| Primary write | `itt12-waze-more` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2638 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.54 `sites/wikipedia/index.html` — Wikipedia — 2012 continuity

| | |
|--|--|
| URL | `/years/2012/sites/wikipedia/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 1987 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.55 `sites/wikipedia/sopa.html` — Wikipedia SOPA/PIPA blackout — 18 Jan 2012

| | |
|--|--|
| URL | `/years/2012/sites/wikipedia/sopa.html` |
| Machine | **leftover-official** |
| Keys in file | `itt12-pop-sopa`, `itt12-sopa` |
| Primary write | `itt12-pop-sopa` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5081 |
| Hooks | `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap`, `data-min-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-popular-req`, `data-sopa-req`, `data-sopa-save` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.56 `sites/windows8/index.html` — Windows 8 — 26 Oct 2012

| | |
|--|--|
| URL | `/years/2012/sites/windows8/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt12-pop3-windows8`, `itt12-win8` |
| Primary write | `itt12-pop3-windows8` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4065 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.57 `sites/yahoo/index.html` — Yahoo — 2012 continuity

| | |
|--|--|
| URL | `/years/2012/sites/yahoo/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt12-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1861 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.58 `sites/youtube/index.html` — YouTube — Gangnam 1B · 21 Dec 2012

| | |
|--|--|
| URL | `/years/2012/sites/youtube/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt12-yt` |
| Primary write | `itt12-yt` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2597 |
| Hooks | `data-4x-go`, `data-4x-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2012"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Frog Slice** `itt12-game-frogslice` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Republia Press** `itt12-game-repubpress` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **Stroke Guess** `itt12-game-strokeguess` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2012`)

1. Recite star `itt12-ig-android` · guided 6 · bans.
2. Count HTML. Now **58**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2012 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt12-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

