# 2022 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2022` or `densify 2022`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | ChatGPT Send (free preview) |
| Chip | ★ One-thing · ChatGPT Send REAL |
| href | `sites/chatgpt/index.html` |
| Key | `itt22-chatgpt` |
| Shell | Win11 residual + Chrome habit |
| Thesis | Send a prompt. Plus / GPT-4 / Bing Chat / Threads / X are 2023. |
| Scale | No ILS June · Netcraft January 2023 cell may print as look-ahead labeled |
| Bans | Plus $20, GPT-4, Bing Chat, Threads, X rebrand, GPT-4o, Gemini |
| Disk now | **28 HTML** · **16 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt22-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2022/sites/chatgpt/index.html
      =  pages/home.html data-ott-one-thing="2022"
      =  flowTrails["2022"][0].href
key   =  itt22-chatgpt
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt21-*` / `itt23-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2022/sites/chatgpt/index.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt22-chatgpt")` | — |
| Incomplete 1 | Empty [data-gpt22-prompt] + [data-gpt22-send] | **no** |
| Incomplete 2 | Whitespace only | **no** |
| Incomplete 3 | Open page and leave | **no** |
| Trap | [data-gpt22-plus] Subscribe Plus · [data-gpt22-gpt4] · [data-gpt22-bing] | **no** |
| Complete | Type ≥2 chars in [data-gpt22-prompt] · [data-gpt22-send] | **`itt22-chatgpt`** |
| Payload | real:true · year:"2022" · preview:true · plus:false · gpt4:false | must include `real` + `year:"2022"` |
| Next | [data-next-flow] → Twitter leftover (still Twitter) | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2022 | `pages/about.html` | HTTP 200 · Send · Plus is 2023 |
| 2 | ChatGPT Send | `sites/chatgpt/index.html` | HTTP 200 · free preview |
| 3 | Twitter leftover | `sites/twitter/index.html` | HTTP 200 · still Twitter |
| 4 | Wordle leftover | `sites/wordle/index.html` | HTTP 200 · NYT |
| 5 | Stable Diffusion | `sites/stablediffusion/index.html` | HTTP 200 · 22 Aug leftover |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2022 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | ChatGPT Send | `sites/chatgpt/index.html` | `itt22-chatgpt` | use Gold minute — not dest-field | Gold complete | `sites/twitter/index.html` Twitter leftover |
| 2 | Twitter leftover | `sites/twitter/index.html` | `itt22-twitter` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/wordle/index.html` Wordle leftover |
| 3 | Wordle leftover | `sites/wordle/index.html` | `itt22-wordle` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/stablediffusion/index.html` Stable Diffusion |
| 4 | Stable Diffusion | `sites/stablediffusion/index.html` | `itt22-sd` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/mastodon/index.html` Mastodon leftover |
| 5 | Mastodon leftover | `sites/mastodon/index.html` | `itt22-mastodon` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/bereal/index.html` BeReal leftover |
| 6 | BeReal leftover | `sites/bereal/index.html` | `itt22-bereal` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/dalle2/index.html` DALL·E 2 leftover |
| 7 | DALL·E 2 leftover | `sites/dalle2/index.html` | `itt22-dalle2` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/chrome/index.html` Chrome habit |
| 8 | Chrome habit | `sites/chrome/index.html` | `itt22-chrome` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/windows10/index.html` Windows 10 |
| 9 | Windows 10 residual | `sites/windows10/index.html` | `itt22-win10` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/playable/game.html` Prompt Box |
| 10 | Prompt Box | `sites/playable/game.html` | `itt22-game-prompt` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/chatgpt/index.html` ChatGPT |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/youtube/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-pop-youtube` |
| `sites/wikipedia/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-pop-wikipedia` |
| `sites/facebook/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-pop-facebook` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/twitter/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-twitter` |
| `sites/wordle/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-wordle` |
| `sites/stablediffusion/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-sd` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/tiktok/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-pop3-tiktok` |
| `sites/midjourney/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-pop3-midjourney` |
| `sites/lensa/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt22-pop3-lensa` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/chatgpt/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop4-chatgpt` |
| `sites/dalle2/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop4-dalle2` |
| `sites/mastodon/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop4-mastodon` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/bereal/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop5-bereal` |
| `sites/chrome/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop5-chrome` |
| `sites/win10/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop5-win10` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/notion/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop6-notion` |
| `sites/characterai/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop6-characterai` |
| `sites/github-copilot/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt22-pop6-github-copilot` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

No `popular-flows.matrix.json` rows for 2022. Do **not** invent F-keys. Official 10 + leftover 27 cover the 3× count.

---

## 7. Leftover-official machines (e2e matrix)

No leftover-official dests stamped for 2022. Official 10 still must write via gold / 4x / leftover-pop. Adding a `[data-lo-panel]` is allowed on densify if it does not fight the star key.

---

## 8. EVERY dest on disk (minute)

28 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 8, 'year-verb': 1, 'dest-field-4x': 2, 'leftover-pop': 9, 'playable': 8}

### D.01 `index.html` — Chrome habit — 2022

| | |
|--|--|
| URL | `/years/2022/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt22-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 20952 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About 2022 — dual scale · bans

| | |
|--|--|
| URL | `/years/2022/pages/about.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt22-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 4647 |
| Hooks | `data-itt-real-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2022

| | |
|--|--|
| URL | `/years/2022/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt22-ch-lx`, `itt22-game-lx`, `itt22-md-lx`, `itt22-sd-lx`, `itt22-tw-lx`, `itt22-wd-lx` |
| Primary write | `itt22-ch-lx` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 6529 |
| Hooks | `data-itt-mass`, `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-tour`, `data-itt-year`, `data-itt-year-extras` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2022 flow map

| | |
|--|--|
| URL | `/years/2022/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | `itt22-bereal`, `itt22-chatgpt`, `itt22-chrome`, `itt22-dalle2`, `itt22-game-prompt`, `itt22-mastodon`, `itt22-pop3-lensa`, `itt22-pop3-midjourney`, `itt22-pop3-tiktok`, `itt22-sd`, `itt22-twitter`, `itt22-win10`, `itt22-wordle` |
| Primary write | `itt22-bereal` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4627 |
| Hooks | `data-itt-3x-also`, `data-itt-pop3x`, `data-itt-ten-flows`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What's new — 2022

| | |
|--|--|
| URL | `/years/2022/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt22-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 1474 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/bereal/index.html` — BeReal leftover — 2022

| | |
|--|--|
| URL | `/years/2022/sites/bereal/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt22-bereal` |
| Primary write | `itt22-bereal` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2376 |
| Hooks | `data-br22-filter`, `data-br22-go`, `data-br22-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.07 `sites/chatgpt/index.html` — ChatGPT research preview — 2022

| | |
|--|--|
| URL | `/years/2022/sites/chatgpt/index.html` |
| Machine | **year-verb** |
| Keys in file | `itt22-chatgpt`, `itt22-gpt-lx` |
| Primary write | `itt22-chatgpt` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3906 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-gpt22-prompt`, `data-gpt22-send`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Empty [data-gpt22-prompt] + [data-gpt22-send]
2. Whitespace only
3. Open page and leave

**Trap (never writes):** [data-gpt22-plus] Subscribe Plus · [data-gpt22-gpt4] · [data-gpt22-bing]

**Complete:** Type ≥2 chars in [data-gpt22-prompt] · [data-gpt22-send]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.08 `sites/chrome/index.html` — Chrome habit — 2022 leftover

| | |
|--|--|
| URL | `/years/2022/sites/chrome/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt22-ch-lx`, `itt22-chrome` |
| Primary write | `itt22-ch-lx` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3434 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-ch22-google`, `data-ch22-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.09 `sites/dalle2/index.html` — DALL·E 2 leftover — 2022

| | |
|--|--|
| URL | `/years/2022/sites/dalle2/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt22-dalle2` |
| Primary write | `itt22-dalle2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2471 |
| Hooks | `data-dl22-go`, `data-dl22-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.10 `sites/facebook/index.html` — Facebook leftover — 2022

| | |
|--|--|
| URL | `/years/2022/sites/facebook/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop-facebook` |
| Primary write | `itt22-pop-facebook` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2589 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.11 `sites/lensa/index.html` — Lensa leftover — 2022

| | |
|--|--|
| URL | `/years/2022/sites/lensa/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop3-lensa` |
| Primary write | `itt22-pop3-lensa` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2625 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-pop3-trap` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.12 `sites/mastodon/index.html` — Mastodon leftover — 2022

| | |
|--|--|
| URL | `/years/2022/sites/mastodon/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt22-mastodon`, `itt22-md-lx` |
| Primary write | `itt22-mastodon` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3212 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-md22-go` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.13 `sites/midjourney/index.html` — Midjourney leftover — 2022

| | |
|--|--|
| URL | `/years/2022/sites/midjourney/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop3-midjourney` |
| Primary write | `itt22-pop3-midjourney` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2655 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-pop3-trap` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.14 `sites/playable/extra-a.html` — Send drill — 2022

| | |
|--|--|
| URL | `/years/2022/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | `itt22-extra-a` |
| Primary write | `itt22-extra-a` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 2330 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.15 `sites/playable/extra-b.html` — 1 million — 2022

| | |
|--|--|
| URL | `/years/2022/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | `itt22-extra-b` |
| Primary write | `itt22-extra-b` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 2150 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.16 `sites/playable/extra-c.html` — Word Guess — 2022

| | |
|--|--|
| URL | `/years/2022/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt22-game-wordguess` |
| Primary write | `itt22-game-wordguess` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3374 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.17 `sites/playable/extra-d.html` — Masto Inst — 2022

| | |
|--|--|
| URL | `/years/2022/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt22-game-mastoinst` |
| Primary write | `itt22-game-mastoinst` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3406 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.18 `sites/playable/extra-e.html` — BeReal Two — 2022

| | |
|--|--|
| URL | `/years/2022/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt22-game-bereal2` |
| Primary write | `itt22-game-bereal2` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3415 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-hold-ms`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.19 `sites/playable/famous.html` — Famous games · 2022

| | |
|--|--|
| URL | `/years/2022/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt22-fam-2`, `itt22-famous-lx`, `itt22-game-breakout`, `itt22-game-memory` |
| Primary write | `itt22-fam-2` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 5558 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.20 `sites/playable/game.html` — Prompt Box — 2022

| | |
|--|--|
| URL | `/years/2022/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt22-game-lx`, `itt22-game-prompt` |
| Primary write | `itt22-game-lx` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 4077 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-prompt-field`, `data-prompt-go`, `data-prompt-req` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.21 `sites/playable/index.html` — 2022 playables

| | |
|--|--|
| URL | `/years/2022/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | _none harvested_ |
| Primary write | `itt22-MISSING` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 2663 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-itt-year-extras`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.22 `sites/stablediffusion/index.html` — Stable Diffusion leftover — 22 Aug

| | |
|--|--|
| URL | `/years/2022/sites/stablediffusion/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop-stablediffusion`, `itt22-sd`, `itt22-sd-lx` |
| Primary write | `itt22-pop-stablediffusion` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4748 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-sd22-go`, `data-sd22-prompt`, `data-sd22-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.23 `sites/tiktok/index.html` — TikTok leftover — 2022

| | |
|--|--|
| URL | `/years/2022/sites/tiktok/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop3-tiktok` |
| Primary write | `itt22-pop3-tiktok` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2603 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-pop3-trap` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.24 `sites/twitter/index.html` — Twitter leftover — still Twitter

| | |
|--|--|
| URL | `/years/2022/sites/twitter/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop-twitter`, `itt22-tw-lx`, `itt22-twitter` |
| Primary write | `itt22-pop-twitter` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4615 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-tw22-go`, `data-tw22-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.25 `sites/wikipedia/index.html` — Wikipedia — 2022 leftover

| | |
|--|--|
| URL | `/years/2022/sites/wikipedia/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop-wikipedia` |
| Primary write | `itt22-pop-wikipedia` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2559 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.26 `sites/windows10/index.html` — Windows 10 residual — 2022

| | |
|--|--|
| URL | `/years/2022/sites/windows10/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt22-win10` |
| Primary write | `itt22-win10` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2309 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-w10-req`, `data-w10-save` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.27 `sites/wordle/index.html` — Wordle leftover — NYT 2022

| | |
|--|--|
| URL | `/years/2022/sites/wordle/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop-wordle`, `itt22-wd-lx`, `itt22-wordle` |
| Primary write | `itt22-pop-wordle` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4674 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-wd22-go`, `data-wd22-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.28 `sites/youtube/index.html` — YouTube — 2022 leftover

| | |
|--|--|
| URL | `/years/2022/sites/youtube/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt22-pop-youtube` |
| Primary write | `itt22-pop-youtube` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2764 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2022"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Word Guess** `itt22-game-wordguess` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Masto Inst** `itt22-game-mastoinst` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **BeReal Two** `itt22-game-bereal2` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2022`)

1. Recite star `itt22-chatgpt` · guided 6 · bans.
2. Count HTML. Now **28**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2022 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt22-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

