# 2018 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2018` or `densify 2018`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | GDPR Manage (not Accept All) |
| Chip | ★ One-thing · GDPR Manage REAL |
| href | `sites/gdpr/index.html` |
| Key | `itt18-gdpr` |
| Shell | Win10 + Chrome habit |
| Thesis | The banner is the door. Accept All is the trap. ILS June table ENDs. |
| Scale | ILS June 1,630,322,579 LAST CELL · after this: Netcraft January |
| Bans | invent ILS June after 2018, TikTok as 2018 gold, Gemini |
| Disk now | **31 HTML** · **15 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt18-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2018/sites/gdpr/index.html
      =  pages/home.html data-ott-one-thing="2018"
      =  flowTrails["2018"][0].href
key   =  itt18-gdpr
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt17-*` / `itt19-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2018/sites/gdpr/index.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt18-gdpr")` | — |
| Incomplete 1 | [data-gdpr-accept-all] — trap, never writes star | **no** |
| Incomplete 2 | [data-gdpr-save] with 0 [data-gdpr-req] | **no** |
| Incomplete 3 | Necessary only without Manage path | **no** |
| Trap | [data-gdpr-accept-all] · [data-gdpr-darkpattern] | **no** |
| Complete | [data-gdpr-manage] · tick [data-gdpr-req] · necessary + date 25 May · [data-gdpr-save] | **`itt18-gdpr`** |
| Payload | real:true · year:"2018" · manage:true · acceptAll:false | must include `real` + `year:"2018"` |
| Next | [data-next-flow] → TikTok For You leftover | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2018 | `pages/about.html` | HTTP 200 · ILS June ENDS · bans |
| 2 | GDPR Manage | `sites/gdpr/index.html` | HTTP 200 · not Accept All |
| 3 | TikTok FYP | `sites/tiktok/fyp.html` | HTTP 200 · leftover |
| 4 | Hearing | `sites/trust/index.html` | HTTP 200 · leftover |
| 5 | IGTV | `sites/instagram/igtv.html` | HTTP 200 · leftover |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2018 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | GDPR Manage | `sites/gdpr/index.html` | `itt18-gdpr` | use Gold minute — not dest-field | Gold complete | `sites/tiktok/fyp.html` TikTok For You |
| 2 | TikTok For You | `sites/tiktok/fyp.html` | `itt18-tiktok-fyp` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/trust/index.html` Hearing |
| 3 | Hearing | `sites/trust/index.html` | `itt18-hearing` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/instagram/igtv.html` IGTV |
| 4 | IGTV | `sites/instagram/igtv.html` | `itt18-igtv` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/chrome/not-secure.html` Chrome 68 |
| 5 | Chrome 68 | `sites/chrome/not-secure.html` | `itt18-not-secure` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/homepod/index.html` HomePod |
| 6 | HomePod | `sites/homepod/index.html` | `itt18-homepod` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/spectre/index.html` Spectre |
| 7 | Spectre | `sites/spectre/index.html` | `itt18-spectre` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/fortnite/switch.html` Fortnite on Switch |
| 8 | Fortnite on Switch | `sites/fortnite/switch.html` | `itt18-fn-switch` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/github/microsoft.html` GitHub |
| 9 | GitHub $7.5B | `sites/github/microsoft.html` | `itt18-github` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/playable/game.html` Consent Dash |
| 10 | Consent Dash | `sites/playable/game.html` | `itt18-game-consentdash` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/gdpr/index.html` GDPR |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/reddit/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop-reddit` |
| `sites/youtube/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop-youtube` |
| `sites/wikipedia/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop-wikipedia` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/discord/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop-discord` |
| `sites/applemusic/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop-applemusic` |
| `sites/fortnite/creative.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop-creative` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/tiktok/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop3-tiktok` |
| `sites/github/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-pop3-github` |
| `sites/homepod/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt18-homepod` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/cambridge/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop4-cambridge` |
| `sites/notsecure/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop4-notsecure` |
| `sites/spectre/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop4-spectre` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/gdpr/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop5-gdpr` |
| `sites/igtv/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop5-igtv` |
| `sites/fyp/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop5-fyp` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/fortnite-switch/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop6-fortnite-switch` |
| `sites/github-ms/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop6-github-ms` |
| `sites/homepod/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt18-pop6-homepod` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

| F | key | path | kind | field | Incomplete | Complete | Next |
|---|-----|------|------|-------|------------|----------|------|
| F1 | `itt18-pop-reddit` | `/years/2018/sites/reddit/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F2 | `itt18-pop-youtube` | `/years/2018/sites/youtube/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F3 | `itt18-pop-wikipedia` | `/years/2018/sites/wikipedia/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F4 | `itt18-gdpr` | `/years/2018/sites/gdpr/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F5 | `itt18-tiktok-fyp` | `/years/2018/sites/tiktok/fyp.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |

Plaque-only dests that the matrix expects must grow `data-itt-popular-save` **or** a leftover-pop machine. Never run unscoped `build-popular-flows.py`.

---

## 7. Leftover-official machines (e2e matrix)

No leftover-official dests stamped for 2018. Official 10 still must write via gold / 4x / leftover-pop. Adding a `[data-lo-panel]` is allowed on densify if it does not fight the star key.

---

## 8. EVERY dest on disk (minute)

31 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 5, 'leftover-pop': 9, 'dest-field-4x': 8, 'year-verb': 1, 'playable': 8}

### D.01 `index.html` — Chrome habit — 2018

| | |
|--|--|
| URL | `/years/2018/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt18-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 20911 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About 2018 — dual scale · bans

| | |
|--|--|
| URL | `/years/2018/pages/about.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt18-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 2406 |
| Hooks | `data-itt-real-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2018

| | |
|--|--|
| URL | `/years/2018/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt18-am`, `itt18-am-2`, `itt18-cab`, `itt18-consent`, `itt18-dc`, `itt18-dc-2`, `itt18-famous`, `itt18-fn-cr`, `itt18-fn-sw`, `itt18-fyp`, `itt18-gdpr-2x`, `itt18-gdpr-door`, `itt18-gh-h`, `itt18-gh-ms`, `itt18-hear`, `itt18-hear-2`, `itt18-hp`, `itt18-hp-2`, `itt18-igtv-2`, `itt18-igtv-lx`, `itt18-ns`, `itt18-ns-2`, `itt18-reddit`, `itt18-reddit-2`, `itt18-sp`, `itt18-sp-2`, `itt18-tt-h`, `itt18-wiki`, `itt18-wiki-2`, `itt18-xa`, `itt18-xb`, `itt18-xc`, `itt18-xd`, `itt18-xe`, `itt18-yt`, `itt18-yt-2` |
| Primary write | `itt18-am` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 8917 |
| Hooks | `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-tour`, `data-itt-year`, `data-itt-year-extras` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2018 flow map

| | |
|--|--|
| URL | `/years/2018/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | `itt18-am`, `itt18-am-2`, `itt18-cab`, `itt18-consent`, `itt18-dc`, `itt18-dc-2`, `itt18-famous`, `itt18-fn-cr`, `itt18-fn-sw`, `itt18-fn-switch`, `itt18-fyp`, `itt18-game-consentdash`, `itt18-gdpr`, `itt18-gdpr-2x`, `itt18-gdpr-door`, `itt18-gh-h`, `itt18-gh-ms`, `itt18-github`, `itt18-hear`, `itt18-hear-2`, `itt18-hearing`, `itt18-homepod`, `itt18-hp`, `itt18-hp-2`, `itt18-igtv`, `itt18-igtv-2`, `itt18-igtv-lx`, `itt18-not-secure`, `itt18-ns`, `itt18-ns-2`, `itt18-pop-reddit`, `itt18-pop-wikipedia`, `itt18-pop-youtube`, `itt18-reddit`, `itt18-reddit-2`, `itt18-sp`, `itt18-sp-2`, `itt18-spectre`, `itt18-tiktok-fyp`, `itt18-tt-h`, `itt18-wiki`, `itt18-wiki-2`, `itt18-xa`, `itt18-xb`, `itt18-xc`, `itt18-xd`, `itt18-xe`, `itt18-yt`, `itt18-yt-2` |
| Primary write | `itt18-am` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 11800 |
| Hooks | `data-itt-2x-map`, `data-itt-3x-also`, `data-itt-flow-map`, `data-itt-pop3x`, `data-itt-ten-flows`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What's new — 2018

| | |
|--|--|
| URL | `/years/2018/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt18-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 1152 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/applemusic/index.html` — Apple Music — 2018 leftover

| | |
|--|--|
| URL | `/years/2018/sites/applemusic/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-am`, `itt18-am-2`, `itt18-pop-applemusic` |
| Primary write | `itt18-am` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4296 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.07 `sites/chrome/not-secure.html` — Chrome 68 — Not secure · 24 Jul 2018

| | |
|--|--|
| URL | `/years/2018/sites/chrome/not-secure.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-not-secure`, `itt18-ns`, `itt18-ns-2` |
| Primary write | `itt18-not-secure` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4508 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-capture-cite`, `data-itt-year`, `data-ns-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.08 `sites/discord/index.html` — Discord — 2018 leftover

| | |
|--|--|
| URL | `/years/2018/sites/discord/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-dc`, `itt18-dc-2`, `itt18-pop-discord` |
| Primary write | `itt18-dc` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4262 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.09 `sites/fortnite/creative.html` — Fortnite Creative — 2018 leftover

| | |
|--|--|
| URL | `/years/2018/sites/fortnite/creative.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-fn-cr`, `itt18-pop-creative` |
| Primary write | `itt18-fn-cr` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3468 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.10 `sites/fortnite/switch.html` — Fortnite on Switch — 12 Jun 2018

| | |
|--|--|
| URL | `/years/2018/sites/fortnite/switch.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-fn-sw`, `itt18-fn-switch` |
| Primary write | `itt18-fn-sw` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3311 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-fns-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.11 `sites/gdpr/index.html` — We use cookies — GDPR · 25 May 2018

| | |
|--|--|
| URL | `/years/2018/sites/gdpr/index.html` |
| Machine | **year-verb** |
| Keys in file | `itt18-cmp`, `itt18-gdpr`, `itt18-gdpr-door` |
| Primary write | `itt18-cmp` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3988 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-gdpr-manage`, `data-gdpr-req`, `data-gdpr-save`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. [data-gdpr-accept-all] — trap, never writes star
2. [data-gdpr-save] with 0 [data-gdpr-req]
3. Necessary only without Manage path

**Trap (never writes):** [data-gdpr-accept-all] · [data-gdpr-darkpattern]

**Complete:** [data-gdpr-manage] · tick [data-gdpr-req] · necessary + date 25 May · [data-gdpr-save]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.12 `sites/gdpr/manage.html` — Manage preferences — GDPR 2018

| | |
|--|--|
| URL | `/years/2018/sites/gdpr/manage.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-gdpr-2x` |
| Primary write | `itt18-gdpr-2x` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2807 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.13 `sites/github/index.html` — GitHub leftover — 2018

| | |
|--|--|
| URL | `/years/2018/sites/github/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-gh-h`, `itt18-pop3-github` |
| Primary write | `itt18-gh-h` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3733 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.14 `sites/github/microsoft.html` — Microsoft buys GitHub — $7.5B · 2018

| | |
|--|--|
| URL | `/years/2018/sites/github/microsoft.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-gh-ms`, `itt18-github` |
| Primary write | `itt18-gh-ms` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3324 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-gh-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.15 `sites/homepod/index.html` — HomePod — $349 · 9 Feb 2018

| | |
|--|--|
| URL | `/years/2018/sites/homepod/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-homepod`, `itt18-hp`, `itt18-hp-2`, `itt18-pop3-homepod` |
| Primary write | `itt18-homepod` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5129 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-hp-req`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.16 `sites/instagram/igtv.html` — IGTV — 20 Jun 2018

| | |
|--|--|
| URL | `/years/2018/sites/instagram/igtv.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-igtv`, `itt18-igtv-2`, `itt18-igtv-lx` |
| Primary write | `itt18-igtv` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4113 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-igtv-post`, `data-igtv-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.17 `sites/playable/extra-a.html` — Manage tap — 2018

| | |
|--|--|
| URL | `/years/2018/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | `itt18-game-managetap`, `itt18-xa` |
| Primary write | `itt18-game-managetap` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 4846 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.18 `sites/playable/extra-b.html` — FYP tap — 2018

| | |
|--|--|
| URL | `/years/2018/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | `itt18-game-fyptap`, `itt18-xb` |
| Primary write | `itt18-game-fyptap` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 4851 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.19 `sites/playable/extra-c.html` — Range 15 — 2018

| | |
|--|--|
| URL | `/years/2018/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt18-game-range15` |
| Primary write | `itt18-game-range15` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3253 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.20 `sites/playable/extra-d.html` — Drift Corner — 2018

| | |
|--|--|
| URL | `/years/2018/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt18-game-driftcorner` |
| Primary write | `itt18-game-driftcorner` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3361 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-hold-ms`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.21 `sites/playable/extra-e.html` — Banner Fight — 2018

| | |
|--|--|
| URL | `/years/2018/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt18-game-bannerfight` |
| Primary write | `itt18-game-bannerfight` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3393 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.22 `sites/playable/famous.html` — Famous games — 2018 leftover

| | |
|--|--|
| URL | `/years/2018/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt18-famous`, `itt18-game-memory`, `itt18-game-pong` |
| Primary write | `itt18-famous` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 4777 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.23 `sites/playable/game.html` — Consent Dash — 2018

| | |
|--|--|
| URL | `/years/2018/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt18-consent`, `itt18-game-consentdash` |
| Primary write | `itt18-consent` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3897 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-cd-manage`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.24 `sites/playable/index.html` — 2018 playables

| | |
|--|--|
| URL | `/years/2018/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | `itt18-cab` |
| Primary write | `itt18-cab` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3399 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-itt-year-extras`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.25 `sites/reddit/index.html` — reddit — 2018 leftover

| | |
|--|--|
| URL | `/years/2018/sites/reddit/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-pop-reddit`, `itt18-reddit`, `itt18-reddit-2` |
| Primary write | `itt18-pop-reddit` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4676 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.26 `sites/spectre/index.html` — Spectre / Meltdown — 3 Jan 2018

| | |
|--|--|
| URL | `/years/2018/sites/spectre/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-sp`, `itt18-sp-2`, `itt18-spectre` |
| Primary write | `itt18-sp` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4141 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-sp-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.27 `sites/tiktok/fyp.html` — TikTok For You — 2 Aug 2018

| | |
|--|--|
| URL | `/years/2018/sites/tiktok/fyp.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-fyp`, `itt18-tiktok-fyp` |
| Primary write | `itt18-fyp` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3419 |
| Hooks | `data-4x-go`, `data-fyp-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.28 `sites/tiktok/index.html` — TikTok leftover — 2018

| | |
|--|--|
| URL | `/years/2018/sites/tiktok/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-pop3-tiktok`, `itt18-tt-h` |
| Primary write | `itt18-pop3-tiktok` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3708 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.29 `sites/trust/index.html` — Hearing — 10 Apr 2018

| | |
|--|--|
| URL | `/years/2018/sites/trust/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt18-hear`, `itt18-hear-2`, `itt18-hearing` |
| Primary write | `itt18-hear` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4118 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-hear-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.30 `sites/wikipedia/index.html` — Wikipedia — 2018 leftover

| | |
|--|--|
| URL | `/years/2018/sites/wikipedia/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-pop-wikipedia`, `itt18-wiki`, `itt18-wiki-2` |
| Primary write | `itt18-pop-wikipedia` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4777 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.31 `sites/youtube/index.html` — YouTube — 2018 leftover

| | |
|--|--|
| URL | `/years/2018/sites/youtube/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt18-pop-youtube`, `itt18-yt`, `itt18-yt-2` |
| Primary write | `itt18-pop-youtube` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4818 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2018"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Range 15** `itt18-game-range15` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Drift Corner** `itt18-game-driftcorner` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **Banner Fight** `itt18-game-bannerfight` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2018`)

1. Recite star `itt18-gdpr` · guided 6 · bans.
2. Count HTML. Now **31**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2018 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt18-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

