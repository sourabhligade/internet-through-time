# 2011 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2011` or `densify 2011`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | Google+ circles + Hangout |
| Chip | ★ One-thing · Google+ REAL |
| href | `sites/googleplus/index.html` |
| Key | `itt11-gplus` |
| Shell | Win7 + IE 9 leftover / Chrome product room |
| Thesis | Circles + Hangouts try to unseat Facebook; Siri and Timeline are leftovers. |
| Scale | ILS June 346 million class · dual-cite only |
| Bans | IG Android, Stories, Reels, web IG composer as default |
| Disk now | **32 HTML** · **19 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt11-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2011/sites/googleplus/index.html
      =  pages/home.html data-ott-one-thing="2011"
      =  flowTrails["2011"][0].href
key   =  itt11-gplus
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt10-*` / `itt12-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2011/sites/googleplus/index.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt11-gplus")` | — |
| Incomplete 1 | Click [data-gp11-hangout] with no [data-gp11-circle] and no people | **no** |
| Incomplete 2 | One person only (need two) | **no** |
| Incomplete 3 | Ticks missing if [data-gp11-req] present | **no** |
| Trap | Snap Stories · IG Android composer | **no** |
| Complete | Name a circle + pick two [data-gp11-person] + [data-gp11-hangout] + tick [data-gp11-req] | **`itt11-gplus`** |
| Payload | real:true · year:"2011" · hangout:true | must include `real` + `year:"2011"` |
| Next | [data-next-flow] → Spotify US | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2011 | `pages/about.html` | HTTP 200 · dual scale · bans |
| 2 | Google+ | `sites/googleplus/index.html` | HTTP 200 · circles + Hangout |
| 3 | Spotify US | `sites/spotify/index.html` | HTTP 200 · US leftover |
| 4 | Siri | `sites/iphone/index.html` | HTTP 200 · iPhone 4S leftover |
| 5 | Timeline | `sites/facebook/index.html` | HTTP 200 · Timeline leftover |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2011 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | Google+ | `sites/googleplus/index.html` | `itt11-gplus` | use Gold minute — not dest-field | Gold complete | `sites/spotify/index.html` Spotify US |
| 2 | Spotify US | `sites/spotify/index.html` | `itt11-spotify` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/iphone/index.html` Siri |
| 3 | Siri | `sites/iphone/index.html` | `itt11-siri` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/facebook/index.html` Timeline |
| 4 | Timeline | `sites/facebook/index.html` | `itt11-timeline` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/ipad/index.html` iPad 2 |
| 5 | iPad 2 leftover | `sites/ipad/index.html` | `itt11-ipad2` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/airbnb/index.html` Airbnb |
| 6 | Airbnb leftover | `sites/airbnb/index.html` | `itt11-airbnb` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/instagram/index.html` IG iOS |
| 7 | IG iOS leftover | `sites/instagram/index.html` | `itt11-ig` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/twitter/index.html` Twitter |
| 8 | Twitter leftover | `sites/twitter/index.html` | `itt11-tweets` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/qwikster/index.html` Qwikster |
| 9 | Qwikster leftover | `sites/qwikster/index.html` | `itt11-qwikster` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/playable/game.html` Letter Swap |
| 10 | Letter Swap | `sites/playable/game.html` | `itt11-game-letterswap` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/googleplus/index.html` Google+ |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/icloud/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt11-pop-icloud` |
| `sites/pinterest/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt11-pop-pinterest` |
| `sites/linkedin/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt11-pop-linkedin` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/spotify/index.html` | dest-field-4x | Click [data-4x-go] with empty [data-4x-field] | Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star | Future-year costume on this dest | `itt11-spotify` |
| `sites/iphone/index.html` | dest-field-4x | Click [data-4x-go] with empty [data-4x-field] | Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star | Future-year costume on this dest | `itt11-siri` |
| `sites/airbnb/index.html` | dest-field-4x | Click [data-4x-go] with empty [data-4x-field] | Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star | Future-year costume on this dest | `itt11-airbnb` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/snapchat/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt11-pop3-snapchat` |
| `sites/tumblr/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt11-pop3-tumblr` |
| `sites/youtube/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt11-pop3-youtube` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/siri/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop4-siri` |
| `sites/ipad2/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop4-ipad2` |
| `sites/twitter/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop4-twitter` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/qwikster/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop5-qwikster` |
| `sites/hangouts/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop5-hangouts` |
| `sites/ie9/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop5-ie9` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/groupon/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop6-groupon` |
| `sites/dropbox/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop6-dropbox` |
| `sites/netflix/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt11-pop6-netflix` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

No `popular-flows.matrix.json` rows for 2011. Do **not** invent F-keys. Official 10 + leftover 27 cover the 3× count.

---

## 7. Leftover-official machines (e2e matrix)

e2e: trap → 0 ticks → ticks only → wrong pick → empty field → complete. Neighbor year keys stay empty.

| href | key | needPick | field | placeholder | Incomplete | Complete |
|------|-----|----------|-------|-------------|------------|----------|
| `sites/facebook/index.html` | `itt11-timeline` | `tl` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `tl` + field + `[data-lo-save]` |
| `sites/instagram/index.html` | `itt11-ig` | `ios` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `ios` + field + `[data-lo-save]` |
| `sites/qwikster/index.html` | `itt11-qwikster` | `funeral` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `funeral` + field + `[data-lo-save]` |

---

## 8. EVERY dest on disk (minute)

32 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 5, 'dest-field-4x': 9, 'leftover-official': 3, 'year-verb': 1, 'leftover-pop': 6, 'playable': 8}

### D.01 `index.html` — Internet Explorer 9.0 — 2011

| | |
|--|--|
| URL | `/years/2011/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt11-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 21072 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About the Web in 2011

| | |
|--|--|
| URL | `/years/2011/pages/about.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt11-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 2494 |
| Hooks | `data-itt-action-status`, `data-itt-real-save`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2011

| | |
|--|--|
| URL | `/years/2011/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt11-air`, `itt11-circles`, `itt11-ghost`, `itt11-hang`, `itt11-icloud`, `itt11-ie9`, `itt11-ig`, `itt11-ipad2-lx`, `itt11-li`, `itt11-pin-lx`, `itt11-qwik`, `itt11-reblog`, `itt11-siri`, `itt11-snap`, `itt11-spot-us`, `itt11-t140`, `itt11-timeline-lx`, `itt11-yt` |
| Primary write | `itt11-air` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 6081 |
| Hooks | `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2011 — UX flow map

| | |
|--|--|
| URL | `/years/2011/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | `itt11-air`, `itt11-circles`, `itt11-ghost`, `itt11-hang`, `itt11-icloud`, `itt11-ie9`, `itt11-ig`, `itt11-ipad2-lx`, `itt11-li`, `itt11-pin-lx`, `itt11-qwik`, `itt11-reblog`, `itt11-siri`, `itt11-snap`, `itt11-spot-us`, `itt11-t140`, `itt11-timeline-lx`, `itt11-yt` |
| Primary write | `itt11-air` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 6196 |
| Hooks | `data-itt-2x-map`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What's New — 2011

| | |
|--|--|
| URL | `/years/2011/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt11-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 945 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/airbnb/index.html` — Airbnb — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/airbnb/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-air`, `itt11-airbnb` |
| Primary write | `itt11-air` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3071 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-ab11-go`, `data-ab11-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.07 `sites/facebook/index.html` — Facebook Timeline — 2011

| | |
|--|--|
| URL | `/years/2011/sites/facebook/index.html` |
| Machine | **leftover-official** |
| Keys in file | `itt11-timeline`, `itt11-timeline-lx` |
| Primary write | `itt11-timeline` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4139 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap`, `data-tl11-req` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.08 `sites/googleplus/hangouts.html` — Hangouts — 2011

| | |
|--|--|
| URL | `/years/2011/sites/googleplus/hangouts.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-hang` |
| Primary write | `itt11-hang` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2556 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.09 `sites/googleplus/index.html` — Google+ — 2011

| | |
|--|--|
| URL | `/years/2011/sites/googleplus/index.html` |
| Machine | **year-verb** |
| Keys in file | `itt11-circles`, `itt11-gplus` |
| Primary write | `itt11-circles` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3602 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-gp11-hangout`, `data-gp11-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-gp11-hangout] with no [data-gp11-circle] and no people
2. One person only (need two)
3. Ticks missing if [data-gp11-req] present

**Trap (never writes):** Snap Stories · IG Android composer

**Complete:** Name a circle + pick two [data-gp11-person] + [data-gp11-hangout] + tick [data-gp11-req]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.10 `sites/icloud/index.html` — iCloud — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/icloud/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt11-icloud`, `itt11-pop-icloud` |
| Primary write | `itt11-icloud` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2964 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.11 `sites/ie9/index.html` — IE9 leftover — 2011

| | |
|--|--|
| URL | `/years/2011/sites/ie9/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-ie9` |
| Primary write | `itt11-ie9` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2754 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.12 `sites/instagram/index.html` — Instagram iOS — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/instagram/index.html` |
| Machine | **leftover-official** |
| Keys in file | `itt11-ig` |
| Primary write | `itt11-ig` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3894 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-ig11-req`, `data-itt-3x-also`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.13 `sites/ipad/index.html` — iPad 2 — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/ipad/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-ipad2`, `itt11-ipad2-lx` |
| Primary write | `itt11-ipad2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3088 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pd11-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.14 `sites/iphone/index.html` — iPhone 4S / Siri — 2011

| | |
|--|--|
| URL | `/years/2011/sites/iphone/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-siri` |
| Primary write | `itt11-siri` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3136 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-sr11-ask`, `data-sr11-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.15 `sites/linkedin/index.html` — LinkedIn — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/linkedin/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt11-li`, `itt11-pop-linkedin` |
| Primary write | `itt11-li` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2977 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.16 `sites/netflix/index.html` — Qwikster leftover — 2011

| | |
|--|--|
| URL | `/years/2011/sites/netflix/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-qwik` |
| Primary write | `itt11-qwik` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2767 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.17 `sites/pinterest/index.html` — Pinterest — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/pinterest/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt11-pin-lx`, `itt11-pop-pinterest` |
| Primary write | `itt11-pin-lx` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2982 |
| Hooks | `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.18 `sites/playable/extra-a.html` — Circles leftover — 2011

| | |
|--|--|
| URL | `/years/2011/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | `itt11-game-circles` |
| Primary write | `itt11-game-circles` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 1884 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-xa-go`, `data-xa-trap` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.19 `sites/playable/extra-b.html` — G+ won trap — 2011

| | |
|--|--|
| URL | `/years/2011/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | `itt11-game-won` |
| Primary write | `itt11-game-won` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 1879 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-xb-go`, `data-xb-trap` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.20 `sites/playable/extra-c.html` — Rush Lane — 2011

| | |
|--|--|
| URL | `/years/2011/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt11-game-rushlane` |
| Primary write | `itt11-game-rushlane` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3256 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.21 `sites/playable/extra-d.html` — Grip Hold — 2011

| | |
|--|--|
| URL | `/years/2011/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt11-game-griphold` |
| Primary write | `itt11-game-griphold` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3293 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-hold-ms`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.22 `sites/playable/extra-e.html` — Spent Week — 2011

| | |
|--|--|
| URL | `/years/2011/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt11-game-spentweek` |
| Primary write | `itt11-game-spentweek` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3333 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.23 `sites/playable/famous.html` — Famous games · 2011

| | |
|--|--|
| URL | `/years/2011/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt11-game-memory`, `itt11-game-snake` |
| Primary write | `itt11-game-memory` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3700 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.24 `sites/playable/game.html` — Letter Swap — 2011

| | |
|--|--|
| URL | `/years/2011/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt11-game-letterswap` |
| Primary write | `itt11-game-letterswap` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 2422 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-peg-trap` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.25 `sites/playable/index.html` — 2011 playable

| | |
|--|--|
| URL | `/years/2011/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | _none harvested_ |
| Primary write | `itt11-MISSING` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 2408 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-itt-year-extras`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.26 `sites/qwikster/index.html` — Qwikster — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/qwikster/index.html` |
| Machine | **leftover-official** |
| Keys in file | `itt11-qwikster` |
| Primary write | `itt11-qwikster` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3046 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap`, `data-qw11-req` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.27 `sites/snapchat/index.html` — Snapchat — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/snapchat/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt11-pop3-snapchat`, `itt11-snap` |
| Primary write | `itt11-pop3-snapchat` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4238 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-sc11-trap` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.28 `sites/snapghost/index.html` — Snap leftover ghost — 2011

| | |
|--|--|
| URL | `/years/2011/sites/snapghost/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-ghost` |
| Primary write | `itt11-ghost` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2756 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.29 `sites/spotify/index.html` — Spotify US — 2011

| | |
|--|--|
| URL | `/years/2011/sites/spotify/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-spot-us`, `itt11-spotify` |
| Primary write | `itt11-spot-us` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3342 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-sp11-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.30 `sites/tumblr/index.html` — Tumblr — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/tumblr/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt11-pop3-tumblr`, `itt11-reblog` |
| Primary write | `itt11-pop3-tumblr` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4257 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-tb11-trap` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.31 `sites/twitter/index.html` — Twitter — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/twitter/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt11-t140`, `itt11-tweets` |
| Primary write | `itt11-t140` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2924 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-tw11-post`, `data-tw11-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.32 `sites/youtube/index.html` — YouTube — 2011 leftover

| | |
|--|--|
| URL | `/years/2011/sites/youtube/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt11-pop3-youtube`, `itt11-yt` |
| Primary write | `itt11-pop3-youtube` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4364 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-yt11-trap` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2011"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Rush Lane** `itt11-game-rushlane` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Grip Hold** `itt11-game-griphold` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **Spent Week** `itt11-game-spentweek` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2011`)

1. Recite star `itt11-gplus` · guided 6 · bans.
2. Count HTML. Now **32**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2011 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt11-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

