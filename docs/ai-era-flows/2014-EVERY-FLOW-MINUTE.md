# 2014 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2014` or `densify 2014`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | WhatsApp Install (not chat) |
| Chip | ★ One-thing · WhatsApp Install REAL |
| href | `sites/whatsapp/index.html` |
| Key | `itt14-wa-install` |
| Shell | Win7/8 + Chrome habit |
| Thesis | $19B is the install, not the chat. Heartbleed and Ice Bucket leftover. |
| Scale | ILS June 968,882,453 · first billion-class · dual-cite |
| Bans | WhatsApp E2E as 2014 default, Stories, GDPR |
| Disk now | **29 HTML** · **15 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt14-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2014/sites/whatsapp/index.html
      =  pages/home.html data-ott-one-thing="2014"
      =  flowTrails["2014"][0].href
key   =  itt14-wa-install
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt13-*` / `itt15-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2014/sites/whatsapp/index.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt14-wa-install")` | — |
| Incomplete 1 | Open chat.html without install | **no** |
| Incomplete 2 | Click [data-wa14-install] without [data-wa14-deal] honesty | **no** |
| Incomplete 3 | Messenger trap [data-wa14-messenger] | **no** |
| Trap | Facebook Messenger as WhatsApp · E2E as 2014 default | **no** |
| Complete | Ack $19B / 450M deal · [data-wa14-install] | **`itt14-wa-install`** |
| Payload | real:true · year:"2014" · installed:true | must include `real` + `year:"2014"` |
| Next | [data-next-flow] → WhatsApp chat leftover | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2014 | `pages/about.html` | HTTP 200 · dual scale · bans |
| 2 | WhatsApp Install | `sites/whatsapp/index.html` | HTTP 200 · $19B install |
| 3 | Chat leftover | `sites/whatsapp/chat.html` | HTTP 200 · not the star |
| 4 | Heartbleed | `sites/heartbleed/index.html` | HTTP 200 · leftover |
| 5 | Ice Bucket | `sites/icebucket/index.html` | HTTP 200 · leftover |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2014 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | WhatsApp | `sites/whatsapp/index.html` | `itt14-wa-install` | use Gold minute — not dest-field | Gold complete | `sites/whatsapp/chat.html` WhatsApp chat |
| 2 | Chat | `sites/whatsapp/chat.html` | `itt14-wa-chat` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/heartbleed/index.html` Heartbleed |
| 3 | Heartbleed | `sites/heartbleed/index.html` | `itt14-heartbleed` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/icebucket/index.html` Ice Bucket |
| 4 | Ice Bucket | `sites/icebucket/index.html` | `itt14-icebucket` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/iphone/index.html` iPhone 6 |
| 5 | iPhone 6 | `sites/iphone/index.html` | `itt14-iphone6` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/iphone/pay.html` Apple Pay |
| 6 | Apple Pay | `sites/iphone/pay.html` | `itt14-applepay` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/material/index.html` Material |
| 7 | Material | `sites/material/index.html` | `itt14-material` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/slack/index.html` Slack |
| 8 | Slack | `sites/slack/index.html` | `itt14-slack` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/twitch/index.html` Twitch |
| 9 | Twitch | `sites/twitch/index.html` | `itt14-twitch` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/whatsapp/index.html` WhatsApp |
| 10 | Tile Fold | `sites/playable/game.html` | `itt14-game-tilefold` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/whatsapp/index.html` WhatsApp |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/snapchat/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-pop-snapchat` |
| `sites/instagram/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-pop-instagram` |
| `sites/uber/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-pop-uber` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/heartbleed/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-heartbleed` |
| `sites/icebucket/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-icebucket` |
| `sites/slack/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-slack` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/youtube/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-pop3-youtube` |
| `sites/wikipedia/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-pop3-wikipedia` |
| `sites/facebook/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt14-pop3-facebook` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/alipay/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop4-alipay` |
| `sites/giphy/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop4-giphy` |
| `sites/swarm/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop4-swarm` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/whatsapp/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop5-whatsapp` |
| `sites/iphone6/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop5-iphone6` |
| `sites/material/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop5-material` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/twitch/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop6-twitch` |
| `sites/applepay/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop6-applepay` |
| `sites/slack/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt14-pop6-slack` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

| F | key | path | kind | field | Incomplete | Complete | Next |
|---|-----|------|------|-------|------------|----------|------|
| F1 | `itt14-tweets` | `/years/2014/sites/twitter/index.html` | new | tweet | empty field / 0 ticks / trap | pick + honesty + go | `../icebucket/index.html` |
| F2 | `itt14-fb` | `/years/2014/sites/facebook/index.html` | new | — | empty field / 0 ticks / trap | pick + honesty + go | `../youtube/index.html` |
| F3 | `itt14-youtube` | `/years/2014/sites/youtube/index.html` | new | — | empty field / 0 ticks / trap | pick + honesty + go | `../whatsapp/index.html` |
| F4 | `itt14-icebucket` | `/years/2014/sites/icebucket/index.html` | reuse | — | empty field / 0 ticks / trap | pick + honesty + go | `../whatsapp/index.html` |
| F5 | `itt14-wa` | `/years/2014/sites/whatsapp/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |

Plaque-only dests that the matrix expects must grow `data-itt-popular-save` **or** a leftover-pop machine. Never run unscoped `build-popular-flows.py`.

---

## 7. Leftover-official machines (e2e matrix)

No leftover-official dests stamped for 2014. Official 10 still must write via gold / 4x / leftover-pop. Adding a `[data-lo-panel]` is allowed on densify if it does not fight the star key.

---

## 8. EVERY dest on disk (minute)

29 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 8, 'leftover-pop': 10, 'dest-field-4x': 2, 'playable': 8, 'year-verb': 1}

### D.01 `index.html` — Internet Explorer residual — 2014

| | |
|--|--|
| URL | `/years/2014/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt14-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 21016 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About the Web in 2014

| | |
|--|--|
| URL | `/years/2014/pages/about.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt14-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 3237 |
| Hooks | `data-itt-action-status`, `data-itt-real-save`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2014

| | |
|--|--|
| URL | `/years/2014/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt14-hb-lx`, `itt14-ice-lx`, `itt14-ip-lx`, `itt14-sl-lx`, `itt14-tw-lx`, `itt14-wa-lx` |
| Primary write | `itt14-hb-lx` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 4941 |
| Hooks | `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2014 — UX flow map

| | |
|--|--|
| URL | `/years/2014/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | `itt14-applepay`, `itt14-game-tilefold`, `itt14-hb-lx`, `itt14-heartbleed`, `itt14-ice-lx`, `itt14-icebucket`, `itt14-ip-lx`, `itt14-iphone6`, `itt14-material`, `itt14-sl-lx`, `itt14-slack`, `itt14-tw-lx`, `itt14-twitch`, `itt14-wa-chat`, `itt14-wa-install`, `itt14-wa-lx` |
| Primary write | `itt14-applepay` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4017 |
| Hooks | `data-itt-2x-map`, `data-itt-3x-also`, `data-itt-ten-flows`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What’s new — 2014

| | |
|--|--|
| URL | `/years/2014/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt14-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 674 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/facebook/index.html` — Facebook leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/facebook/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop3-facebook` |
| Primary write | `itt14-pop3-facebook` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3605 |
| Hooks | `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.07 `sites/heartbleed/index.html` — Heartbleed leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/heartbleed/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-hb-lx`, `itt14-heartbleed`, `itt14-pop-heartbleed` |
| Primary write | `itt14-hb-lx` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4225 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-hb14-req`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.08 `sites/icebucket/index.html` — Ice Bucket leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/icebucket/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-ice-lx`, `itt14-icebucket`, `itt14-pop-icebucket` |
| Primary write | `itt14-ice-lx` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 4907 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.09 `sites/instagram/index.html` — Instagram leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/instagram/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop-instagram` |
| Primary write | `itt14-pop-instagram` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2577 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.10 `sites/iphone/index.html` — iPhone 6 leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/iphone/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt14-ip-lx`, `itt14-iphone6` |
| Primary write | `itt14-ip-lx` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3374 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-ip14-req`, `data-ip14-save`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.11 `sites/iphone/pay.html` — Apple Pay leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/iphone/pay.html` |
| Machine | **thin-door** |
| Keys in file | `itt14-applepay` |
| Primary write | `itt14-applepay` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2277 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pay14-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.12 `sites/material/index.html` — Material leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/material/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt14-material` |
| Primary write | `itt14-material` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2414 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-mat14-req`, `data-mat14-save` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.13 `sites/playable/extra-a.html` — 2014 extra-a leftover

| | |
|--|--|
| URL | `/years/2014/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | _none harvested_ |
| Primary write | `itt14-MISSING` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 1711 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.14 `sites/playable/extra-b.html` — 2014 extra-b leftover

| | |
|--|--|
| URL | `/years/2014/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | _none harvested_ |
| Primary write | `itt14-MISSING` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 1715 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.15 `sites/playable/extra-c.html` — Slack Chan — 2014

| | |
|--|--|
| URL | `/years/2014/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt14-game-slackchan` |
| Primary write | `itt14-game-slackchan` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3353 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.16 `sites/playable/extra-d.html` — Ice Pour — 2014

| | |
|--|--|
| URL | `/years/2014/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt14-game-icepour` |
| Primary write | `itt14-game-icepour` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3400 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-prompt`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.17 `sites/playable/extra-e.html` — Rotate TLS — 2014

| | |
|--|--|
| URL | `/years/2014/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt14-game-rotatels` |
| Primary write | `itt14-game-rotatels` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3392 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.18 `sites/playable/famous.html` — Famous games · 2014

| | |
|--|--|
| URL | `/years/2014/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt14-game-memory`, `itt14-game-snake` |
| Primary write | `itt14-game-memory` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3733 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.19 `sites/playable/game.html` — Tile Fold — 2014

| | |
|--|--|
| URL | `/years/2014/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt14-game-tilefold` |
| Primary write | `itt14-game-tilefold` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 2497 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-tile-trap` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.20 `sites/playable/index.html` — Playable — 2014

| | |
|--|--|
| URL | `/years/2014/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | _none harvested_ |
| Primary write | `itt14-MISSING` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 2344 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.21 `sites/slack/index.html` — Slack leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/slack/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop-slack`, `itt14-sl-lx`, `itt14-slack` |
| Primary write | `itt14-pop-slack` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3903 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-sl14-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.22 `sites/snapchat/index.html` — Snapchat leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/snapchat/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop-snapchat` |
| Primary write | `itt14-pop-snapchat` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2632 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.23 `sites/twitch/index.html` — Twitch leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/twitch/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt14-tw-lx`, `itt14-twitch` |
| Primary write | `itt14-tw-lx` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3005 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-tw14-google`, `data-tw14-req`, `data-tw14-save` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.24 `sites/twitter/index.html` — Twitter leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/twitter/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop-twitter` |
| Primary write | `itt14-pop-twitter` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3766 |
| Hooks | `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-popular-field`, `data-popular-req`, `data-require-field`, `data-require-field-min` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.25 `sites/uber/index.html` — Uber leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/uber/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop-uber` |
| Primary write | `itt14-pop-uber` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2562 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.26 `sites/whatsapp/chat.html` — WhatsApp chat leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/whatsapp/chat.html` |
| Machine | **thin-door** |
| Keys in file | `itt14-wa-chat` |
| Primary write | `itt14-wa-chat` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2327 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-wa14-req`, `data-wa14-send` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.27 `sites/whatsapp/index.html` — WhatsApp Install — 2014

| | |
|--|--|
| URL | `/years/2014/sites/whatsapp/index.html` |
| Machine | **year-verb** |
| Keys in file | `itt14-wa-install`, `itt14-wa-lx` |
| Primary write | `itt14-wa-install` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3285 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-wa14-install` |

**Incomplete (never writes)**

1. Open chat.html without install
2. Click [data-wa14-install] without [data-wa14-deal] honesty
3. Messenger trap [data-wa14-messenger]

**Trap (never writes):** Facebook Messenger as WhatsApp · E2E as 2014 default

**Complete:** Ack $19B / 450M deal · [data-wa14-install]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.28 `sites/wikipedia/index.html` — Wikipedia leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/wikipedia/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop3-wikipedia` |
| Primary write | `itt14-pop3-wikipedia` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2608 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.29 `sites/youtube/index.html` — YouTube leftover — 2014

| | |
|--|--|
| URL | `/years/2014/sites/youtube/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt14-pop3-youtube` |
| Primary write | `itt14-pop3-youtube` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3618 |
| Hooks | `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2014"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Slack Chan** `itt14-game-slackchan` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Ice Pour** `itt14-game-icepour` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **Rotate TLS** `itt14-game-rotatels` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2014`)

1. Recite star `itt14-wa-install` · guided 6 · bans.
2. Count HTML. Now **29**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2014 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt14-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

