# 2021 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2021` or `densify 2021`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | Ask App Not to Track |
| Chip | ★ One-thing · Ask App Not to Track REAL |
| href | `sites/att/index.html` |
| Key | `itt21-att` |
| Shell | Win10 residual + Chrome habit |
| Thesis | Allow is the trap. Ask is the save. Copilot is a waitlist. |
| Scale | No ILS June · Netcraft January + ITU |
| Bans | ChatGPT, Plus, GPT-4, Windows 11 as January shell |
| Disk now | **27 HTML** · **15 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt21-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2021/sites/att/index.html
      =  pages/home.html data-ott-one-thing="2021"
      =  flowTrails["2021"][0].href
key   =  itt21-att
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt20-*` / `itt22-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2021/sites/att/index.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt21-att")` | — |
| Incomplete 1 | [data-att-allow] — trap | **no** |
| Incomplete 2 | Never tap Ask | **no** |
| Incomplete 3 | 4x leftover empty go (not star) | **no** |
| Trap | [data-att-allow] · treat ATT as optional banner | **no** |
| Complete | [data-att-ask] (Ask App Not to Track) | **`itt21-att`** |
| Payload | real:true · year:"2021" · ask:true · allow:false | must include `real` + `year:"2021"` |
| Next | [data-next-flow] → Signal leftover | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2021 | `pages/about.html` | HTTP 200 · ATT · bans |
| 2 | ATT Ask | `sites/att/index.html` | HTTP 200 · Allow is the trap |
| 3 | Signal leftover | `sites/signal/index.html` | HTTP 200 · Jan leftover |
| 4 | Copilot waitlist | `sites/copilot/index.html` | HTTP 200 · preview leftover |
| 5 | Meta rename | `sites/meta/index.html` | HTTP 200 · 28 Oct leftover |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2021 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | ATT Ask | `sites/att/index.html` | `itt21-att` | use Gold minute — not dest-field | Gold complete | `sites/signal/index.html` Signal leftover |
| 2 | Signal leftover | `sites/signal/index.html` | `itt21-signal` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/copilot/index.html` Copilot waitlist |
| 3 | Copilot waitlist | `sites/copilot/index.html` | `itt21-copilot` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/meta/index.html` Meta rename |
| 4 | Meta rename | `sites/meta/index.html` | `itt21-meta` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/windows11/index.html` Windows 11 |
| 5 | Windows 11 leftover | `sites/windows11/index.html` | `itt21-win11` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/flash/index.html` Flash brick |
| 6 | Flash brick | `sites/flash/index.html` | `itt21-flash-brick` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/chrome/index.html` Chrome habit |
| 7 | Chrome habit | `sites/chrome/index.html` | `itt21-chrome` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/windows10/index.html` Windows 10 |
| 8 | Windows 10 residual | `sites/windows10/index.html` | `itt21-win10` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/facebook/index.html` Facebook leftover |
| 9 | Facebook leftover | `sites/facebook/index.html` | `itt21-pop-facebook` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/playable/game.html` Five Letter |
| 10 | Five Letter | `sites/playable/game.html` | `itt21-game-five` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/att/index.html` ATT |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/youtube/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-pop-youtube` |
| `sites/wikipedia/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-pop-wikipedia` |
| `sites/facebook/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-pop-facebook` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/windows11/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-win11` |
| `sites/flash/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-flash-brick` |
| `sites/chrome/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-chrome` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/clubhouse/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-pop3-clubhouse` |
| `sites/nft/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-pop3-nft` |
| `sites/squid/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt21-pop3-squid` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/signal/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop4-signal` |
| `sites/copilot/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop4-copilot` |
| `sites/meta/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop4-meta` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/att/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop5-att` |
| `sites/win11/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop5-win11` |
| `sites/wordle-seed/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop5-wordle-seed` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/opensea/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop6-opensea` |
| `sites/robinhood/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop6-robinhood` |
| `sites/discord/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt21-pop6-discord` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

No `popular-flows.matrix.json` rows for 2021. Do **not** invent F-keys. Official 10 + leftover 27 cover the 3× count.

---

## 7. Leftover-official machines (e2e matrix)

No leftover-official dests stamped for 2021. Official 10 still must write via gold / 4x / leftover-pop. Adding a `[data-lo-panel]` is allowed on densify if it does not fight the star key.

---

## 8. EVERY dest on disk (minute)

27 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 5, 'year-verb': 1, 'leftover-pop': 9, 'dest-field-4x': 4, 'playable': 8}

### D.01 `index.html` — Chrome habit — 2021

| | |
|--|--|
| URL | `/years/2021/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt21-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 20895 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About 2021 — dual scale · bans

| | |
|--|--|
| URL | `/years/2021/pages/about.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt21-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 4605 |
| Hooks | `data-itt-real-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2021

| | |
|--|--|
| URL | `/years/2021/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt21-att-lx`, `itt21-cab-2`, `itt21-ch-2`, `itt21-ch-lx`, `itt21-chouse-2`, `itt21-chouse-lx`, `itt21-cop-2`, `itt21-cop-lx`, `itt21-fam-2`, `itt21-famous-lx`, `itt21-fb-2`, `itt21-fb-lx`, `itt21-flash-2`, `itt21-flash-lx`, `itt21-game-2`, `itt21-game-lx`, `itt21-meta-2`, `itt21-meta-lx`, `itt21-nft-2`, `itt21-nft-lx`, `itt21-sig-2`, `itt21-sig-lx`, `itt21-squid-2`, `itt21-squid-lx`, `itt21-w10-2`, `itt21-w10-lx`, `itt21-w11-2`, `itt21-w11-lx`, `itt21-wiki-2`, `itt21-wiki-lx`, `itt21-xa-2`, `itt21-xa-lx`, `itt21-xb-2`, `itt21-xb-lx`, `itt21-yt-2`, `itt21-yt-lx` |
| Primary write | `itt21-att-lx` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 9534 |
| Hooks | `data-itt-mass`, `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-tour`, `data-itt-year`, `data-itt-year-extras` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2021 flow map

| | |
|--|--|
| URL | `/years/2021/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | `itt21-att`, `itt21-att-lx`, `itt21-cab-2`, `itt21-ch-2`, `itt21-ch-lx`, `itt21-chouse-2`, `itt21-chouse-lx`, `itt21-chrome`, `itt21-cop-2`, `itt21-cop-lx`, `itt21-copilot`, `itt21-fam-2`, `itt21-famous-lx`, `itt21-fb-2`, `itt21-fb-lx`, `itt21-flash-2`, `itt21-flash-brick`, `itt21-flash-lx`, `itt21-game-2`, `itt21-game-five`, `itt21-game-lx`, `itt21-meta`, `itt21-meta-2`, `itt21-meta-lx`, `itt21-nft-2`, `itt21-nft-lx`, `itt21-pop-facebook`, `itt21-pop-wikipedia`, `itt21-pop-youtube`, `itt21-pop3-clubhouse`, `itt21-pop3-nft`, `itt21-pop3-squid`, `itt21-sig-2`, `itt21-sig-lx`, `itt21-signal`, `itt21-squid-2`, `itt21-squid-lx`, `itt21-w10-2`, `itt21-w10-lx`, `itt21-w11-2`, `itt21-w11-lx`, `itt21-wiki-2`, `itt21-wiki-lx`, `itt21-win10`, `itt21-win11`, `itt21-xa-2`, `itt21-xa-lx`, `itt21-xb-2`, `itt21-xb-lx`, `itt21-yt-2`, `itt21-yt-lx` |
| Primary write | `itt21-att` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 12756 |
| Hooks | `data-itt-2x-map`, `data-itt-3x-also`, `data-itt-flow-map`, `data-itt-pop3x`, `data-itt-ten-flows`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What's new — 2021

| | |
|--|--|
| URL | `/years/2021/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt21-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 1434 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/att/index.html` — Ask App Not to Track — iOS 14.5

| | |
|--|--|
| URL | `/years/2021/sites/att/index.html` |
| Machine | **year-verb** |
| Keys in file | `itt21-att`, `itt21-att-lx` |
| Primary write | `itt21-att` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3440 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-att-allow`, `data-att-ask`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. [data-att-allow] — trap
2. Never tap Ask
3. 4x leftover empty go (not star)

**Trap (never writes):** [data-att-allow] · treat ATT as optional banner

**Complete:** [data-att-ask] (Ask App Not to Track)

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.07 `sites/chrome/index.html` — Chrome habit — 2021 leftover

| | |
|--|--|
| URL | `/years/2021/sites/chrome/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-ch-2`, `itt21-ch-lx`, `itt21-chrome`, `itt21-pop-chrome` |
| Primary write | `itt21-ch-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5010 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.08 `sites/clubhouse/index.html` — Clubhouse leftover — 2021

| | |
|--|--|
| URL | `/years/2021/sites/clubhouse/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-chouse-2`, `itt21-chouse-lx`, `itt21-pop3-clubhouse` |
| Primary write | `itt21-chouse-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4384 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-ch-field`, `data-ch-go`, `data-ch-req`, `data-ch-trap`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.09 `sites/copilot/index.html` — GitHub Copilot technical preview

| | |
|--|--|
| URL | `/years/2021/sites/copilot/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt21-cop-2`, `itt21-cop-lx`, `itt21-copilot` |
| Primary write | `itt21-cop-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4425 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-copilot-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.10 `sites/facebook/index.html` — Facebook leftover — 2021

| | |
|--|--|
| URL | `/years/2021/sites/facebook/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-fb-2`, `itt21-fb-lx`, `itt21-pop-facebook` |
| Primary write | `itt21-fb-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4285 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.11 `sites/flash/index.html` — Flash brick — 12 Jan 2021

| | |
|--|--|
| URL | `/years/2021/sites/flash/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-flash-2`, `itt21-flash-brick`, `itt21-flash-lx`, `itt21-pop-flash` |
| Primary write | `itt21-flash-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5205 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-flash-req`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.12 `sites/meta/index.html` — Meta rename — 28 Oct 2021

| | |
|--|--|
| URL | `/years/2021/sites/meta/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt21-meta`, `itt21-meta-2`, `itt21-meta-lx` |
| Primary write | `itt21-meta` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4260 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-meta-req`, `data-meta-save` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.13 `sites/nft/index.html` — NFT literacy — 2021

| | |
|--|--|
| URL | `/years/2021/sites/nft/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-nft-2`, `itt21-nft-lx`, `itt21-pop3-nft` |
| Primary write | `itt21-nft-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4347 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-nft-field`, `data-nft-go`, `data-nft-req`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.14 `sites/playable/extra-a.html` — Ask drill — 2021

| | |
|--|--|
| URL | `/years/2021/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | `itt21-extra-a`, `itt21-xa-2`, `itt21-xa-lx` |
| Primary write | `itt21-extra-a` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 4097 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.15 `sites/playable/extra-b.html` — 90 users — 2021

| | |
|--|--|
| URL | `/years/2021/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | `itt21-extra-b`, `itt21-xb-2`, `itt21-xb-lx` |
| Primary write | `itt21-extra-b` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3992 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.16 `sites/playable/extra-c.html` — Sig Handle — 2021

| | |
|--|--|
| URL | `/years/2021/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt21-game-sighandle` |
| Primary write | `itt21-game-sighandle` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3370 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.17 `sites/playable/extra-d.html` — Wait Copilot — 2021

| | |
|--|--|
| URL | `/years/2021/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt21-game-waitcop` |
| Primary write | `itt21-game-waitcop` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3412 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.18 `sites/playable/extra-e.html` — Meta Note — 2021

| | |
|--|--|
| URL | `/years/2021/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt21-game-metanote` |
| Primary write | `itt21-game-metanote` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3399 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.19 `sites/playable/famous.html` — Famous games · 2021

| | |
|--|--|
| URL | `/years/2021/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt21-fam-2`, `itt21-famous-lx`, `itt21-game-breakout`, `itt21-game-memory` |
| Primary write | `itt21-fam-2` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 5528 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.20 `sites/playable/game.html` — Five Letter — 2021

| | |
|--|--|
| URL | `/years/2021/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt21-game-2`, `itt21-game-five`, `itt21-game-lx` |
| Primary write | `itt21-game-2` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 4975 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-five-field`, `data-five-req`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.21 `sites/playable/index.html` — 2021 playables

| | |
|--|--|
| URL | `/years/2021/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | `itt21-cab-2` |
| Primary write | `itt21-cab-2` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3561 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-itt-year-extras`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.22 `sites/signal/index.html` — Signal leftover — Jan 2021

| | |
|--|--|
| URL | `/years/2021/sites/signal/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt21-sig-2`, `itt21-sig-lx`, `itt21-signal` |
| Primary write | `itt21-sig-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4335 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-sig-req`, `data-sig-trap` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.23 `sites/squid/index.html` — Squid Game print leftover

| | |
|--|--|
| URL | `/years/2021/sites/squid/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-pop3-squid`, `itt21-squid-2`, `itt21-squid-lx` |
| Primary write | `itt21-pop3-squid` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4435 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-sq-field`, `data-sq-go`, `data-sq-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.24 `sites/wikipedia/index.html` — Wikipedia — 2021 leftover

| | |
|--|--|
| URL | `/years/2021/sites/wikipedia/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-pop-wikipedia`, `itt21-wiki-2`, `itt21-wiki-lx` |
| Primary write | `itt21-pop-wikipedia` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4261 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.25 `sites/windows10/index.html` — Windows 10 residual — 2021

| | |
|--|--|
| URL | `/years/2021/sites/windows10/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt21-w10-2`, `itt21-w10-lx`, `itt21-win10` |
| Primary write | `itt21-w10-2` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3962 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-w10-req`, `data-w10-save` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.26 `sites/windows11/index.html` — Windows 11 leftover

| | |
|--|--|
| URL | `/years/2021/sites/windows11/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-pop-windows11`, `itt21-w11-2`, `itt21-w11-lx`, `itt21-win11` |
| Primary write | `itt21-pop-windows11` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5213 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-w11-gone`, `data-w11-install`, `data-w11-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.27 `sites/youtube/index.html` — YouTube — 2021 leftover

| | |
|--|--|
| URL | `/years/2021/sites/youtube/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt21-pop-youtube`, `itt21-yt-2`, `itt21-yt-lx` |
| Primary write | `itt21-pop-youtube` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4565 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2021"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Sig Handle** `itt21-game-sighandle` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Wait Copilot** `itt21-game-waitcop` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **Meta Note** `itt21-game-metanote` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2021`)

1. Recite star `itt21-att` · guided 6 · bans.
2. Count HTML. Now **27**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2021 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt21-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

