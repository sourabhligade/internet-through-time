# 2017 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2017` or `densify 2017`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | iPhone X / Face ID unlock |
| Chip | ★ One-thing · iPhone X / Face ID REAL |
| href | `sites/iphone/x.html` |
| Key | `itt17-faceid` |
| Shell | Win10 + Chrome habit |
| Thesis | Face is the password. Fortnite BR and 280 are leftovers. |
| Scale | ILS June still live |
| Bans | GDPR as 2017 default, TikTok US mass, no-home as optional |
| Disk now | **29 HTML** · **16 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt17-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2017/sites/iphone/x.html
      =  pages/home.html data-ott-one-thing="2017"
      =  flowTrails["2017"][0].href
key   =  itt17-faceid
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt16-*` / `itt18-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2017/sites/iphone/x.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt17-faceid")` | — |
| Incomplete 1 | Click [data-faceid-unlock] with 0 [data-faceid-req] | **no** |
| Incomplete 2 | Look only, no unlock | **no** |
| Incomplete 3 | Treat Touch ID as Face ID | **no** |
| Trap | Touch ID as 2017 gold · home button still there | **no** |
| Complete | Tick look + no-home + not-XS [data-faceid-req] · [data-faceid-look] · [data-faceid-unlock] | **`itt17-faceid`** |
| Payload | real:true · year:"2017" · faceid:true · noHome:true | must include `real` + `year:"2017"` |
| Next | [data-next-flow] → Fortnite BR | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2017 | `pages/about.html` | HTTP 200 · dual scale · bans |
| 2 | Face ID | `sites/iphone/x.html` | HTTP 200 · look then unlock |
| 3 | Fortnite BR | `sites/fortnite/index.html` | HTTP 200 · leftover |
| 4 | Twitter 280 | `sites/twitter/280.html` | HTTP 200 · leftover |
| 5 | Teams GA | `sites/teams/index.html` | HTTP 200 · leftover |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2017 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | Face ID / iPhone X | `sites/iphone/x.html` | `itt17-faceid` | use Gold minute — not dest-field | Gold complete | `sites/fortnite/index.html` Fortnite BR |
| 2 | Fortnite BR | `sites/fortnite/index.html` | `itt17-fortnite` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/twitter/280.html` Twitter 280 |
| 3 | Twitter 280 | `sites/twitter/280.html` | `itt17-twitter-280` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/teams/index.html` Teams GA |
| 4 | Teams GA | `sites/teams/index.html` | `itt17-teams` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/vine/gone.html` Vine gone |
| 5 | Vine gone | `sites/vine/gone.html` | `itt17-vine-gone` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/switch/index.html` Switch |
| 6 | Nintendo Switch | `sites/switch/index.html` | `itt17-switch` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/wannacry/index.html` WannaCry |
| 7 | WannaCry | `sites/wannacry/index.html` | `itt17-wannacry` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/musically/index.html` musical.ly |
| 8 | musical.ly | `sites/musically/index.html` | `itt17-musically` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/equifax/index.html` Equifax |
| 9 | Equifax freeze | `sites/equifax/index.html` | `itt17-equifax` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/playable/game.html` Storm Circle |
| 10 | Storm Circle | `sites/playable/game.html` | `itt17-game-stormcircle` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/iphone/x.html` Face ID |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/reddit/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-pop-reddit` |
| `sites/youtube/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-pop-youtube` |
| `sites/amazon/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-pop-amazon` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/snapipo/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-pop-snapipo` |
| `sites/bitcoinath/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-pop-bitcoinath` |
| `sites/echoshow/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-pop-echoshow` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/fortnite/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-fortnite` |
| `sites/teams/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-teams` |
| `sites/switch/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt17-switch` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/wannacry/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop4-wannacry` |
| `sites/equifax/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop4-equifax` |
| `sites/notpetya/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop4-notpetya` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/faceid/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop5-faceid` |
| `sites/twitter280/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop5-twitter280` |
| `sites/musically/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop5-musically` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/bitmoji/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop6-bitmoji` |
| `sites/hqtrivia/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop6-hqtrivia` |
| `sites/pubg/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt17-pop6-pubg` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

| F | key | path | kind | field | Incomplete | Complete | Next |
|---|-----|------|------|-------|------------|----------|------|
| F1 | `itt17-pop-reddit` | `/years/2017/sites/reddit/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F2 | `itt17-pop-youtube` | `/years/2017/sites/youtube/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F3 | `itt17-pop-amazon` | `/years/2017/sites/amazon/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F4 | `itt17-faceid` | `/years/2017/sites/iphone/x.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F5 | `itt17-fortnite` | `/years/2017/sites/fortnite/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |

Plaque-only dests that the matrix expects must grow `data-itt-popular-save` **or** a leftover-pop machine. Never run unscoped `build-popular-flows.py`.

---

## 7. Leftover-official machines (e2e matrix)

No leftover-official dests stamped for 2017. Official 10 still must write via gold / 4x / leftover-pop. Adding a `[data-lo-panel]` is allowed on densify if it does not fight the star key.

---

## 8. EVERY dest on disk (minute)

29 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 5, 'leftover-pop': 9, 'dest-field-4x': 6, 'year-verb': 1, 'playable': 8}

### D.01 `index.html` — Chrome habit — 2017

| | |
|--|--|
| URL | `/years/2017/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt17-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 20887 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About 2017 — dual scale · bans

| | |
|--|--|
| URL | `/years/2017/pages/about.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt17-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 2876 |
| Hooks | `data-itt-real-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2017

| | |
|--|--|
| URL | `/years/2017/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt17-amzn`, `itt17-amzn-2`, `itt17-animoji-lx`, `itt17-btc`, `itt17-btc-2`, `itt17-cab`, `itt17-eq`, `itt17-faceid-2x`, `itt17-famous`, `itt17-fn`, `itt17-fn-2`, `itt17-mly`, `itt17-mly-2`, `itt17-reddit`, `itt17-reddit-2`, `itt17-show`, `itt17-show-2`, `itt17-snap-2`, `itt17-snapipo`, `itt17-storm`, `itt17-sw-2`, `itt17-switch-lx`, `itt17-t280`, `itt17-t280-2`, `itt17-teams-2`, `itt17-teams-lx`, `itt17-vine-gone-lx`, `itt17-wc`, `itt17-wc-2`, `itt17-xa`, `itt17-xb`, `itt17-xc`, `itt17-xd`, `itt17-xe`, `itt17-yt`, `itt17-yt-2` |
| Primary write | `itt17-amzn` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 8895 |
| Hooks | `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-tour`, `data-itt-year`, `data-itt-year-extras` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2017 flow map

| | |
|--|--|
| URL | `/years/2017/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | `itt17-amzn`, `itt17-amzn-2`, `itt17-animoji-lx`, `itt17-btc`, `itt17-btc-2`, `itt17-cab`, `itt17-eq`, `itt17-equifax`, `itt17-faceid`, `itt17-faceid-2x`, `itt17-famous`, `itt17-fn`, `itt17-fn-2`, `itt17-fortnite`, `itt17-game-stormcircle`, `itt17-mly`, `itt17-mly-2`, `itt17-musically`, `itt17-pop-amazon`, `itt17-pop-reddit`, `itt17-pop-youtube`, `itt17-reddit`, `itt17-reddit-2`, `itt17-show`, `itt17-show-2`, `itt17-snap-2`, `itt17-snapipo`, `itt17-storm`, `itt17-sw-2`, `itt17-switch`, `itt17-switch-lx`, `itt17-t280`, `itt17-t280-2`, `itt17-teams`, `itt17-teams-2`, `itt17-teams-lx`, `itt17-twitter-280`, `itt17-vine-gone`, `itt17-vine-gone-lx`, `itt17-wannacry`, `itt17-wc`, `itt17-wc-2`, `itt17-xa`, `itt17-xb`, `itt17-xc`, `itt17-xd`, `itt17-xe`, `itt17-yt`, `itt17-yt-2` |
| Primary write | `itt17-amzn` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 11814 |
| Hooks | `data-itt-2x-map`, `data-itt-3x-also`, `data-itt-flow-map`, `data-itt-pop3x`, `data-itt-ten-flows`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What's new — 2017

| | |
|--|--|
| URL | `/years/2017/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt17-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 1368 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/amazon/index.html` — Amazon — 2017 leftover

| | |
|--|--|
| URL | `/years/2017/sites/amazon/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-amzn`, `itt17-amzn-2`, `itt17-pop-amazon` |
| Primary write | `itt17-amzn` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4821 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.07 `sites/bitcoinath/index.html` — Bitcoin leftover — 2017

| | |
|--|--|
| URL | `/years/2017/sites/bitcoinath/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-btc`, `itt17-btc-2`, `itt17-pop-bitcoinath` |
| Primary write | `itt17-btc` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4258 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.08 `sites/echoshow/index.html` — Echo Show leftover — 2017

| | |
|--|--|
| URL | `/years/2017/sites/echoshow/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-pop-echoshow`, `itt17-show`, `itt17-show-2` |
| Primary write | `itt17-pop-echoshow` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4291 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.09 `sites/equifax/index.html` — Equifax freeze literacy — 7 Sep 2017

| | |
|--|--|
| URL | `/years/2017/sites/equifax/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt17-eq`, `itt17-equifax` |
| Primary write | `itt17-eq` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3288 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-eq-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.10 `sites/fortnite/index.html` — Fortnite Battle Royale — leftover · Sep 26 2017

| | |
|--|--|
| URL | `/years/2017/sites/fortnite/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-fn`, `itt17-fn-2`, `itt17-fortnite`, `itt17-pop3-fortnite` |
| Primary write | `itt17-fn` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5394 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-fn-req`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.11 `sites/iphone/animoji.html` — Animoji leftover — 2017

| | |
|--|--|
| URL | `/years/2017/sites/iphone/animoji.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt17-animoji`, `itt17-animoji-lx` |
| Primary write | `itt17-animoji` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3375 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-animoji-pick`, `data-animoji-send`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.12 `sites/iphone/x.html` — iPhone X · Face ID — 2017

| | |
|--|--|
| URL | `/years/2017/sites/iphone/x.html` |
| Machine | **year-verb** |
| Keys in file | `itt17-faceid`, `itt17-faceid-2x` |
| Primary write | `itt17-faceid` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3797 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-faceid-look`, `data-faceid-req`, `data-faceid-unlock`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-faceid-unlock] with 0 [data-faceid-req]
2. Look only, no unlock
3. Treat Touch ID as Face ID

**Trap (never writes):** Touch ID as 2017 gold · home button still there

**Complete:** Tick look + no-home + not-XS [data-faceid-req] · [data-faceid-look] · [data-faceid-unlock]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.13 `sites/musically/index.html` — musical.ly — 2017 leftover · not TikTok

| | |
|--|--|
| URL | `/years/2017/sites/musically/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt17-mly`, `itt17-mly-2`, `itt17-musically` |
| Primary write | `itt17-mly` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4149 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-ml-caption`, `data-ml-post` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.14 `sites/playable/extra-a.html` — Face tap — 2017

| | |
|--|--|
| URL | `/years/2017/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | `itt17-game-facetap`, `itt17-xa` |
| Primary write | `itt17-game-facetap` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 4882 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.15 `sites/playable/extra-b.html` — Battle-bus tap — 2017

| | |
|--|--|
| URL | `/years/2017/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | `itt17-game-bustap`, `itt17-xb` |
| Primary write | `itt17-game-bustap` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 4939 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.16 `sites/playable/extra-c.html` — Clip Desk — 2017

| | |
|--|--|
| URL | `/years/2017/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt17-game-clipdesk` |
| Primary write | `itt17-game-clipdesk` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3275 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.17 `sites/playable/extra-d.html` — Hint Guess — 2017

| | |
|--|--|
| URL | `/years/2017/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt17-game-hintguess` |
| Primary write | `itt17-game-hintguess` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3403 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-prompt`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.18 `sites/playable/extra-e.html` — Pond Tier — 2017

| | |
|--|--|
| URL | `/years/2017/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt17-game-pondtier` |
| Primary write | `itt17-game-pondtier` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3349 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.19 `sites/playable/famous.html` — Famous games — 2017 leftover

| | |
|--|--|
| URL | `/years/2017/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt17-famous`, `itt17-game-snake`, `itt17-game-tetris` |
| Primary write | `itt17-famous` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 4841 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.20 `sites/playable/game.html` — Storm Circle — 2017

| | |
|--|--|
| URL | `/years/2017/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt17-game-stormcircle`, `itt17-storm` |
| Primary write | `itt17-game-stormcircle` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3820 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.21 `sites/playable/index.html` — 2017 playables

| | |
|--|--|
| URL | `/years/2017/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | `itt17-cab` |
| Primary write | `itt17-cab` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3446 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-itt-year-extras`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.22 `sites/reddit/index.html` — reddit — 2017 leftover

| | |
|--|--|
| URL | `/years/2017/sites/reddit/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-pop-reddit`, `itt17-reddit`, `itt17-reddit-2` |
| Primary write | `itt17-pop-reddit` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4687 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.23 `sites/snapipo/index.html` — Snap IPO leftover — 2017

| | |
|--|--|
| URL | `/years/2017/sites/snapipo/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-pop-snapipo`, `itt17-snap-2`, `itt17-snapipo` |
| Primary write | `itt17-pop-snapipo` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4256 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.24 `sites/switch/index.html` — Nintendo Switch — 3 Mar 2017 · $299.99

| | |
|--|--|
| URL | `/years/2017/sites/switch/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-pop3-switch`, `itt17-sw-2`, `itt17-switch`, `itt17-switch-lx` |
| Primary write | `itt17-pop3-switch` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5268 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-switch-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.25 `sites/teams/index.html` — Microsoft Teams GA — 14 Mar 2017

| | |
|--|--|
| URL | `/years/2017/sites/teams/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-pop3-teams`, `itt17-teams`, `itt17-teams-2`, `itt17-teams-lx` |
| Primary write | `itt17-pop3-teams` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5101 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req`, `data-teams-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.26 `sites/twitter/280.html` — Twitter 280 — 7 Nov 2017

| | |
|--|--|
| URL | `/years/2017/sites/twitter/280.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt17-t280`, `itt17-t280-2`, `itt17-twitter-280` |
| Primary write | `itt17-t280` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4362 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.27 `sites/vine/gone.html` — Vine is gone — 17 Jan 2017

| | |
|--|--|
| URL | `/years/2017/sites/vine/gone.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt17-vine-gone`, `itt17-vine-gone-lx` |
| Primary write | `itt17-vine-gone` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3323 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-vine-gone-ack`, `data-vine-gone-req`, `data-vine-gone-status` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.28 `sites/wannacry/index.html` — WannaCry literacy — 12 May 2017

| | |
|--|--|
| URL | `/years/2017/sites/wannacry/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt17-wannacry`, `itt17-wc`, `itt17-wc-2` |
| Primary write | `itt17-wannacry` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4362 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-wc-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.29 `sites/youtube/index.html` — YouTube — 2017 leftover

| | |
|--|--|
| URL | `/years/2017/sites/youtube/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt17-pop-youtube`, `itt17-yt`, `itt17-yt-2` |
| Primary write | `itt17-pop-youtube` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4838 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2017"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Clip Desk** `itt17-game-clipdesk` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Hint Guess** `itt17-game-hintguess` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **Pond Tier** `itt17-game-pondtier` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2017`)

1. Recite star `itt17-faceid` · guided 6 · bans.
2. Count HTML. Now **29**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2017 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt17-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

