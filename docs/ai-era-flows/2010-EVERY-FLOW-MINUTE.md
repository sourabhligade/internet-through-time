# 2010 — every flow · minute · 3× densify

**Date:** 2026-08-24  
**Parent corpus:** [`../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md`](../2010-2023-AI-ERA-RESEARCH-CORPUS-EVERY-FLOW-MINUTE.md)  
**Status:** implement **only** when you say `implement 2010` or `densify 2010`. Git only if asked.

---

## 0. Lock

| | |
|--|--|
| Star | Instagram iOS filter → share |
| Chip | ★ One-thing · Instagram iOS filter REAL |
| href | `sites/instagram/index.html` |
| Key | `itt10-ig` |
| Shell | Win7 + IE 8 |
| Thesis | Tablet + camera-phone filter arrive; Facebook colonizes the rest of the Web. |
| Scale | ILS June 206,956,723 · Pingdom Dec 255 million · ~2.05B users |
| Bans | Stories, IG Android as default, iPad 2 cameras, Siri, Timeline, Google+, Snapchat, Spotify US |
| Disk now | **56 HTML** · **35 site folders** |
| 3× target | **90–120 HTML** · **45–60 rooms** · **45–60 completable flows** |
| Guided `<ol>` | **exactly 6** |
| Prefix | `itt10-*` only |

**3× vs rest of the museum:** do **not** 3× 2008’s 345 HTML. 3× **this lean door**. Current lean ~15–20 completable flows → **45–60**. Leftover dests 9 → **27**. Dest also-stamps → **100% dests, ≥3 live exits**.

---

## 1. Align (recite before any edit)

```
hero  =  years/2010/sites/instagram/index.html
      =  pages/home.html data-ott-one-thing="2010"
      =  flowTrails["2010"][0].href
key   =  itt10-ig
```

Star dest and key **do not move**. Guided count stays 6. Neighbor prefixes `itt09-*` / `itt11-*` stay empty after every write.

---

## 2. Gold minute (the one-thing)

**URL:** `/years/2010/sites/instagram/index.html`

| Beat | Do | Writes? |
|------|----|---------|
| Wipe | `localStorage.removeItem("itt10-ig")` | — |
| Incomplete 1 | Click [data-ig-share] with no [data-ig-filter] selected | **no** |
| Incomplete 2 | Caption only, no filter, then share | **no** |
| Incomplete 3 | 4x leftover panel empty [data-4x-go] (does not write star key) | **no** |
| Trap | Stories composer · Android share as default · treat as 2012 | **no** |
| Complete | Click a [data-ig-filter] · optional [data-ig-caption] · [data-ig-share] | **`itt10-ig`** |
| Payload | real:true · year:"2010" · filter set · ios:true | must include `real` + `year:"2010"` |
| Next | [data-next-flow][data-next-when-key="itt10-ig"] → iPhone 4 | hidden until key exists |
| Reload | key still present · Next still visible | yes |
| Neighbor | no other year prefix | — |

---

## 3. Guided 6 (count is the test)

| # | Copy | href | Pass |
|--:|------|------|------|
| 1 | About 2010 | `pages/about.html` | HTTP 200 · dual scale · bans |
| 2 | Instagram | `sites/instagram/index.html` | HTTP 200 · filter then share |
| 3 | iPhone 4 | `sites/iphone/index.html` | HTTP 200 · FaceTime Wi-Fi · Antenna |
| 4 | iPad | `sites/ipad/index.html` | HTTP 200 · $499 · 300k day one |
| 5 | Open Graph | `sites/facebook/index.html` | HTTP 200 · Like ×2 |
| 6 | Year flow map | `pages/map.html` | HTTP 200 · map |

`#ott-guided-2010 ol > li` **= 6**. A 7th `<li>` fails the year. Chips / 3× / 5× sit **outside** the `<ol>`.

---

## 4. Official 10 — every stop is a writer

Each `whenKey` in `flow-trails.js` must **write**. Load-only / plaque-only is a hole. n=1 uses the **Gold minute**. Others: leftover-official panel if present, else dest-field until S8 grows a year-true verb.

| n | Name | href | whenKey | Incomplete (never writes) | Complete | Next |
|--:|------|------|---------|---------------------------|----------|------|
| 1 | Instagram | `sites/instagram/index.html` | `itt10-ig-posts` | use Gold minute — not dest-field | Gold complete | `sites/iphone/index.html` iPhone 4 |
| 2 | iPhone 4 | `sites/iphone/index.html` | `itt10-iphone4` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/ipad/index.html` iPad |
| 3 | iPad | `sites/ipad/order.html` | `itt10-ipad` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/facebook/index.html` Open Graph |
| 4 | Open Graph | `sites/facebook/index.html` | `itt10-fb-og` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/farmville/index.html` FarmVille peak |
| 5 | FarmVille peak | `sites/farmville/index.html` | `itt10-farm` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/imgur/index.html` Imgur |
| 6 | Imgur | `sites/imgur/index.html` | `itt10-imgur` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/foursquare/index.html` Foursquare |
| 7 | Foursquare | `sites/foursquare/index.html` | `itt10-4sq` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/twitter/index.html` Twitter |
| 8 | Twitter | `sites/twitter/index.html` | `itt10-tweets` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/youtube/index.html` YouTube |
| 9 | YouTube | `sites/youtube/index.html` | `itt10-yt` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/playable/game.html` Sling Nest |
| 10 | Sling Nest | `sites/playable/game.html` | `itt10-game-slingnest` | trap · 0 ticks · wrong pick · empty field | required pick + honesty + field + save → this whenKey | `sites/instagram/index.html` Instagram |

After each official write: `[data-next-flow][data-next-when-key]` visible · HTTP 200 · chip on home **still** the star.

---

## 5. Leftover layers — 9 on disk + 18 to add = 27 dests

L2∩L3∩L4 slugs must stay **empty**. L5∩L6∩L7 slugs must stay **empty**. L5–L7 do **not** exist until densify. Do not clone an L3 slug into L4.

### L2 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/netflix/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop-netflix` |
| `sites/tumblr/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop-tumblr` |
| `sites/formspring/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop-formspring` |

### L3 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/groupondeal/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop-groupondeal` |
| `sites/quorawait/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop-quorawait` |
| `sites/instagramios/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop-instagramios` |

### L4 — ON DISK

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/chrome/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop3-chrome` |
| `sites/wave/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-wave-fun` |
| `sites/android/index.html` | leftover-pop | Click [data-pop-go] with no [data-pop-pick] | Pick · tick honesty · fill field to placeholder · [data-pop-go] | [data-pop3-costume] / named trap if present | `itt10-pop3-android` |

### L5 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/dropbox/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop4-dropbox` |
| `sites/hulu/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop4-hulu` |
| `sites/spotify-eu/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop4-spotify-eu` |

### L6 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/flickr/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop5-flickr` |
| `sites/angrybirds/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop5-angrybirds` |
| `sites/gmail/index.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop5-gmail` |

### L7 — ADD on densify · unique slugs

| href | Machine | Incomplete | Complete | Trap | Key shape |
|------|---------|------------|----------|------|-----------|
| `sites/farmville/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop6-farmville` |
| `sites/foursquare/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop6-foursquare` |
| `sites/pinterest/about.html` | leftover-pop (new) | empty go / 0 ticks / wrong pick | pick + honesty + field + go | future-year costume | `itt10-pop6-pinterest` |

L2∩L3∩L4 intersection: `∅`. L5∩L6∩L7 intersection: `∅`.

---

## 6. Popular F1–F5 (matrix)

| F | key | path | kind | field | Incomplete | Complete | Next |
|---|-----|------|------|-------|------------|----------|------|
| F1 | `itt10-ask` | `/years/2010/sites/ask/index.html` | new | query | empty field / 0 ticks / trap | pick + honesty + go | `../instagram/index.html` |
| F2 | `itt10-ig` | `/years/2010/sites/instagram/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F3 | `itt10-imgur` | `/years/2010/sites/imgur/index.html` | star | — | use Gold / official minute — do not double-write a second star path unless leftover key | pick + honesty + go | `—` |
| F4 | `itt10-facebook` | `/years/2010/sites/facebook/index.html` | reuse | — | empty field / 0 ticks / trap | pick + honesty + go | `../youtube/index.html` |
| F5 | `itt10-youtube` | `/years/2010/sites/youtube/index.html` | reuse | — | empty field / 0 ticks / trap | pick + honesty + go | `../instagram/index.html` |

Plaque-only dests that the matrix expects must grow `data-itt-popular-save` **or** a leftover-pop machine. Never run unscoped `build-popular-flows.py`.

---

## 7. Leftover-official machines (e2e matrix)

e2e: trap → 0 ticks → ticks only → wrong pick → empty field → complete. Neighbor year keys stay empty.

| href | key | needPick | field | placeholder | Incomplete | Complete |
|------|-----|----------|-------|-------------|------------|----------|
| `sites/youtube/index.html` | `itt10-yt` | `watch` | no | `—` | trap / 0 ticks / wrong pick / empty field | req + pick `watch` + field + `[data-lo-save]` |

---

## 8. EVERY dest on disk (minute)

56 HTML files (error pages skipped). Each row is a flow. Thin / plaque dests are **holes** — S8.

Machine mix on disk: {'thin-door': 16, 'leftover-pop': 9, 'dest-field-4x': 17, 'year-verb': 1, 'playable': 12, 'leftover-official': 1}

### D.01 `index.html` — Win7 · IE 8 residual — 2010

| | |
|--|--|
| URL | `/years/2010/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 21111 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.02 `pages/about.html` — About the Web in 2010

| | |
|--|--|
| URL | `/years/2010/pages/about.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 4635 |
| Hooks | `data-itt-action-status`, `data-itt-real-save`, `data-itt-year`, `data-min-req`, `data-thesis-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.03 `pages/home.html` — Welcome to the World Wide Web — 2010

| | |
|--|--|
| URL | `/years/2010/pages/home.html` |
| Machine | **thin-door** |
| Keys in file | `itt10-4sq-lx`, `itt10-ballot`, `itt10-digg-v4`, `itt10-farm-peak`, `itt10-groupon`, `itt10-ig-cap`, `itt10-imgur-lx`, `itt10-ipad-ord`, `itt10-iphone4`, `itt10-og1`, `itt10-og2`, `itt10-pin-lx`, `itt10-quora`, `itt10-t140`, `itt10-uber-sf`, `itt10-wave-fun`, `itt10-wl`, `itt10-yt` |
| Primary write | `itt10-4sq-lx` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 10921 |
| Hooks | `data-itt-5x-atlas`, `data-itt-pop-3x3`, `data-itt-pop-more`, `data-itt-pop3x`, `data-itt-tour`, `data-itt-year`, `data-itt-year-extras` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.04 `pages/map.html` — 2010 — UX flow map

| | |
|--|--|
| URL | `/years/2010/pages/map.html` |
| Machine | **thin-door** |
| Keys in file | `itt10-4sq-lx`, `itt10-ballot`, `itt10-digg-v4`, `itt10-farm-peak`, `itt10-groupon`, `itt10-ig-cap`, `itt10-imgur-lx`, `itt10-ipad-ord`, `itt10-iphone4`, `itt10-og1`, `itt10-og2`, `itt10-pin-lx`, `itt10-quora`, `itt10-t140`, `itt10-uber-sf`, `itt10-wave-fun`, `itt10-wl`, `itt10-yt` |
| Primary write | `itt10-4sq-lx` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 8267 |
| Hooks | `data-itt-2x-map`, `data-itt-3x-also`, `data-itt-flow-map`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.05 `pages/whats-new.html` — What's New — 2010

| | |
|--|--|
| URL | `/years/2010/pages/whats-new.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 0 (need **≥3** live hrefs on dests) |
| Bytes | 857 |
| Hooks | `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.06 `sites/android/index.html` — Android · Nexus One — 2010

| | |
|--|--|
| URL | `/years/2010/sites/android/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt10-pop3-android` |
| Primary write | `itt10-pop3-android` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2896 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.07 `sites/ask/index.html` — Ask.com — 2010

| | |
|--|--|
| URL | `/years/2010/sites/ask/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3517 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-popular-field`, `data-popular-req`, `data-require-field`, `data-require-field-min` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.08 `sites/browserchoice/index.html` — BrowserChoice.eu — 2010

| | |
|--|--|
| URL | `/years/2010/sites/browserchoice/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-ballot` |
| Primary write | `itt10-ballot` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3383 |
| Hooks | `data-4x-go`, `data-ballot-pick`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.09 `sites/chrome/index.html` — Google Chrome — 2010 product

| | |
|--|--|
| URL | `/years/2010/sites/chrome/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt10-pop3-chrome` |
| Primary write | `itt10-pop3-chrome` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2986 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.10 `sites/digg/index.html` — Digg v4 — 25 Aug 2010

| | |
|--|--|
| URL | `/years/2010/sites/digg/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-digg-v4` |
| Primary write | `itt10-digg-v4` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3001 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.11 `sites/facebook/cnn.html` — CNN.com — Like · Open Graph 2010

| | |
|--|--|
| URL | `/years/2010/sites/facebook/cnn.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-og1` |
| Primary write | `itt10-og1` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3066 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.12 `sites/facebook/imdb.html` — IMDb — Like · Open Graph 2010

| | |
|--|--|
| URL | `/years/2010/sites/facebook/imdb.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-og2` |
| Primary write | `itt10-og2` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3023 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.13 `sites/facebook/index.html` — Facebook Open Graph — F8 21 Apr 2010

| | |
|--|--|
| URL | `/years/2010/sites/facebook/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-fb-og` |
| Primary write | `itt10-fb-og` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 4368 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-min-req`, `data-popular-req` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.14 `sites/facetime/index.html` — FaceTime — iPhone 4 · Wi-Fi only · 2010 leftover

| | |
|--|--|
| URL | `/years/2010/sites/facetime/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt10-facetime` |
| Primary write | `itt10-facetime` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2588 |
| Hooks | `data-ft-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.15 `sites/farmville/index.html` — FarmVille — peak 83.76M · March 2010

| | |
|--|--|
| URL | `/years/2010/sites/farmville/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-farm`, `itt10-farm-peak` |
| Primary write | `itt10-farm` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3976 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-farm-log`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.16 `sites/formspring/index.html` — Formspring — 2009–10

| | |
|--|--|
| URL | `/years/2010/sites/formspring/index.html` |
| Machine | **leftover-pop** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2084 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-pop-field`, `data-pop-go` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.17 `sites/foursquare/index.html` — Foursquare — mayor 2010

| | |
|--|--|
| URL | `/years/2010/sites/foursquare/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-4sq`, `itt10-4sq-lx` |
| Primary write | `itt10-4sq` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4224 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.18 `sites/google/index.html` — Google — 2010

| | |
|--|--|
| URL | `/years/2010/sites/google/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2342 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.19 `sites/groupon/index.html` — Groupon — daily deal 2010

| | |
|--|--|
| URL | `/years/2010/sites/groupon/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-groupon` |
| Primary write | `itt10-groupon` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3181 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-groupon-req`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.20 `sites/groupondeal/index.html` — Groupon deal leftover — 2010

| | |
|--|--|
| URL | `/years/2010/sites/groupondeal/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt10-pop-groupondeal` |
| Primary write | `itt10-pop-groupondeal` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2484 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.21 `sites/ie9/index.html` — Internet Explorer 9 — preview 2010

| | |
|--|--|
| URL | `/years/2010/sites/ie9/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1933 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.22 `sites/imgur/index.html` — Imgur — image host · 2010

| | |
|--|--|
| URL | `/years/2010/sites/imgur/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-imgur`, `itt10-imgur-lx` |
| Primary write | `itt10-imgur` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3265 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.23 `sites/instagram/index.html` — Instagram — 6 Oct 2010 · iOS only

| | |
|--|--|
| URL | `/years/2010/sites/instagram/index.html` |
| Machine | **year-verb** |
| Keys in file | `itt10-ig`, `itt10-ig-2`, `itt10-ig-cap`, `itt10-ig-posts`, `itt10-next` |
| Primary write | `itt10-ig` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 6328 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-ig-caption`, `data-ig-filter`, `data-ig-share`, `data-itt-3x-also`, `data-itt-year`, `data-itt10-next` |

**Incomplete (never writes)**

1. Click [data-ig-share] with no [data-ig-filter] selected
2. Caption only, no filter, then share
3. 4x leftover panel empty [data-4x-go] (does not write star key)

**Trap (never writes):** Stories composer · Android share as default · treat as 2012

**Complete:** Click a [data-ig-filter] · optional [data-ig-caption] · [data-ig-share]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.24 `sites/instagramios/index.html` — Instagram iOS leftover door — 2010

| | |
|--|--|
| URL | `/years/2010/sites/instagramios/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt10-pop-instagramios` |
| Primary write | `itt10-pop-instagramios` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2521 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.25 `sites/instant/index.html` — Google Instant — 8 Sep 2010 leftover

| | |
|--|--|
| URL | `/years/2010/sites/instant/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt10-instant` |
| Primary write | `itt10-instant` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2605 |
| Hooks | `data-gi-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.26 `sites/ipad/index.html` — iPad — Apple · 2010

| | |
|--|--|
| URL | `/years/2010/sites/ipad/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3449 |
| Hooks | `data-itt-3x-also`, `data-itt-capture-cite`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.27 `sites/ipad/order.html` — iPad — Order · 2010

| | |
|--|--|
| URL | `/years/2010/sites/ipad/order.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-ipad`, `itt10-ipad-ord`, `itt10-ipad-order` |
| Primary write | `itt10-ipad` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 3770 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.28 `sites/ipad/safari.html` — iPad Safari — magazine · 2010

| | |
|--|--|
| URL | `/years/2010/sites/ipad/safari.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 2131 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.29 `sites/iphone/index.html` — iPhone 4 — FaceTime · Antenna · 2010

| | |
|--|--|
| URL | `/years/2010/sites/iphone/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-iphone4` |
| Primary write | `itt10-iphone4` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4635 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.30 `sites/kickstarter/index.html` — Kickstarter — 2010 leftover

| | |
|--|--|
| URL | `/years/2010/sites/kickstarter/index.html` |
| Machine | **thin-door** |
| Keys in file | `itt10-kickstarter` |
| Primary write | `itt10-kickstarter` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2804 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-ks-go`, `data-ks-req` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.31 `sites/netflix/index.html` — Netflix Watch Instantly — 2010

| | |
|--|--|
| URL | `/years/2010/sites/netflix/index.html` |
| Machine | **leftover-pop** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2194 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-pop-field`, `data-pop-go` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.32 `sites/pinterest/index.html` — Pinterest — closed beta 2010

| | |
|--|--|
| URL | `/years/2010/sites/pinterest/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-pin-lx` |
| Primary write | `itt10-pin-lx` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3158 |
| Hooks | `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pin-save` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.33 `sites/playable/extra-a.html` — Open Graph burst — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/extra-a.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-ogburst` |
| Primary write | `itt10-game-ogburst` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3840 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.34 `sites/playable/extra-b.html` — iPad tilt — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/extra-b.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-ipadtilt` |
| Primary write | `itt10-game-ipadtilt` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3922 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-mx-field`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.35 `sites/playable/extra-c.html` — Rag Hill — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/extra-c.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-raghill` |
| Primary write | `itt10-game-raghill` |
| 3×-also exits | 16 (need **≥3** live hrefs on dests) |
| Bytes | 3312 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.36 `sites/playable/extra-d.html` — Four Objects — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/extra-d.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-fourobj` |
| Primary write | `itt10-game-fourobj` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3288 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.37 `sites/playable/extra-e.html` — Filter Hold — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/extra-e.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-filterhold` |
| Primary write | `itt10-game-filterhold` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3317 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-more-field`, `data-more-goods`, `data-more-hold-ms`, `data-more-traps`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.38 `sites/playable/famous.html` — Famous games · 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/famous.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-breakout`, `itt10-game-snake` |
| Primary write | `itt10-game-breakout` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3726 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.39 `sites/playable/game-2.html` — IG filter — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/game-2.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-igfilter` |
| Primary write | `itt10-game-igfilter` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3840 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.40 `sites/playable/game-3.html` — iPad rotate — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/game-3.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-ipadrot` |
| Primary write | `itt10-game-ipadrot` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3621 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.41 `sites/playable/game-4.html` — OG like — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/game-4.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-oglike` |
| Primary write | `itt10-game-oglike` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3684 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.42 `sites/playable/game-5.html` — Imgur up — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/game-5.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-imgurup` |
| Primary write | `itt10-game-imgurup` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3843 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.43 `sites/playable/game.html` — Sling Nest — 2010

| | |
|--|--|
| URL | `/years/2010/sites/playable/game.html` |
| Machine | **playable** |
| Keys in file | `itt10-game-slingnest` |
| Primary write | `itt10-game-slingnest` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 3152 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-yg-goal`, `data-yg-next-href`, `data-yg-next-label` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.44 `sites/playable/index.html` — Sling Nest · 2010 game cabinet

| | |
|--|--|
| URL | `/years/2010/sites/playable/index.html` |
| Machine | **playable** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 17 (need **≥3** live hrefs on dests) |
| Bytes | 2948 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-itt-year-extras`, `data-itt-year-more` |

**Incomplete (never writes)**

1. Finish with no Start — never writes
2. Start then Finish with 0 goods / 0 acts
3. Score 0

**Trap (never writes):** Skip-intro / costume cabinet

**Complete:** Start · year acts / goods · Finish → game key

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.45 `sites/quora/index.html` — Quora — public 2010

| | |
|--|--|
| URL | `/years/2010/sites/quora/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-quora` |
| Primary write | `itt10-quora` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2893 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-quora-ask` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.46 `sites/quorawait/index.html` — Quora — 2010 leftover

| | |
|--|--|
| URL | `/years/2010/sites/quorawait/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt10-pop-quorawait` |
| Primary write | `itt10-pop-quorawait` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2432 |
| Hooks | `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.47 `sites/reddit/index.html` — reddit — 2010

| | |
|--|--|
| URL | `/years/2010/sites/reddit/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 15 (need **≥3** live hrefs on dests) |
| Bytes | 1980 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.48 `sites/reddit/submit.html` — reddit submit — 2010

| | |
|--|--|
| URL | `/years/2010/sites/reddit/submit.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1905 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.49 `sites/tumblr/index.html` — Tumblr — 2010 habit

| | |
|--|--|
| URL | `/years/2010/sites/tumblr/index.html` |
| Machine | **leftover-pop** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2109 |
| Hooks | `data-itt-3x-also`, `data-itt-action-status`, `data-itt-year`, `data-pop-field`, `data-pop-go` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.50 `sites/twitter/index.html` — Twitter — New Twitter · 2010

| | |
|--|--|
| URL | `/years/2010/sites/twitter/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-t140`, `itt10-tweets` |
| Primary write | `itt10-t140` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3462 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.51 `sites/uber/index.html` — UberCab — San Francisco 2010

| | |
|--|--|
| URL | `/years/2010/sites/uber/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-uber-sf` |
| Primary write | `itt10-uber-sf` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3324 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.52 `sites/wave/index.html` — Google Wave — public May · funeral 4 Aug 2010

| | |
|--|--|
| URL | `/years/2010/sites/wave/index.html` |
| Machine | **leftover-pop** |
| Keys in file | `itt10-pop3-wave`, `itt10-wave-fun` |
| Primary write | `itt10-pop3-wave` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 4637 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year`, `data-pop-field`, `data-pop-go`, `data-pop-pick`, `data-pop-req` |

**Incomplete (never writes)**

1. Click [data-pop-go] with no [data-pop-pick]
2. Pick but 0 [data-pop-req]
3. Empty [data-pop-field] then go

**Trap (never writes):** [data-pop3-costume] / named trap if present

**Complete:** Pick · tick honesty · fill field to placeholder · [data-pop-go]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

### D.53 `sites/wikileaks/index.html` — Cablegate — 28 Nov 2010

| | |
|--|--|
| URL | `/years/2010/sites/wikileaks/index.html` |
| Machine | **dest-field-4x** |
| Keys in file | `itt10-wl` |
| Primary write | `itt10-wl` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 3179 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Click [data-4x-go] with empty [data-4x-field]
2. Field shorter than [data-4x-min]
3. 0 honesty ticks if [data-4x-req] exists

**Trap (never writes):** Future-year costume on this dest

**Complete:** Fill [data-4x-field] to placeholder / min · tick req · [data-4x-go] — leftover key, not star

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.54 `sites/windowsphone/index.html` — Windows Phone 7 — Nov 2010

| | |
|--|--|
| URL | `/years/2010/sites/windowsphone/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 1961 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.55 `sites/yahoo/index.html` — Yahoo! — 2010

| | |
|--|--|
| URL | `/years/2010/sites/yahoo/index.html` |
| Machine | **thin-door** |
| Keys in file | _none harvested_ |
| Primary write | `itt10-MISSING` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 2028 |
| Hooks | `data-itt-3x-also`, `data-itt-year` |

**Incomplete (never writes)**

1. Load the page — must not write
2. Any empty button — must not write
3. S8 required: this dest is not yet a machine

**Trap (never writes):** Do not add a dest-field checkbox and call it 3×

**Complete:** S8: invent nothing; add a year-true verb. Incomplete never writes

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

**S8 hole:** grow a year-true verb on this dest (or a second page under the same slug). Dest-field literacy may stay as a leftover note. It is **not** one of the 45–60 completable 3× flows until the verb exists.

---

### D.56 `sites/youtube/index.html` — YouTube — Broadcast Yourself · 2010

| | |
|--|--|
| URL | `/years/2010/sites/youtube/index.html` |
| Machine | **leftover-official** |
| Keys in file | `itt10-yt` |
| Primary write | `itt10-yt` |
| 3×-also exits | 14 (need **≥3** live hrefs on dests) |
| Bytes | 5194 |
| Hooks | `data-4x-field`, `data-4x-go`, `data-itt-3x-also`, `data-itt-popular`, `data-itt-popular-save`, `data-itt-real-status`, `data-itt-year`, `data-lo-pick`, `data-lo-req`, `data-lo-save`, `data-lo-trap`, `data-min-req`, `data-popular-req`, `data-yt-share-bridges` |

**Incomplete (never writes)**

1. Click [data-lo-trap] — never writes
2. Click [data-lo-save] with 0 [data-lo-req]
3. Ticks only, no required [data-lo-pick] / empty [data-lo-field]
4. Wrong [data-lo-pick] (not needPick)

**Trap (never writes):** [data-lo-trap] · costume pick

**Complete:** Tick every [data-lo-req] · required pick · field to placeholder if present · [data-lo-save]

**Payload:** `real:true` · `year:"2010"` · `multiStep:true` · typed fields. Reload still shows the save. Next chip hidden until this dest’s key exists.

---

## 9. Playables

| File | Role | Incomplete | Complete |
|------|------|------------|----------|
| `sites/playable/game.html` | official-10 year game | Finish no Start | Start + acts + Finish |
| `game-2.html` … `game-5.html` | extra cabinets | Finish no Start | Start + acts + Finish |
| `famous.html` | leftover famous · not a 3rd official cabinet | empty | leftover famous key |
| `extra-a.html` · `extra-b.html` | minute extras | Finish no Start | year-true extra |
| `sites/playable/extra-c.html` | extra-c **Rag Hill** `itt10-game-raghill` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-d.html` | extra-d **Four Objects** `itt10-game-fourobj` | Finish no Start · `?test=1` still honest | Start + goods + Finish |
| `sites/playable/extra-e.html` | extra-e **Filter Hold** `itt10-game-filterhold` | Finish no Start · `?test=1` still honest | Start + goods + Finish |

---

## 10. 3× densify checklist (when `densify 2010`)

1. Recite star `itt10-ig` · guided 6 · bans.
2. Count HTML. Now **56**. Stop adding rooms at **120 HTML**.
3. Confirm L2/L3/L4 files exist and are **machines**, not costume doors.
4. Add L5 + L6 + L7 (9 dests) as leftover-pop machines. Unique slugs.
5. Add 9 more dest-pages (about / second URL) listed in L5–L7 about rows — or extra pages on existing rooms. Total leftover dests **27**.
6. Official 10: each whenKey writes. Replace plaque-only.
7. Every leftover dest: 3 incomplete + 1 trap + 1 complete.
8. `python3 scripts/build-3x-links.py` · dest also ≥3 · 0 marked 404s.
9. Guided still 6. Star unmoved. Neighbor prefix empty.
10. Prove: `python3 scripts/check-all-years.py` · one-thing 2010 · leftover-official · year-3x3 · 3x-links · extra-cde.

---

## 11. Shared laws (reprint)

1. Incomplete REAL never writes.
2. Prefix `itt10-*` only.
3. Never invent brand pixels. No ripped weights.
4. Guided `<ol>` exactly 6.
5. Star dest + key do not move.
6. Traps never write.
7. After new rooms: `python3 scripts/build-3x-links.py`.
8. No ILS June websites digit after 2018.
9. Lean first (~50 HTML) then 3× densify. Do not start at 120.

