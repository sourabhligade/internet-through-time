# 2007 — check every flow

**Date:** 2026-09-10  
**Prefix** `itt07` · star **iPhone Safari**  
**Lock:** [`2007-READ-FIRST.md`](2007-READ-FIRST.md) · dest-true map [`2007-DEST-TRUE-UNIQUE-MAP-GOALS-PHASES-FLOWS-MINUTE-2026-09-10.md`](2007-DEST-TRUE-UNIQUE-MAP-GOALS-PHASES-FLOWS-MINUTE-2026-09-10.md) · dest minutes [`2007-EVERY-FLOW-GOALS-PHASES-FLOWS-MINUTE-2026-09-10.md`](2007-EVERY-FLOW-GOALS-PHASES-FLOWS-MINUTE-2026-09-10.md)

```
test -d years/2007 && echo tree-ok
```

Guided stays **exactly 6**. Chip never moves off Safari. Desktop stays **XP + IE6**. Hub **25 years open**. 2008 / 2009 boarded.

---

## Year card — locked vs leftover vs dest-true

```mermaid
flowchart TB
  HUB["Hub · y2007 card"] --> SHELL["/years/2007/ · XP + IE6 · not iPhone desktop"]
  SHELL --> SP[Starting Point]
  subgraph locked [Locked — dest-true official]
    G["★ Safari · itt07-iphone"]
    OL["Guided 6"]
    OFF["Official 10 dest-true"]
  end
  subgraph leftover [Leftover — never the star]
    L2["Leftover 2× 120"]
    L3["Leftover-3× first 9 / second 9 / third 9"]
    L4["Leftover 4× every dest"]
    YES[Continuity leftover dests]
  end
  subgraph ban [No dest folder · no official write]
    TRAP["App Store · Chrome · 3G · Like"]
  end
  SP --> G
  SP --> OL
  SP --> OFF
  SP --> L2
  SP --> L3
  SP --> L4
  SP --> YES
  SP -.-> TRAP
```

---

## Visitor minute — official dest (n=1–10)

```mermaid
flowchart LR
  A[Land dest] --> B{What they do}
  B -->|App Store / Chrome / 3G / year-wrong| T[Trap · never writes]
  B -->|empty / 0 ticks / Go| I[Incomplete · never writes]
  B -->|period control + honesty| C["Complete official JSON"]
  C --> K["real multiStep official:true year:2007"]
  C --> NG["itt06-tweets empty · itt08-chrome empty"]
  C --> NX[Next dest HTTP 200]
  T --> STAR[itt07-iphone empty if not gold dest]
  I --> STAR
```

Use this walk on: Safari · Street View · Gmail open · Platform · Twitter leftover · YouTube leftover · Tumblr · Kindle · IE6 · Safari Queue.

---

## Visitor minute — leftover dest (never the star)

```mermaid
flowchart LR
  A[Land leftover dest] --> B{What they do}
  B -->|empty / 0 ticks| M1[never writes]
  B -->|trap pick · App Store / Like / neighbor gold| M2[never writes]
  B -->|keep + honesty + leftover save| M3[leftover JSON only]
  M3 --> K["real leftover multiStep year"]
  M3 --> STAR["itt07-iphone empty"]
  M3 --> NX[Next dest HTTP 200]
```

---

## Official 10 Next chain

```mermaid
flowchart LR
  N1["1 Safari · itt07-iphone"] --> N2["2 Street View"]
  N2 --> N3["3 Gmail open"]
  N3 --> N4["4 FB Platform"]
  N4 --> N5["5 Twitter leftover"]
  N5 --> N6["6 YouTube leftover"]
  N6 --> N7["7 Tumblr"]
  N7 --> N8["8 Kindle"]
  N8 --> N9["9 XP/IE6"]
  N9 --> N10["10 Safari Queue"]
  N10 --> N1
```

n=11–20 leftover trail only. Do not promote.

---

## Guided 6

```mermaid
flowchart LR
  G1[About] --> G2["★ Safari"]
  G2 --> G3[Street View leftover]
  G3 --> G4[Gmail leftover]
  G4 --> G5[Platform leftover]
  G5 --> G6[Year flow map]
```

`#ott-guided-2007 ol > li` = **6**. No 7th `<li>`.

---

## Leftover-3× who may sit

```mermaid
flowchart TB
  DEST[On-disk dest famous 2007] --> GQ{Gold dest iphone?}
  GQ -->|yes| BAN[Never leftover-3×]
  GQ -->|no| OQ{Official n=1-10?}
  OQ -->|no| FS[First or second]
  OQ -->|yes| TH[Third only · pop3]
  FS --> F9["First 9 · wiki myspace maps ebay stumble wow flickr reddit digg"]
  FS --> S9["Second 9 · google blogger cnn orkut itunes steam wii ff2 nyt"]
  TH --> T9["Third 9 · streetview gmail fbplat twitter youtube tumblr kindle ie6 xbox"]
```

---

## Check table

| Step | Pass |
|------|------|
| Hub card 2007 | `a.year-card.available.y2007` |
| `/years/2007/` Skip connect | XP · IE6 · not iPhone desktop |
| Chip | ★ Safari → `sites/iphone/index.html` |
| Guided ol | **6** |
| About | June **121,892,559** · Jan **106,875,138** · users **1,373,327,790** |
| Gold empty / App Store / Chrome | write nothing |
| Gold URL + ticks + Go | `itt07-iphone` `{official:true}` |
| Official 10 dest-true | trap/empty never write · complete official key · Next 200 |
| Neighbor keys | `itt06-tweets` · `itt08-chrome` empty |
| Leftover complete | never `itt07-iphone` |
| Exit | `itt07-*` only |
| Leftover-3× first | no `iphone` |
| Warehouse | each dest **once** |
| `#ott-2x-2007` | below guided · not inside `<ol>` |
| Atlas / hub | **25 years open** · 2007 available · 2008/2009 boarded |

**e2e:** `e2e/2007-every-link.spec.js` (every dest folder + every home dest href + official dest-true face) · `e2e/2007-dest-true-official.spec.js` · `e2e/2007-mvp.spec.js` · `e2e/gold-leftover-isolation.spec.js` · `e2e/hub-years.spec.js`

**DONE 2026-09-10:** tree-ok · un-board · dest-true official 10 · first-board 27 · leftover never writes gold · dest freeze **82**.
