# 1997 — 5× real dests implement bible

**Parent research:** [`../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md`](../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md)  
**Games/toys/trails bible (already shipped):** [`../5X-MEASURABLE-IMPLEMENT/YEAR-1997.md`](../5X-MEASURABLE-IMPLEMENT/YEAR-1997.md)  
**Leftover F1–F5 (already shipped, do not rebuild):** Slashdot → eBay → ICQ → Think Different → Drudge → PointCast  
**Star (locked):** **PointCast 2 channels** · `itt97-pointcast` · `sites/pointcast/index.html`  
**Shell:** Win95 · IE/Netscape  
**Thesis:** Push + black eBay. PointCast is the star.  
**Sources:** Traffic: AOL #1 Yahoo #2 MSN #3. Wiki 203.  
**Status:** implemented 2026-08-16 · D0 leftovers + D1/D2/D3 dests on disk · dest 5× met

FOREST year — **reuse** leftover HTML already on disk. Do **not** 5× clone-Yahoo trees. D0 leftovers were listed 2026-08-16 (14 chips). Remaining gap = new period pages on **non-Yahoo** products.

---

## 1. Baseline → 5× dests (this year only)

| Metric | Now (files) | **5×** | Add |
|--------|------------:|-------:|----:|
| Year-games | 5 | **5** | 0 (shipped) |
| Toys | 15 | **15** | 0 (shipped) |
| Trail stops | 50 | **50** | 0 (shipped) |
| Flow-map unique dests | 53 | **70** | **+17** |
| Home+map unique dests (no 3×) | 77 | **185** | **+108** |
| Site dirs on disk | 25 | keep | 0 unless lean + dual-cite |
| HTML on disk | 90 | — | **+95 real pages** if gap remains |

Prefix: `itt97-*` only. Never write neighbor-year keys.

---

## 2. Night diagram (how a visitor uses the real 5× dests)

```
 /years/1997/
      │ skip-connect
      ▼
 pages/home.html  ← 185 unique dests (chips · no 404 · no ?night=)
      │
      ├──▶ 15 toys     sites/playable/index.html?g=1 … g=15   (shipped)
      ├──▶ 5 year-games sites/playable/game.html … game-5.html (shipped)
      ├──▶ 5 trail chains (10 stops) → STAR                   (shipped)
      └──▶ flow map     pages/map.html  (70 leaves)
                │
                ▼
         sites/pointcast/index.html
         STAR PointCast
         empty / 0–1 checks → NO itt97-pointcast
```

**Do not** add a 7th guided `<ol>` item. Guided stays **6**. New dests live in chips + map + densify pages.

---

## 3. D0 — leftovers already on disk

D0 ran 2026-08-16: **+14 leftover chips** on home + 5× atlas (IUMA bands / handbook / GeoCities houses / Yahoo *hubs only* / Wikipedia internals / iPod faq — not clone-Yahoo trees).

Still short after D0: L1 **77+ leftovers vs 185** · map **53 vs 70**. Remaining = D1 densify on non-Yahoo products.

```
[Home] → leftover chip → existing HTML
              │
     missing file → DO NOT SHIP
              │
     open page → optional Next → sites/pointcast/index.html (star)
```

---

## 4. D1 — densify existing rooms (real pages)

Add **up to 4 extra HTML files per existing dir** only when that surface existed in 1997. Incomplete never writes. Never invent brand pixels.

**Minute build (every extra page)**
1. Create `years/1997/sites/<slug>/<page>.html` (dir already exists).
2. Period action: one writer **or** honest literacy dead-end.
3. Honesty strip: class + date + “museum original · no commercial SWF”.
4. Prefix `itt97-*` only. Empty / 0–1 checks **never** write.
5. Next chip → `sites/pointcast/index.html` (PointCast).
6. Add `href` to home atlas + `flow-maps-5x-atlas.js`.
7. `js/config/1997.js` urlMap + titleMap for the new file.
8. e2e: open · incomplete · complete writes only if writer.

| Existing dir | New file | Premise | Key / empty |
|--------------|----------|---------|-------------|
| `sites/pointcast/` | `channels.html` | 2 channels · not star write | no write (literacy) |
| `sites/icq/` | `uin.html` | add buddy | `itt97-icq-b` · empty never writes |
| `sites/ebay/` | `item.html` | bid confirm | `itt97-ebay-b` · empty never writes |
| `sites/slashdot/` | `moderate.html` | comment not empty | `itt97-slash` · empty never writes |
| `sites/hotmail/` | `compose.html` | To + body | `itt97-hm` · empty never writes |
| `sites/apple/` | `thinkdifferent.html` | 2 product hops | `itt97-td` · empty never writes |
| `sites/drudge/` | `story.html` | headline then story | `itt97-drudge` · empty never writes |

D1 page count in this bible: **7**.

```
[Start] → open extra page → do the period action
              │
     empty / timeout / 0–1 checks → NO WRITE
              │
     complete (writer only) → localStorage itt97-<slug>-<page>
              │
           Next chip → sites/pointcast/index.html
```

---

## 5. D4 — forest remainder (non-Yahoo densify)

Do **not** add new `sites/` dirs. Close the remaining L1/map gap with D1 extras on Amazon / eBay / Hotmail / Google / AIM / GeoCities homestead / NASA / White House — **never** `/yahoo/Art/Photography/`.

Remaining after D0: **+95** pages. Use the D1 table above until unique dests ≥ 185 and map ≥ 70.

---

## 6. Flow-map dests

Add new `href`s to `ITT.flowMaps["1997"]` via `js/config/flow-maps-5x-atlas.js` branch **“5× atlas”**.

1. Every leaf: `name`, `href` (must exist), `do` (period verb + key if writer).
2. `node scripts/audit-every-flow.js` must stay green.
3. No `#`. No `pages/error/*`. No `?night=`.
4. Gate: unique hrefs ≥ **70**.

On-disk rooms to reuse: `aim, altavista, amazon, aol, apple, bbc, cnn, drudge, ebay, excite, geocities, hotbot, hotmail, icq, javaplugin, lycos, microsoft, msn, netscape, pointcast, scripting, slashdot, winamp, yahoo`

---

## 7. Home chips (L1)

`years/1997/pages/home.html`

1. Keep the locked star chip.
2. Add chips until unique dests (home+map, no 3×) ≥ **185**.
3. Every chip `href` exists. No `#`. No 404. No `?night=`.
4. Optional `data-trail-keys` for writers so home shows ✓.
5. Guided `<ol>` stays **6**.

---

## 8. Phases (implement in order)

### D0 — Leftovers
Already listed 2026-08-16. Re-run only if a leftover href 404s.

### D1 / D4 — Densify non-Yahoo
**Files:** extras in §4 · urlMap · atlas · home  
**Gate:** unique dests ≥ 185 · map ≥ 70 · `audit-every-flow`

After each phase: one-thing `itt97-pointcast` still writes · empty still blocked.

---

## 9. ROI

| Spend | Visitor gets | Skip |
|-------|--------------|------|
| leftover chips (D0) | “I can go there” on files already on disk | `?night=` |
| 4 extras / existing dir (D1) | period verb × rooms you already know | clone-Yahoo |
| map + home chips | L1×5 and mapU×5 **as files** | 404 chips |

**Why this year:** Push + black eBay. PointCast is the star.

---

## 10. Acceptance (this year is real-5× dests done)

- [x] flow-map unique hrefs ≥ **70**
- [x] home+map unique dests ≥ **185**
- [x] every new href is a real HTML file (no `?night=`, no 404 chip)
- [x] no new `sites/` dir unless lean + dual-cited + named in §5
- [x] `itt97-pointcast` still the locked star
- [x] incomplete never writes
- [x] guided `<ol>` still 6
- [x] `check-all-years.py` pass
- [x] `audit-every-flow.js` + `one-thing-per-year` grep `1997` green

---

## 11. Command

```
implement 5x real dests 1997
```

Do **not** implement another year in the same pass.
