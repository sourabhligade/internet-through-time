# 2002 — 5× real dests implement bible

**Parent research:** [`../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md`](../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md)  
**Games/toys/trails bible (already shipped):** [`../5X-MEASURABLE-IMPLEMENT/YEAR-2002.md`](../5X-MEASURABLE-IMPLEMENT/YEAR-2002.md)  
**Leftover F1–F5 (already shipped, do not rebuild):** Netflix queue → Friendster → KaZaA → Wired CSS → Google News → Stumble  
**Star (locked):** **StumbleUpon** · `itt02-stumble` · `sites/stumbleupon/index.html`  
**Shell:** WinXP · IE6  
**Thesis:** Stumble. Friendster. KaZaA. Netflix discs.  
**Sources:** Google #4. Wiki 171. Alien Hominid NG 2002.  
**Status:** implemented 2026-08-16 · D0 leftovers + D1/D2/D3 dests on disk · dest 5× met

FOREST year — **reuse** leftover HTML already on disk. Do **not** 5× clone-Yahoo trees. D0 leftovers were listed 2026-08-16 (60 chips). Remaining gap = new period pages on **non-Yahoo** products.

---

## 1. Baseline → 5× dests (this year only)

| Metric | Now (files) | **5×** | Add |
|--------|------------:|-------:|----:|
| Year-games | 5 | **5** | 0 (shipped) |
| Toys | 15 | **15** | 0 (shipped) |
| Trail stops | 50 | **50** | 0 (shipped) |
| Flow-map unique dests | 66 | **70** | **+4** |
| Home+map unique dests (no 3×) | 170 | **170** | **+0** |
| Site dirs on disk | 62 | keep | 0 unless lean + dual-cite |
| HTML on disk | 214 | — | **+0 real pages** if gap remains |

Prefix: `itt02-*` only. Never write neighbor-year keys.

**This year already meets dest targets.** Keep the star. Do not add clone rooms. Optional: hygiene only.

---

## 2. Night diagram (how a visitor uses the real 5× dests)

```
 /years/2002/
      │ skip-connect
      ▼
 pages/home.html  ← 170 unique dests (chips · no 404 · no ?night=)
      │
      ├──▶ 15 toys     sites/playable/index.html?g=1 … g=15   (shipped)
      ├──▶ 5 year-games sites/playable/game.html … game-5.html (shipped)
      ├──▶ 5 trail chains (10 stops) → STAR                   (shipped)
      └──▶ flow map     pages/map.html  (70 leaves)
                │
                ▼
         sites/stumbleupon/index.html
         STAR StumbleUpon
         empty / 0–1 checks → NO itt02-stumble
```

**Do not** add a 7th guided `<ol>` item. Guided stays **6**. New dests live in chips + map + densify pages.

---

## 3. D0 — leftovers already on disk

D0 leftovers listed where useful (60 chips). Targets already met. Do not pad.

```
[Home] → leftover chip → existing HTML
              │
     missing file → DO NOT SHIP
              │
     open page → optional Next → sites/stumbleupon/index.html (star)
```

---

## 4. D1 — densify existing rooms (real pages)

Add **up to 4 extra HTML files per existing dir** only when that surface existed in 2002. Incomplete never writes. Never invent brand pixels.

**Minute build (every extra page)**
1. Create `years/2002/sites/<slug>/<page>.html` (dir already exists).
2. Period action: one writer **or** honest literacy dead-end.
3. Honesty strip: class + date + “museum original · no commercial SWF”.
4. Prefix `itt02-*` only. Empty / 0–1 checks **never** write.
5. Next chip → `sites/stumbleupon/index.html` (StumbleUpon).
6. Add `href` to home atlas + `flow-maps-5x-atlas.js`.
7. `js/config/2002.js` urlMap + titleMap for the new file.
8. e2e: open · incomplete · complete writes only if writer.

| Existing dir | New file | Premise | Key / empty |
|--------------|----------|---------|-------------|
| `sites/stumbleupon/` | `about.html` | how stumble works | no write (literacy) |
| `sites/friendster/` | `testimonial.html` | profile + note | `itt02-fs` · empty never writes |
| `sites/netflix/` | `queue.html` | add + reorder | `itt02-nfq` · empty never writes |
| `sites/kazaa/` | `search.html` | no files | no write (literacy) |
| `sites/googlenews/` | `cluster.html` | headline | `itt02-gn` · empty never writes |

D1 page count in this bible: **5**.

```
[Start] → open extra page → do the period action
              │
     empty / timeout / 0–1 checks → NO WRITE
              │
     complete (writer only) → localStorage itt02-<slug>-<page>
              │
           Next chip → sites/stumbleupon/index.html
```

---

## 5. D4 — forest remainder (non-Yahoo densify)

Do **not** add new `sites/` dirs. Close the remaining L1/map gap with D1 extras on Amazon / eBay / Hotmail / Google / AIM / GeoCities homestead / NASA / White House — **never** `/yahoo/Art/Photography/`.

Remaining after D0: **+0** pages. Use the D1 table above until unique dests ≥ 170 and map ≥ 70.

---

## 6. Flow-map dests

Add new `href`s to `ITT.flowMaps["2002"]` via `js/config/flow-maps-5x-atlas.js` branch **“5× atlas”**.

1. Every leaf: `name`, `href` (must exist), `do` (period verb + key if writer).
2. `node scripts/audit-every-flow.js` must stay green.
3. No `#`. No `pages/error/*`. No `?night=`.
4. Gate: unique hrefs ≥ **70**.

On-disk rooms to reuse: `about, altavista, amazon, aol, apple, askjeeves, bbc, blogdex, blogger, bowienet, cnn, daypop, deviantart, dmoz, ebay, encarta, excite, friendster, gamespot, geocities, gnutella, google, googlenews, hampsterdance` …

---

## 7. Home chips (L1)

`years/2002/pages/home.html`

1. Keep the locked star chip.
2. Add chips until unique dests (home+map, no 3×) ≥ **170**.
3. Every chip `href` exists. No `#`. No 404. No `?night=`.
4. Optional `data-trail-keys` for writers so home shows ✓.
5. Guided `<ol>` stays **6**.

---

## 8. Phases (implement in order)

### D0 — Leftovers
Already listed 2026-08-16. Re-run only if a leftover href 404s.

### D1 / D4 — Densify non-Yahoo
**Files:** extras in §4 · urlMap · atlas · home  
**Gate:** unique dests ≥ 170 · map ≥ 70 · `audit-every-flow`

After each phase: one-thing `itt02-stumble` still writes · empty still blocked.

---

## 9. ROI

| Spend | Visitor gets | Skip |
|-------|--------------|------|
| leftover chips (D0) | “I can go there” on files already on disk | `?night=` |
| 4 extras / existing dir (D1) | period verb × rooms you already know | clone-Yahoo |
| map + home chips | L1×5 and mapU×5 **as files** | 404 chips |

**Why this year:** Stumble. Friendster. KaZaA. Netflix discs.

---

## 10. Acceptance (this year is real-5× dests done)

- [x] flow-map unique hrefs ≥ **70**
- [x] home+map unique dests ≥ **170**
- [x] every new href is a real HTML file (no `?night=`, no 404 chip)
- [x] no new `sites/` dir unless lean + dual-cited + named in §5
- [x] `itt02-stumble` still the locked star
- [x] incomplete never writes
- [x] guided `<ol>` still 6
- [x] `check-all-years.py` pass
- [x] `audit-every-flow.js` + `one-thing-per-year` grep `2002` green

---

## 11. Command

```
implement 5x real dests 2002
```

Do **not** implement another year in the same pass.
