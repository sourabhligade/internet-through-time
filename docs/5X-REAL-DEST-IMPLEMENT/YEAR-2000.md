# 2000 — 5× real dests implement bible

**Parent research:** [`../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md`](../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md)  
**Games/toys/trails bible (already shipped):** [`../5X-MEASURABLE-IMPLEMENT/YEAR-2000.md`](../5X-MEASURABLE-IMPLEMENT/YEAR-2000.md)  
**Leftover F1–F5 (already shipped, do not rebuild):** eBay → Pets → Amazon smile → Napster legal → Flash nag → MapQuest  
**Star (locked):** **MapQuest** · `itt00-mapquest` · `sites/mapquest/index.html`  
**Shell:** Win98/ME · IE5  
**Thesis:** MapQuest print. Pets.com sock. Dot-com fade.  
**Sources:** Yahoo #1. eBay #4. Wiki 263.  
**Status:** implemented 2026-08-16 · D0 leftovers + D1/D2/D3 dests on disk · dest 5× met

FOREST year — **reuse** leftover HTML already on disk. Do **not** 5× clone-Yahoo trees. D0 leftovers were listed 2026-08-16 (15 chips). Remaining gap = new period pages on **non-Yahoo** products.

---

## 1. Baseline → 5× dests (this year only)

| Metric | Now (files) | **5×** | Add |
|--------|------------:|-------:|----:|
| Year-games | 5 | **5** | 0 (shipped) |
| Toys | 15 | **15** | 0 (shipped) |
| Trail stops | 50 | **50** | 0 (shipped) |
| Flow-map unique dests | 68 | **85** | **+17** |
| Home+map unique dests (no 3×) | 164 | **295** | **+131** |
| Site dirs on disk | 49 | keep | 0 unless lean + dual-cite |
| HTML on disk | 178 | — | **+117 real pages** if gap remains |

Prefix: `itt00-*` only. Never write neighbor-year keys.

---

## 2. Night diagram (how a visitor uses the real 5× dests)

```
 /years/2000/
      │ skip-connect
      ▼
 pages/home.html  ← 295 unique dests (chips · no 404 · no ?night=)
      │
      ├──▶ 15 toys     sites/playable/index.html?g=1 … g=15   (shipped)
      ├──▶ 5 year-games sites/playable/game.html … game-5.html (shipped)
      ├──▶ 5 trail chains (10 stops) → STAR                   (shipped)
      └──▶ flow map     pages/map.html  (85 leaves)
                │
                ▼
         sites/mapquest/index.html
         STAR MapQuest
         empty / 0–1 checks → NO itt00-mapquest
```

**Do not** add a 7th guided `<ol>` item. Guided stays **6**. New dests live in chips + map + densify pages.

---

## 3. D0 — leftovers already on disk

D0 ran 2026-08-16: **+15 leftover chips** on home + 5× atlas (IUMA bands / handbook / GeoCities houses / Yahoo *hubs only* / Wikipedia internals / iPod faq — not clone-Yahoo trees).

Still short after D0: L1 **164+ leftovers vs 295** · map **68 vs 85**. Remaining = D1 densify on non-Yahoo products.

```
[Home] → leftover chip → existing HTML
              │
     missing file → DO NOT SHIP
              │
     open page → optional Next → sites/mapquest/index.html (star)
```

---

## 4. D1 — densify existing rooms (real pages)

Add **up to 4 extra HTML files per existing dir** only when that surface existed in 2000. Incomplete never writes. Never invent brand pixels.

**Minute build (every extra page)**
1. Create `years/2000/sites/<slug>/<page>.html` (dir already exists).
2. Period action: one writer **or** honest literacy dead-end.
3. Honesty strip: class + date + “museum original · no commercial SWF”.
4. Prefix `itt00-*` only. Empty / 0–1 checks **never** write.
5. Next chip → `sites/mapquest/index.html` (MapQuest).
6. Add `href` to home atlas + `flow-maps-5x-atlas.js`.
7. `js/config/2000.js` urlMap + titleMap for the new file.
8. e2e: open · incomplete · complete writes only if writer.

| Existing dir | New file | Premise | Key / empty |
|--------------|----------|---------|-------------|
| `sites/mapquest/` | `print.html` | turn list · not star | no write (literacy) |
| `sites/mapquest/` | `directions.html` | A to B | `itt00-mq-dir` · empty never writes |
| `sites/ebay/` | `dutch.html` | qty bid | `itt00-dutch` · empty never writes |
| `sites/pets/` | `shop.html` | open then shutdown honesty | no write (literacy) |
| `sites/amazon/` | `cart.html` | smile cart | `itt00-amzn` · empty never writes |
| `sites/napster/` | `legal.html` | 2 legal hops | `itt00-nap-l` · empty never writes |
| `sites/flash4/` | `nag.html` | download theater | no write (literacy) |

D1 page count in this bible: **7**.

```
[Start] → open extra page → do the period action
              │
     empty / timeout / 0–1 checks → NO WRITE
              │
     complete (writer only) → localStorage itt00-<slug>-<page>
              │
           Next chip → sites/mapquest/index.html
```

---

## 5. D4 — forest remainder (non-Yahoo densify)

Do **not** add new `sites/` dirs. Close the remaining L1/map gap with D1 extras on Amazon / eBay / Hotmail / Google / AIM / GeoCities homestead / NASA / White House — **never** `/yahoo/Art/Photography/`.

Remaining after D0: **+117** pages. Use the D1 table above until unique dests ≥ 295 and map ≥ 85.

---

## 6. Flow-map dests

Add new `href`s to `ITT.flowMaps["2000"]` via `js/config/flow-maps-5x-atlas.js` branch **“5× atlas”**.

1. Every leaf: `name`, `href` (must exist), `do` (period verb + key if writer).
2. `node scripts/audit-every-flow.js` must stay green.
3. No `#`. No `pages/error/*`. No `?night=`.
4. Gate: unique hrefs ≥ **85**.

On-disk rooms to reuse: `about, altavista, amazon, aol, apple, askjeeves, bbc, blogger, bowienet, camworld, cnn, dmoz, ebay, excite, expedia, flash4, gamespot, geocities, gnutella, google, half, hampsterdance, homestar, hotbot` …

---

## 7. Home chips (L1)

`years/2000/pages/home.html`

1. Keep the locked star chip.
2. Add chips until unique dests (home+map, no 3×) ≥ **295**.
3. Every chip `href` exists. No `#`. No 404. No `?night=`.
4. Optional `data-trail-keys` for writers so home shows ✓.
5. Guided `<ol>` stays **6**.

---

## 8. Phases (implement in order)

### D0 — Leftovers
Already listed 2026-08-16. Re-run only if a leftover href 404s.

### D1 / D4 — Densify non-Yahoo
**Files:** extras in §4 · urlMap · atlas · home  
**Gate:** unique dests ≥ 295 · map ≥ 85 · `audit-every-flow`

After each phase: one-thing `itt00-mapquest` still writes · empty still blocked.

---

## 9. ROI

| Spend | Visitor gets | Skip |
|-------|--------------|------|
| leftover chips (D0) | “I can go there” on files already on disk | `?night=` |
| 4 extras / existing dir (D1) | period verb × rooms you already know | clone-Yahoo |
| map + home chips | L1×5 and mapU×5 **as files** | 404 chips |

**Why this year:** MapQuest print. Pets.com sock. Dot-com fade.

---

## 10. Acceptance (this year is real-5× dests done)

- [x] flow-map unique hrefs ≥ **85**
- [x] home+map unique dests ≥ **295**
- [x] every new href is a real HTML file (no `?night=`, no 404 chip)
- [x] no new `sites/` dir unless lean + dual-cited + named in §5
- [x] `itt00-mapquest` still the locked star
- [x] incomplete never writes
- [x] guided `<ol>` still 6
- [x] `check-all-years.py` pass
- [x] `audit-every-flow.js` + `one-thing-per-year` grep `2000` green

---

## 11. Command

```
implement 5x real dests 2000
```

Do **not** implement another year in the same pass.
