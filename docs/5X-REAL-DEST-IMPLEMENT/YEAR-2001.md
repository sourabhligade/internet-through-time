# 2001 — 5× real dests implement bible

**Parent research:** [`../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md`](../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md)  
**Games/toys/trails bible (already shipped):** [`../5X-MEASURABLE-IMPLEMENT/YEAR-2001.md`](../5X-MEASURABLE-IMPLEMENT/YEAR-2001.md)  
**Leftover F1–F5 (already shipped, do not rebuild):** Wiki → iPod → Wayback → Movable Type → broadband → MSN  
**Star (locked):** **MSN** · `itt01-msn` · `sites/msn/index.html`  
**Shell:** WinXP · IE6  
**Thesis:** Wiki + iPod + Wayback. MSN is the star.  
**Sources:** Google #9. Wiki 208. Wikipedia 15 Jan 2001.  
**Status:** implemented 2026-08-16 · D0 leftovers + D1/D2/D3 dests on disk · dest 5× met

FOREST year — **reuse** leftover HTML already on disk. Do **not** 5× clone-Yahoo trees. D0 leftovers were listed 2026-08-16 (19 chips). Remaining gap = new period pages on **non-Yahoo** products.

---

## 1. Baseline → 5× dests (this year only)

| Metric | Now (files) | **5×** | Add |
|--------|------------:|-------:|----:|
| Year-games | 5 | **5** | 0 (shipped) |
| Toys | 15 | **15** | 0 (shipped) |
| Trail stops | 50 | **50** | 0 (shipped) |
| Flow-map unique dests | 65 | **80** | **+15** |
| Home+map unique dests (no 3×) | 173 | **195** | **+22** |
| Site dirs on disk | 51 | keep | 0 unless lean + dual-cite |
| HTML on disk | 191 | — | **+4 real pages** if gap remains |

Prefix: `itt01-*` only. Never write neighbor-year keys.

---

## 2. Night diagram (how a visitor uses the real 5× dests)

```
 /years/2001/
      │ skip-connect
      ▼
 pages/home.html  ← 195 unique dests (chips · no 404 · no ?night=)
      │
      ├──▶ 15 toys     sites/playable/index.html?g=1 … g=15   (shipped)
      ├──▶ 5 year-games sites/playable/game.html … game-5.html (shipped)
      ├──▶ 5 trail chains (10 stops) → STAR                   (shipped)
      └──▶ flow map     pages/map.html  (80 leaves)
                │
                ▼
         sites/msn/index.html
         STAR MSN
         empty / 0–1 checks → NO itt01-msn
```

**Do not** add a 7th guided `<ol>` item. Guided stays **6**. New dests live in chips + map + densify pages.

---

## 3. D0 — leftovers already on disk

D0 ran 2026-08-16: **+19 leftover chips** on home + 5× atlas (IUMA bands / handbook / GeoCities houses / Yahoo *hubs only* / Wikipedia internals / iPod faq — not clone-Yahoo trees).

Still short after D0: L1 **173+ leftovers vs 195** · map **65 vs 80**. Remaining = D1 densify on non-Yahoo products.

```
[Home] → leftover chip → existing HTML
              │
     missing file → DO NOT SHIP
              │
     open page → optional Next → sites/msn/index.html (star)
```

---

## 4. D1 — densify existing rooms (real pages)

Add **up to 4 extra HTML files per existing dir** only when that surface existed in 2001. Incomplete never writes. Never invent brand pixels.

**Minute build (every extra page)**
1. Create `years/2001/sites/<slug>/<page>.html` (dir already exists).
2. Period action: one writer **or** honest literacy dead-end.
3. Honesty strip: class + date + “museum original · no commercial SWF”.
4. Prefix `itt01-*` only. Empty / 0–1 checks **never** write.
5. Next chip → `sites/msn/index.html` (MSN).
6. Add `href` to home atlas + `flow-maps-5x-atlas.js`.
7. `js/config/2001.js` urlMap + titleMap for the new file.
8. e2e: open · incomplete · complete writes only if writer.

| Existing dir | New file | Premise | Key / empty |
|--------------|----------|---------|-------------|
| `sites/wikipedia/` | `preview.html` | preview never writes | no write (literacy) |
| `sites/wikipedia/` | `history.html` | after save | `itt01-wiki-h` · empty never writes |
| `sites/apple/` | `ipod.html` | library no Store | no write (literacy) |
| `sites/apple/` | `faq.html` | faq | no write (literacy) |
| `sites/wayback/` | `lookup.html` | query not empty | `itt01-wb` · empty never writes |
| `sites/msn/` | `hotmail.html` | residual | no write (literacy) |
| `sites/movabletype/` | `publish.html` | title | `itt01-mt` · empty never writes |

D1 page count in this bible: **7**.

```
[Start] → open extra page → do the period action
              │
     empty / timeout / 0–1 checks → NO WRITE
              │
     complete (writer only) → localStorage itt01-<slug>-<page>
              │
           Next chip → sites/msn/index.html
```

---

## 5. D4 — forest remainder (non-Yahoo densify)

Do **not** add new `sites/` dirs. Close the remaining L1/map gap with D1 extras on Amazon / eBay / Hotmail / Google / AIM / GeoCities homestead / NASA / White House — **never** `/yahoo/Art/Photography/`.

Remaining after D0: **+4** pages. Use the D1 table above until unique dests ≥ 195 and map ≥ 80.

---

## 6. Flow-map dests

Add new `href`s to `ITT.flowMaps["2001"]` via `js/config/flow-maps-5x-atlas.js` branch **“5× atlas”**.

1. Every leaf: `name`, `href` (must exist), `do` (period verb + key if writer).
2. `node scripts/audit-every-flow.js` must stay green.
3. No `#`. No `pages/error/*`. No `?night=`.
4. Gate: unique hrefs ≥ **80**.

On-disk rooms to reuse: `about, altavista, amazon, aol, apple, askjeeves, bbc, blogdex, blogger, bowienet, broadband, cnet, cnn, dmoz, ebay, encarta, excite, gamespot, geocities, gnutella, google, habbo, hampsterdance, hotbot` …

---

## 7. Home chips (L1)

`years/2001/pages/home.html`

1. Keep the locked star chip.
2. Add chips until unique dests (home+map, no 3×) ≥ **195**.
3. Every chip `href` exists. No `#`. No 404. No `?night=`.
4. Optional `data-trail-keys` for writers so home shows ✓.
5. Guided `<ol>` stays **6**.

---

## 8. Phases (implement in order)

### D0 — Leftovers
Already listed 2026-08-16. Re-run only if a leftover href 404s.

### D1 / D4 — Densify non-Yahoo
**Files:** extras in §4 · urlMap · atlas · home  
**Gate:** unique dests ≥ 195 · map ≥ 80 · `audit-every-flow`

After each phase: one-thing `itt01-msn` still writes · empty still blocked.

---

## 9. ROI

| Spend | Visitor gets | Skip |
|-------|--------------|------|
| leftover chips (D0) | “I can go there” on files already on disk | `?night=` |
| 4 extras / existing dir (D1) | period verb × rooms you already know | clone-Yahoo |
| map + home chips | L1×5 and mapU×5 **as files** | 404 chips |

**Why this year:** Wiki + iPod + Wayback. MSN is the star.

---

## 10. Acceptance (this year is real-5× dests done)

- [x] flow-map unique hrefs ≥ **80**
- [x] home+map unique dests ≥ **195**
- [x] every new href is a real HTML file (no `?night=`, no 404 chip)
- [x] no new `sites/` dir unless lean + dual-cited + named in §5
- [x] `itt01-msn` still the locked star
- [x] incomplete never writes
- [x] guided `<ol>` still 6
- [x] `check-all-years.py` pass
- [x] `audit-every-flow.js` + `one-thing-per-year` grep `2001` green

---

## 11. Command

```
implement 5x real dests 2001
```

Do **not** implement another year in the same pass.
