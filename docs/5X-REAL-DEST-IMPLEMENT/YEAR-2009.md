# 2009 — 5× real dests implement bible

**Parent research:** [`../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md`](../5X-REAL-DEST-GAPS-2011-2020-AND-1994-2007.md)  
**Games/toys/trails bible (already shipped):** [`../5X-MEASURABLE-IMPLEMENT/YEAR-2009.md`](../5X-MEASURABLE-IMPLEMENT/YEAR-2009.md)  
**Leftover F1–F5 (already shipped, do not rebuild):** Foursquare → FarmVille → Bing → SO accept → Win7 → Like  
**Star (locked):** **Facebook Like** · `itt09-fb-likes` · `sites/facebook/feed.html`  
**Shell:** Win7 · IE8  
**Thesis:** Like. FarmVille. Bing. Foursquare. Win7.  
**Sources:** FarmVille 2009. Bing 3 Jun. Wiki 336.  
**Status:** implemented 2026-08-16 · D0 leftovers + D1/D2/D3 dests on disk · dest 5× met

FOREST year — **reuse** leftover HTML already on disk. Do **not** 5× clone-Yahoo trees. D0 leftovers were listed 2026-08-16 (0 chips). Remaining gap = new period pages on **non-Yahoo** products.

---

## 1. Baseline → 5× dests (this year only)

| Metric | Now (files) | **5×** | Add |
|--------|------------:|-------:|----:|
| Year-games | 5 | **5** | 0 (shipped) |
| Toys | 15 | **15** | 0 (shipped) |
| Trail stops | 50 | **50** | 0 (shipped) |
| Flow-map unique dests | 112 | **90** | **+0** |
| Home+map unique dests (no 3×) | 295 | **290** | **+0** |
| Site dirs on disk | 107 | keep | 0 unless lean + dual-cite |
| HTML on disk | 340 | — | **+0 real pages** if gap remains |

Prefix: `itt09-*` only. Never write neighbor-year keys.

**This year already meets dest targets.** Keep the star. Do not add clone rooms. Optional: hygiene only.

---

## 2. Night diagram (how a visitor uses the real 5× dests)

```
 /years/2009/
      │ skip-connect
      ▼
 pages/home.html  ← 290 unique dests (chips · no 404 · no ?night=)
      │
      ├──▶ 15 toys     sites/playable/index.html?g=1 … g=15   (shipped)
      ├──▶ 5 year-games sites/playable/game.html … game-5.html (shipped)
      ├──▶ 5 trail chains (10 stops) → STAR                   (shipped)
      └──▶ flow map     pages/map.html  (90 leaves)
                │
                ▼
         sites/facebook/feed.html
         STAR Facebook Like
         empty / 0–1 checks → NO itt09-fb-likes
```

**Do not** add a 7th guided `<ol>` item. Guided stays **6**. New dests live in chips + map + densify pages.

---

## 3. D0 — leftovers already on disk

D0 leftovers listed where useful (0 chips). Targets already met. Do not pad.

```
[Home] → leftover chip → existing HTML
              │
     missing file → DO NOT SHIP
              │
     open page → optional Next → sites/facebook/feed.html (star)
```

---

## 4. D1 — densify existing rooms (real pages)

Add **up to 4 extra HTML files per existing dir** only when that surface existed in 2009. Incomplete never writes. Never invent brand pixels.

**Minute build (every extra page)**
1. Create `years/2009/sites/<slug>/<page>.html` (dir already exists).
2. Period action: one writer **or** honest literacy dead-end.
3. Honesty strip: class + date + “museum original · no commercial SWF”.
4. Prefix `itt09-*` only. Empty / 0–1 checks **never** write.
5. Next chip → `sites/facebook/feed.html` (Facebook Like).
6. Add `href` to home atlas + `flow-maps-5x-atlas.js`.
7. `js/config/2009.js` urlMap + titleMap for the new file.
8. e2e: open · incomplete · complete writes only if writer.

No extra D1 rows — dests already at target. Hygiene only.

```
[Start] → open extra page → do the period action
              │
     empty / timeout / 0–1 checks → NO WRITE
              │
     complete (writer only) → localStorage itt09-<slug>-<page>
              │
           Next chip → sites/facebook/feed.html
```

---

## 5. D4 — forest remainder (non-Yahoo densify)

Do **not** add new `sites/` dirs. Close the remaining L1/map gap with D1 extras on Amazon / eBay / Hotmail / Google / AIM / GeoCities homestead / NASA / White House — **never** `/yahoo/Art/Photography/`.

Remaining after D0: **+0** pages. Use the D1 table above until unique dests ≥ 290 and map ≥ 90.

---

## 6. Flow-map dests

Add new `href`s to `ITT.flowMaps["2009"]` via `js/config/flow-maps-5x-atlas.js` branch **“5× atlas”**.

1. Every leaf: `name`, `href` (must exist), `do` (period verb + key if writer).
2. `node scripts/audit-every-flow.js` must stay green.
3. No `#`. No `pages/error/*`. No `?night=`.
4. Gate: unique hrefs ≥ **90**.

On-disk rooms to reuse: `adsense, altavista, amazon, android, apple, appstore, ask, askjeeves, aws, bing, blogdex, blogger, bloglines, bowienet, chrome, cnn, daypop, delicious, digg, dmoz, docs, dropbox, ebay, encarta` …

---

## 7. Home chips (L1)

`years/2009/pages/home.html`

1. Keep the locked star chip.
2. Add chips until unique dests (home+map, no 3×) ≥ **290**.
3. Every chip `href` exists. No `#`. No 404. No `?night=`.
4. Optional `data-trail-keys` for writers so home shows ✓.
5. Guided `<ol>` stays **6**.

---

## 8. Phases (implement in order)

### D0 — Leftovers
Already listed 2026-08-16. Re-run only if a leftover href 404s.

### D1 / D4 — Densify non-Yahoo
**Files:** extras in §4 · urlMap · atlas · home  
**Gate:** unique dests ≥ 290 · map ≥ 90 · `audit-every-flow`

After each phase: one-thing `itt09-fb-likes` still writes · empty still blocked.

---

## 9. ROI

| Spend | Visitor gets | Skip |
|-------|--------------|------|
| leftover chips (D0) | “I can go there” on files already on disk | `?night=` |
| 4 extras / existing dir (D1) | period verb × rooms you already know | clone-Yahoo |
| map + home chips | L1×5 and mapU×5 **as files** | 404 chips |

**Why this year:** Like. FarmVille. Bing. Foursquare. Win7.

---

## 10. Acceptance (this year is real-5× dests done)

- [x] flow-map unique hrefs ≥ **90**
- [x] home+map unique dests ≥ **290**
- [x] every new href is a real HTML file (no `?night=`, no 404 chip)
- [x] no new `sites/` dir unless lean + dual-cited + named in §5
- [x] `itt09-fb-likes` still the locked star
- [x] incomplete never writes
- [x] guided `<ol>` still 6
- [x] `check-all-years.py` pass
- [x] `audit-every-flow.js` + `one-thing-per-year` grep `2009` green

---

## 11. Command

```
implement 5x real dests 2009
```

Do **not** implement another year in the same pass.
